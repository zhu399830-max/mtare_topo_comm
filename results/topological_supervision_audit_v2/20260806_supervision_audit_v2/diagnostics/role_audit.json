{
  "test": {
    "canonical_role_field_available_in_loader": false,
    "directional_role_must_not_be_reused_as_canonical": true,
    "note": "The v3 sample contains rotation_canonical_role_teacher on disk; the existing training loader ignores it. The audit contract separates it explicitly.",
    "topological_role_teacher_shape": [
      64
    ]
  },
  "train": {
    "canonical_role_field_available_in_loader": false,
    "directional_role_must_not_be_reused_as_canonical": true,
    "note": "The v3 sample contains rotation_canonical_role_teacher on disk; the existing training loader ignores it. The audit contract separates it explicitly.",
    "topological_role_teacher_shape": [
      64
    ]
  },
  "val": {
    "canonical_role_field_available_in_loader": false,
    "directional_role_must_not_be_reused_as_canonical": true,
    "note": "The v3 sample contains rotation_canonical_role_teacher on disk; the existing training loader ignores it. The audit contract separates it explicitly.",
    "topological_role_teacher_shape": [
      64
    ]
  }
}