# Preview provenance

Every preview must record split, world, trajectory/sample ID, method, units, and the hypothesis it supports or contradicts.

This infrastructure task generated no scientific preview: there was no dataset,
trajectory, metric distribution, model output, or planning comparison to visualize.
The relevant evidence is the machine-readable validation and test report in
`metrics/` and `logs/`.
