# Preview provenance

Every preview must record split, world, trajectory/sample ID, method, units, and the hypothesis it supports or contradicts.

No preview was produced. The original script failed before completing its first grown tunnel, and it contains no native topology, point-cloud, or mesh export path. Creating a replacement preview would misrepresent this failed source/executability run.
