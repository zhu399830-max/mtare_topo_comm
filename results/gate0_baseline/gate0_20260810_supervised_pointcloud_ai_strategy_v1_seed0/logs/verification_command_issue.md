# 文档检索命令转义问题

- 事实：一次只读 `rg` 核验把 Markdown 反引号放入双引号 shell 参数，zsh 尝试执行字符串 `NONE`，返回 `command not found`。
- 影响：该次检索命令不能作为完整证据，但其他 JSON 解析和 13 项单元测试均已独立通过；没有文件写入或研究动作被触发。
- 处理：改用单引号固定模式重新执行只读检索，并保存最终一致性结论。
