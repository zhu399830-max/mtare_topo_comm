{
  "schema_version":"v3_data_export_proposal_v2",
  "proposal_id":"cano_phase2_supervised_range_dataset_v2",
  "status":"APPROVED_FOR_ONE_IMPLEMENTATION_AND_ONE_EXECUTION_NOT_YET_EXECUTED",
  "gate":1,
  "question":"Can the sealed 80/10 procedural topology split produce a spatially balanced, fully auditable 112500-frame supervised range-image dataset after objective eligibility is known?",
  "data_card":"configs/v3/gate1/data_cards/cano_phase2_supervised_range_dataset_v2.json",
  "source_selector":"results/gate1_data/gate1_20260812_cano_phase2_selector_v2_feasibility_audit_seed0",
  "data_scope":{
    "train_worlds":80,
    "validation_worlds":10,
    "development_test_worlds_read":0,
    "mtare_worlds_read":0,
    "candidate_clusters_evaluated":27247,
    "candidate_frames_evaluated":136235,
    "dual_scene_candidate_scans":272470,
    "selected_clusters_stored":22500,
    "selected_frames_stored":112500,
    "training":false,
    "models":0
  },
  "method":"Evaluate all candidate clusters with the frozen scan/teacher/LOS/replay contract, persist all eligibility and rejection evidence, then solve the frozen selector-V2 MILP over eligible candidates with exact role and length-proportional tunnel quotas, all events, every tunnel and <=10m coverage. Export only the final selected set to 90 Zarr shards.",
  "baseline":"V1 sealed dataset is reproducible but unusable because 50/90 worlds omit 58 tunnels and gaps reach 330m. The zero-ray V2 feasibility upper bound has zero missing tunnels and <=10m gaps before eligibility filtering.",
  "no_ad_hoc_replacement":"If a pre-eligibility selected candidate is invalid, the entire world selection is re-solved over the complete eligible candidate set; sequential same-role replacement is forbidden.",
  "evidence":"27247 candidate records, 136235 frame audits, explicit rejected-candidate reasons, exact final selector manifest, 90 Zarr shards, per-world/aggregate metrics, ten complete train sample pages, ten train spatial maps, one train distribution figure, source before/after seals and final run seal.",
  "acceptance":"90/90 worlds; exact 22500 clusters/112500 frames and frozen role quotas; 0 missing tunnels; <=10m maximum same-tunnel coverage; 634/575 events; 0 replay/teacher failures among selected frames; complete evidence; <=3600s and <=12GiB; zero C10/M-TARE/training/models.",
  "estimated_cost":{"disk_gb":12.0,"wall_time_hours":1.0,"compute":"CPU-only Open3D dual-scene eligibility over all 27247 candidates, then Zarr export; no simulator, GPU or training."},
  "approval":{"status":"APPROVED","approved_by":"user","approved_at":"2026-08-12T16:09:21+08:00","scope":"One implementation and one formal V2 eligibility-aware data export plus objective teacher generation; no training.","confirmation_reference":"User replied '同意' on 2026-08-12 after the exact V2 scope, data counts, method, evidence, limits and no-training boundary were presented."}
}
