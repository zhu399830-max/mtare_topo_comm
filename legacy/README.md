# Legacy 索引

这里不复制旧代码和旧结果，只记录它们在 V3 中的真实用途。

- `CODE_ASSET_MAP.md`：旧实现的复用候选、历史 baseline 和淘汰候选。
- `RESULT_ASSET_MAP.md`：旧结果属于哪类证据，禁止支持哪些结论。
- `FAILURE_KNOWLEDGE.md`：已知失败模式、证据和重新验证要求。

旧资产默认不可进入 V3 数据、训练、调参、checkpoint 选择或最终 test。
