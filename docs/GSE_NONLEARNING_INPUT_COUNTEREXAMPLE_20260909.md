# 旧出口适配的输入失配：合成反例

2026-09-09完整感知/局部拟合版本完成：full_sensor_local_fit复用原50m距离图提案、仅10m原回波拟合，不裁远回波造表面；30软件测试0.40秒通过。同T形3单帧视点均恢复3候选，但有限直线交汇输出仍0。中心三条有限拟合折线离真实路口均3.0292m，中点偏弦0.0562m；不是只有0.01m合成容差问题，观测表面拟合范围不包含路口中心。合成原参考只用于诊断。停止把该有限轴线适配推进正式307节点基线；不扩大范围/延长轴/调阈值。下一只读审查已有骨架/自由空间建图组件与成熟基线接口，选择能表达路口区域的非学习参照，旧出口保留原输出比较；不重训、不开正式run。此反例不否定学习主线或全部几何方法。

2026-09-09完整感知输入元数据绑定完成：development_sensor_scope核对10个冻结元数据文件，307身份/分区/feature来源行与输入SHA一致；250/27/30、56父地图、156任务、1529去重变体帧，任务容器共2184观察。scope SHA17c94619c34a60861bdc5507ae010749eeb5411234db0cc8a8a1d2235f03a334。未读sensor/teacher/checkpoint payload；5漂移拒绝测试0.10秒通过。下一独立版本使用完整感知提案、原10m点局部拟合，合成核验后精确卡/spec及真实输入字节认证，不重采集/编码，不改307选择、评分或旧run。

## 结论

不能把“所有方法都使用10米内点块”等同于“所有方法只有10米感知”。学习方法还接收从完整距离图提取的冻结上下文。因此当前只向传统出口方法提供ROI表面回波的适配不能直接作为公平对照。

## 实际检查

命令：`PYTHONPATH=src /home/zeng-workstation/.local/share/mtare_topo_comm/envs/phase3_torch290_cu129_zarr2187_v1/bin/python tools/v3/check_observed_axis_synthetic.py`。

无地图文件、无教师文件、无GPU、无训练。用两个轴对齐长方体的空气体积并集生成T形通道；按现有16×720射线求并集首个出界点，传感器在x=-4/0/4米。只填五帧接口的当前帧，不能把它写成五帧感知总体成绩。其余帧未知，不重复当前帧伪造历史。

| 视点x | 10米ROI回波 | 方向候选 | 路口输出 |
|---:|---:|---:|---:|
| -4 | 10956 | 2 | 0 |
| 0 | 9828 | 0 | 0 |
| 4 | 10956 | 1 | 0 |

两侧拟合虽有表面回波支持，三个控制点不能在本次0.01米合成容差内表示为直线，因此被拒绝。该数值是已有精确几何软件测试值，不是校准参数，不据此宣布真实曲线拟合失败。

中心视点独立输入兼容检查：10米候选0；保留50米量程内首返回后有11520个点，候选为84、174、354度。只改变合成诊断的可用回波范围，不变更真实训练输入，也不是阈值搜索或新排名。说明删去远距回波会删去旧长距离扇区方法依赖的信息；完整回波产生候选并不证明其路口位置准确。

## 代码依据

- `tools/v3/surface_features_v1.py:135`：原始range_valid、translation、yaw进入公共冻结特征适配器。
- `src/mtare_topo/representation/primitive_relation_model.py:179`：`_memory`从完整距离图与注册点计算上下文，未施加10米ROI。
- `src/mtare_topo/data/development_compact_blocks.py:43`：筛10米点和射线索引，同时保留原900×128上下文，再按点块汇集。

所以不能把ROI-only传统方法结果与当前上下文学习方法直接解释为几何学习增益。现有三组学习方法共享该上下文，此发现本身不否定三组配对；它否定的是新旧基线的输入等价假设。也不涉及测试泄漏。

## 后续决定

停止ROI-only适配的正式运行准备。先核对已有307观察的完整原始距离图/相对运动绑定，区分共同感知输入与10米输出/几何作用域，再定同源传统对照入口。用户已有范围内的只读源核对无需新增批准。若需要变更真实实验输入合同，必须独立版本明确登记，不静默覆盖。传统方法水平投影的局限仍要报告，不能把完整量程恢复当成三维层级问题已解决。

新增`observed_axis_structure.py`和3项接口测试，合计27项测试0.35秒通过，仅证明软件行为。合成反例可由上方脚本复现；它不是正式研究run，不替代307样本的结果。
