# 双向观察配对训练：读入交接

更新：`test_partial_target_loss_payload_equivalence.py` 新增5项合成测试，连同原损失回归共14项通过（0.99秒）。两种分支完整性下，三字段载荷与含额外诊断的载荷得到逐元素一致的损失、匹配和梯度；来源损坏仍被拒绝。真实860项适配及其内存尚待验证，不能用合成测试替代。

本记录是现有代码和封存元数据的只读核对，不是模型成绩或新训练授权。

## 已经具备什么

860 个五帧观察的公共特征与三种分块读入均已验证。拟合 282、校准 578；本人口没有独立开发评价部分，不能在校准数据上调阈值后再声称泛化成功。

原方向拟合 141 项使用已核验的跨运行索引，其余反向拟合 141、正向校准 289、反向校准 289 使用各自封存的 target_manifest。四部分与公共输入按任务、序列和五帧索引连接，860 个身份恰好覆盖，无重复或缺项。这只是元数据完整性，尚未证明全部结果能经过损失适配器。

## 必须复用的读入顺序

1. 从已冻结的输入引用读取五帧扫描和运动；构造与传感器参考只进入损失侧。
2. 读取原紧凑特征与同点分块。模型输入不含教师编号、候选集合或真实连接。
3. 接入已绑定的射线网格，保留自由、占据和未知三种状态。原 27 项复用，缺 833 项正在唯一运行 `gate3_20260909_gse_bidirectional_grids_v1_seed20260906` 中导出。
4. 逐项读取并验证封存 V8 结果，调用现有 `archived_junction_targets`。该适配器验证构造关联、进入方向证据和坐标，不把裁剪端点变成物理开口。
5. 训练沿用 `conditional_branch_selection` 和查询级条件负监督。未知不作为负例；已匹配正例不同时受负监督。

## 需要避免的内存问题

仅三个完整新运行的 719 项原始诊断 JSON 就有 **11,566,127,788 字节**，最大单项 **49,996,618 字节**；还不含原方向拟合 141 项。JSON 解析成对象后通常进一步膨胀。不能把所有原始诊断留在训练列表里。

应逐项验证和适配，立即释放原始诊断。现有节点损失实际读取 `produced_targets` 的 `record`、`source_binding` 和 `target_record_sha256`；分支方向及完整性由已验证的类型化 target 承载。任何精简都必须以原损失输出及梯度等价测试验证，不能仅因字段看起来不用就删改评分所需证据。原始文件全部保留。

## 这轮可以和不可以回答什么

可以先诊断：双向输入下，模型能否减少固定后方位置模板，关系模块是否改善有证据的节点位置与分支方向。

不能据此宣称：完整开口检测成功、地面物理可达性成功、严格未见泛化成功，或整个探索系统已经优于基线。当前没有有效尽头标签；构造分支完整性不等于所有物理开口完整性。

## 绑定证据

- 原方向 141 索引：`docs/figures/gse_graph/v8_saved_paired_audit_20260909.json`，SHA-256 `42026d9bf0c38af1c185ae1a2d291842dbd48e993e9fe414d657668ea9b23d65`。
- 反向拟合 target_manifest：`6c3f0fcdd7d607ca6a5ea5657f87f3464766faf08532ad1bdc6bf4bbbdc86f47`。
- 正向校准 target_manifest：`b5eea1c1d510c51b5d100db77a3679c3f8b90c332f085c129f21942c660f327e`。
- 反向校准 target_manifest：`90ff2871cb9ea38b8acc08ec85019c90ca9f9dc6cbd7e39b959761afd684be49`。

以上三个 manifest 均通过各运行的已固定 seal 读取，并核对其证据文件哈希与 seal 一致。该核对未再次逐个解压全部结果，后续适配器必须完成内容验证。
