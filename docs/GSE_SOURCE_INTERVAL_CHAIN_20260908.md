# 数值边界与连续通道检查结果

## 本次完成

实现 `gse_source_plane_intervals.py`：对原float64坐标与实际float32坐标构造逐坐标包络，减法、乘法、加法使用向外舍入，计算射线/端面平面参数分子、分母的符号区间。只有整段区间同号才标记forward；跨零或平行不确定则unknown。没有拟合距离容差，不把该判据称为物理安全或完整拓扑证明。

精确Fraction有理数检查覆盖24组几何的原始与量化行列式，验证结果落入计算区间；合成测试包括明确进/出、反向、平面边界和量化翻转。该区间只覆盖数值表示变化，不覆盖传感器噪声、建模误差或任意坐标变换误差。

对同四观察只读应用，结果：

| 观察 | 稳定进入 | 方向未知 |
|---|---:|---:|
| S04 mixed | 1696 | 0 |
| S04 ellipse | 1887 | 0 |
| S04 rounded_rectangle | 1696 | 0 |
| S07 rounded_rectangle（两候选共享） | 0 | 1356 |

因此S07不能靠原来这组端面进入见证认证负归属；S04入口证据在这个数值范围内稳定。未改写原标签或失败运行。

## 独立连续通道重建

新增 `gse_reference_chain_audit.py`，复用完整文件解码，但不调用生产器连续段分组：从全部双端所属节点和真实端点坐标构建邻接表，独立遍历精确重合的degree2连接。缺端点、未解间隙、错误保存分组均拒绝，不使用anchor连接线补几何。

原五候选全部与独立分组一致：

- S04尽头通道只有1个source，开口通道由13个source连续组成；分别为node_0010↔node_0013、node_0013↔node_0053。
- S07尽头通道1个source，另两个开口通道分别3和10个source；共享node_0097，但不延续至该尽头node_0100。

这排除了当前候选“只是通道编号切分不同”的错误解释；并不抵消S07的观测数值边界问题，也不意味着不同连续段之间物理不可达。身份只在离线参考检查中使用，不进入模型。

20项区间/连续段/射线/篡改测试0.25秒通过。实际两个只读工具均退出0，没有新标签、训练或第三次教师批次。保留全部旧证据及论文图。

## 下一步

把已验证的边界资格检查接入端面见证的非标签验证接口，并检查S04开口到路口的其余见证，而不是再发明一个教师版本或直接扩人口。仅证明连续段和入口方向仍不足以认证完整检测背景；不得跳过标签字段资格开正式比较。
