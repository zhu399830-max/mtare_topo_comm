# 路口观察支持诊断：实现失败，停止本轮执行

2026-09-07。本轮没有获得路口支持结果，没有训练，更没有证明方法无效。

两次运行都在首个S01混合几何观察处停止，完成观察数均0：

| 运行 | 用时 | 直接原因 |
|---|---:|---|
| anchor diagnostic V1 | 8.107秒 | 新解析器要求`world`，真实坐标名是`cano_world` |
| 独立V1R | 8.060秒 | 新解析器要求`compositions`，真实字段是`composition_operations` |

失败原因是新增解析器没有准确复用现有文件合同。原`PrimitiveConstructionGraph.as_dict()`和`load_p1a_realized_construction()`已经明确这两个字段；我在合成测试中使用了自己构造的简化格式，遗漏真实格式回归。不能把测试通过解释成真实接线可靠，也不能将这些错误归于数据、教师原理或模型。

V1源码和结果未覆盖；V1R独立封存。两份seal已逐项复核：

- V1：`c3b8ff4f1636e12952585015f9e92036a89c323b8ab4318747793dc5f45cc387`
- V1R：`66b058b4524fd92f693bb4ade0c678f855e9f92d728f5eec20825e4d8efddc81`

## 停止边界与建议

用户方案规定每类问题最多一次修正。当前来源格式适配问题已修正一次但仍失败，因此不创建第三次运行，不以换版本名绕过预算。之前的30观察/63截面诊断仍是独立有效的section证据，不受这个新增解析器错误影响；仍不能据其放行训练。

建议若允许继续：停止逐字段补丁，改为复用原source loader及serializer，用它们生成的完整原格式做集成回归；一次核对坐标名、组成字段、度数、端点与源轴线，再冻结独立兼容修正运行。该行动不应改地图、数据、标签含义、阈值或模型，但需要明确调整本轮“一次修正”的执行限制。另一选择是维持原限制，本轮诊断结束，保留已有工程成果，不宣称研究完成。

当前新语义标签0、训练0、路口观测人口未知，目标未完成；无后台进程。
