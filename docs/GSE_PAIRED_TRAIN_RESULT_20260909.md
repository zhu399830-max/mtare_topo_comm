# 三种分组表示：首轮真实数据配对训练结果

本轮已完整结束，但没有证明几何分组改善结构理解，停止扩大训练。不是重新生成地图，不是模型尚未运行。

## 做了什么

同307个五帧观察：250拟合、27校准、30开发。共享冻结点云上下文、488584参数初始化、样本顺序和损失。扫描布局R0、体素R1、经典几何超点R2各2000次AdamW更新，micro1累积4。全部初始和最终结果保留，不选最好轮数。只研究部分路口/支路参考，不是完整节点检测或图实验。

## 实际结果

预先固定的4米路口匹配、10度方向匹配，开发集30个路口出现和90条已见支路：

| 表示 | 匹配路口 | 已证错误/重复路口预测 | 未决路口预测 | 正确支路 | 已证错误支路预测 | 未决支路预测 |
|---|---:|---:|---:|---:|---:|---:|
| 扫描布局R0 | 27/30 | 33 | 0 | 4/90 | 8 | 0 |
| 体素R1 | 27/30 | 63 | 0 | 5/90 | 144 | 0 |
| 超点R2 | 29/30 | 82 | 1 | 0/90 | 331 | 64 |

匹配到路口不等于检测可靠：4米容差较宽，重复点明显。超点多匹配两个路口，却同时产生更多错误预测，支路没有正确恢复，不能称其胜出。未知预测单列，没有将它们视为正确或负例；不报告伪造的全场景F1。

拟合集已见支路的正确匹配也仅为R0=48/774、R1=31/774、R2=30/774。因此不能只把问题归因于开发集泛化；方向输出、存在概率与损失决策链需要从保存预测中分开检查。当前证据不能证明几何方法在原理上不可行，也不支持盲目扩大数据、轮数或种子。

## 证据完整性

正式运行：[原始摘要](../results/gate3_semantics/gate3_20260909_gse_development_paired_train_v1_seed0/metrics/summary.json)。会话25722 exit0，1489.66秒，峰值RSS5125230592字节，结果161851187字节。

独立检查会话87771 exit0：6000条更新记录逐条对应共享拟合调度；三个最终优化器均2000步，参数有限且不同于共同初始权重；1842份初末预测和16578条评分记录覆盖固定人口与九档敏感性，形状、有限性、来源、分区和已见目标分母正确；1874个封存文件哈希及集合完整通过。

seal SHA-256：`fb156c3b7fb35d0125669b9d5e65962ee2213ef16b7f469410f0928a9a9977eb`。

这项校验确认运行证据完整，不等于已经独立重放每次网络推理或证明评分语义无误。

## 下一步

只用本轮保存的预测、损失和原目标做错误分解：区分方向本身不对、置信度压掉正确支路、路口关联造成支路漏计，以及重复节点为何未受充分约束。同步产出同场景点云/目标/预测对照图。先定位具体环节，再按预登记的有限修正分支处理；不启动额外种子，不进入建图/探索，不改旧run或匹配阈值。
