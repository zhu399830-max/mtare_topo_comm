# 固定批次的训练前置结论：当前不具备正式比较条件

2026-09-07。结论是本批正式A/B/C比较 `NOT_READY`，不是整个方法 `GATE_FAIL`，也不是模型已训练失败或项目完成。

## 已核对证据

1. 公共特征已经真实完成3360观察、源帧16800，零优化器更新。不是等待GPU。
2. 封存空间覆盖summary的 `coverage.development.by_kind`：5个junction/5父、5个terminal/3父；109个construction_segment不能改算语义节点。当前240开发观察不能当240独立结构。
3. 端面V2已有支持涉及4个独立开发尽头；分支来源记录涉及5个路口，但未建立合格局部连接标签。即使暂把这5个路口全部算候选，也不能由现有支持记录证明10个有效锚点。
4. 另一个末端S04C07node0055无端面匹配，且中心位于另一通道内部。保持未知；点级包含不能外推整个端面不可见，但现有证据不支持把它晋升正例。
5. 新合格标签仍为0；可评分完整区域和物理通行标签也未通过。原32有效观察拟合与正式A/B/C均不能用未知或候选替代前置条件。

这不证明五帧原理不可学，也不证明今后绝对无法利用原批次。它只说明当前证据不能支持正式实验；继续位置代理、编码器提取或追加种子不能替代标签证据。

## 受影响工作与仍保留成果

停止本批正式训练/成绩比较的创建，保留全部3360公共特征、原输入、候选、源几何、旧模型和论文图片。Phase3目标保持未完成，不以软件完成缩小目标。原实验不改写，未知不改负例或正例。

## 有界修正建议（尚未执行）

建议独立补充开发人口合同，仅使用既有C01–C07和原父地图分区，不读取C08–C10，不改阈值、教师语义或模型。先按固定、与模型分数无关的结构/背景分层规则核验库存，再冻结一次补充清单；不边训练边补样、不用相邻帧凑独立实体、不用构造标签直接冒充观测标签。必须同时覆盖负例背景和未知，而不是只增加容易的正例。

此建议改变原每父16边的固定采样约束，超出普通格式修复，需明确的采样合同变更授权。新增准确数量、有效比例及成本不能预先捏造；元数据库存后给出确切数据卡，再执行受控导出。原批次可继续作诊断，不自动拼入新排行榜。

备选是维持原固定人口继续实际标签复核；可以得到局部诊断，但不能承诺其能达到人口线。降低10实体门、补造标签或将构造分段改算语义节点均不采用。

推荐补充人口而非降门或继续重复诊断。当前没有运行中的训练，也没有启动补充数据读取。此为首轮明确记录的采样合同变更阻碍；目标未标complete/blocked。
