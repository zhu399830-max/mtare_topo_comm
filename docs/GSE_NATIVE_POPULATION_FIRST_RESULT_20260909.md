# 官方建图首次真实输入：输出合同失败，不是模型训练失败

## 独立读取修正与最终图统计

新增native_candidate_graph_v2，唯一放开的是原生自连接，不修改原V1检查器或任何原图。12项新旧检查测试通过（0.09秒），同端点保留，缺失端点、错配审计、非有限值、重复审计ID、无TSDF支持继续拒绝。

inspect_native_saved_graph_v2.py校验原图SHA后完整读取成功：1480点、2508边、12自连接、65条重复无向边（相同无向端点的额外条数）。独立DFS得10个分量，规模781/681/4及7个2；多重图cycle rank1038。官方stderr显示11个分量，不与最终DFS一致，暂不推测具体缓存/计数原因，采用最终连接计算，不引用日志11作为图指标。

原边审计中28条含未知采样、40条含低于0.4米采样，最低0.226663米；两类可能重叠。这些是原图风险属性，不是修正后放行路径，更不是地面通行证明。所有2508条原边都保留，自连接不表示机器人移动。

本次没有重新调用原生算法、没有读取扫描、没有恢复旧run。原首例输出现在可解释，但仍无完整307人口结论、语义节点计数或已穿越连接成绩。下一可以登记独立V2人口输出合同：只允许忠实保存原生自连接，并单列风险；不改算法/数据/参数，不把旧失败改为成功。

正式批次 `gate3_20260909_gse_native_graph_population_v1_seed0` 已执行并终止，不能续跑或覆盖。预检使用已绑定Python环境通过，系统Python因无NumPy未进入预检。会话81897 exit1，总耗时2.340秒。

第一例原生算法正常返回1480节点、2508边，外层检查发现12条起终点相同的边，报 `edge/audit mismatch`，因此正式完成数为0/307，而非没有生成任何图。原图、逐边诊断和stderr均保留。15份封存文件独立重算哈希一致；seal为404201d1f72984f760753d7fb1919a26a92ea3740e7c871638bb3bb6438b15aa。

## 具体问题与判断

12条边均是自连接，端点存在，edge_audit端点与原边一致；不是节点ID丢失或日志错配。自连接ID为3438、3529、3548、3564、3750、3892、3921、3937、3983、4101、4112、4113。

本地固定官方 `skeleton.cpp:77` 的addEdge没有拒绝同端点；`skeleton_generator.cpp:1620`附近简化两个相邻边时，也未要求两个外端点不同。因此官方数据结构和简化路径允许产生这种输出。这是源码可解释的机制，不是已跟踪证明这12条边全部从该分支产生。

我们保存的是官方原生候选多重图，而检查器额外要求无自连接，二者合同不一致。失败否定本次导出完整性，不足以否定官方方法或学习几何方法。1480个几何点不能称为1480个真实路口；stderr还报告11个分量，后续需与观测支持一起解释，不能按节点数宣布效果。

## 处理选项

1. 推荐：新版本读取合同允许原生自连接、单独统计并明确不可作为移动路径；首先只对已保存输出验证，不重积分、不动旧run。全部原始结果保留。之后如需继续人口，必须独立登记新版本及失败原因，不直接重启这份run。
2. 修改官方剪枝或删除自连接：会改变参照算法，不适合作为当前原生基线，不采用。
3. 停止整个基线：当前证据不足以支持，暂不采用。

推荐只修输出解释而非放宽导航安全：自连接可以作为原生算法产物保存，但不能算恢复了一条可行驶通道。当前尚未修改已冻结检查器、追加正式运行或重训；按用户持续授权可推进独立版本诊断，无需新的数据或测试权限。
