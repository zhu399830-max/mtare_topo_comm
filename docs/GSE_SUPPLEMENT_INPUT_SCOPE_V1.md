# 只补新增扫描输入，不重做原批次

已从封存补样及原选择清单编译输入范围，未读取扫描或运动数据块。C01–C07原父地图划分不变。

实际新增2676个五帧观察：拟合2310、校准189、开发177；其历史去重为13374变体帧。原3360输入保留复用；合并人口6036不变。新增历史允许重叠，不能当成独立地点。

2175元数据读取并前后哈希核验完成（约0.403秒），下一导出精确涉及14589文件、其中12489数据块。累计解码3,546,934,272字节，包括压缩块内邻接行；逐任务解码释放，不是峰值RAM或有效训练数据量。尚未执行导出或生成标签。

范围SHA：`0c3b8512bd937e30cc02c918ccc4d506aa59f4d455ba7219bd04d828e9e096cb`。代码`gse_supplement_input_scope_v1.py`从两份封存选择编译，不按模型分数筛样，字段只有range、valid、五帧ID、源序列ID、相对translation/yaw。构造、绝对pose、teacher目标不得进入学生输入。

旧读取器默认仍严格16观察/80帧；新增显式variable_population模式支持不同任务观察数和重叠历史。对实际合成Zarr的两观察/六唯一帧检查验证数值逐元素相等、全部精确文件仅一次读取、无teacher目标访问，旧默认拒绝该人口。与旧输入/补样回归共53项通过（0.53秒）。历史run及源码快照不改写。

下一项将可变输入模式绑定新card/spec/执行器，唯一导出新增2676观察；随后复用观测支持模块处理实际标签及未知，不能凭范围成功开训。原3360输入和公共特征不用重算，全部旧图片保留。
