# 多位置监督核验的复用边界

2026-09-09隔离传输接线完成：`historical_teacher_client_v1.py`启动同一个旧CPU环境worker并按顺序请求；数组和元数据无损往返，不混载当前teacher包。每请求设置期限，压缩传输最多64MiB、返回JSON展开最多256MiB；错误终止进程且无自动重启。运行总内存/CPU/磁盘上界仍需外层executor绑定，不能把单包限制冒充整轮资源控制。9项软件测试0.78秒通过，包括持久请求、短读写、损坏帧、超时回收和读取器；使用合成元数据，真实教师调用0。下一是精确正式规格和全窗口覆盖核验，不是追加网络训练。

2026-09-09启动前校验补充：仅核对V6入口文件不足以保证教师相同。旧运行绑定的项目源码中，当前有17个文件哈希不同，包含几何采样、方向证据、锚点与终止结构相关依赖。因此尚未创建或启动新的正式教师运行。

处理方式是不覆盖当前工作区，而在独立进程中从旧运行的`source_snapshot.zip`导入完整历史包。归档SHA-256为`c4c0d42d844bfd6e514949efe7ba9f70952727ff6afa59752e72749e6a51fe97`。`teacher_snapshot_probe_v1.py`已在原CPU环境成功导入V6及原接口诊断，并逐个确认已加载的项目模块全部来自该归档，未读取数据或调用教师。动态加载仍须在实际worker里继续核查，导入成功不等于数值复现通过。

下一运行需要将新跨包输入读取与历史教师进程分离，传递明确的源绑定和原数值数组；不能在同一已加载当前包的进程中临时替换两个模块。先核验接口往返和历史模块来源，再冻结完整运行规格。若历史教师在新位置遇到问题，保留失败证据，不能静默换回工作区修正版。

输入已完成2259观察的跨包扫描和相对运动检查，包含65父地图、195变体任务、3039去重源帧；仅2079拟合和180校准，开发评价不进入本轮。

本轮不是重做教师方法。原307保存标签来自`gate3_20260908_gse_supplement_joint_v3_seed20260906`；其executor调用`gse_joint_reference_targets_v6.produce_joint_reference_targets`。原始接口证据来自`gse_junction_interface_diagnostic_v1.diagnose_observation`。下一正式核验固定复用这两个版本及原参数。

V7源码明确标为实验性，存在未通过的合成验收，不能因为版本较新就采用。V6仍然只提供部分构造参考监督，不是完整物理开口或安全标签；本次核验不会把未知变成负例或宣布背景完整。

`MultiviewTeacherReader`只改变输入包访问方式：每任务可从多个旧／新包取窗口，保留原六字段，并调用原`verify_alignment`检查构造/来源编号、回波有效性、源归属和绝对—相对位姿一致性。所有构造、codebook与绝对位姿留在teacher-only字段；学生前向接口不变。

已完成4项软件测试，无真实教师载荷读取。源边界已绑定1725文件，其中195构造记录、195来源codebook、三类原传感器字段所需header/chunk，预计151022400字节解码量。该数量是读取规模，不是标签有效性证据。

正式核验必须先冻结精确数据卡与运行规格，保存所有窗口的原始接口、目标、未知原因和来源。按同一结构在不同视点的可观察性统计位置分布；无正例窗口也保留。只有多位置监督与可评分背景覆盖得到证据，才决定是否进入一次有限修正训练。当前没有训练许可升级，也没有方法或建图通过结论。
