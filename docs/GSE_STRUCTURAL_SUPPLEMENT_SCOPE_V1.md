# 补充采样的真实位姿范围

本次只编译精确权限，没有读取真实位姿数据块或扫描，没有导出补充观察。

已读取并前后核验493个封存元数据文件：70个身份库存、210个构造记录、210个XYZ数组头、1个原分区、2个封存索引。仅C01–C07。构造文件内容的parent/variant与路径一致，三变体拓扑一致；原5/5 C07分区不变。

实际提名926个实体，对应918条不同有向穿越。其所有合法决策涉及32676个变体当前位置，210个任务。后续位姿读取精确限于210个头和270个数据块，共480文件；块解码上界20,517,552字节，包含块内非请求行，但只访问列出的决策行。该数不是新观察数或训练样本数。

范围SHA-256：`6fc278abeae006ac8b4f8ffb825644ee3675a96c113dc4d5524e9aa34cb42773`。首次编译3.966秒；补充构造内容身份检查后重编译，范围哈希完全一致。完整范围由编译函数确定，尚未写入正式run spec。

代码：`gse_structural_supplement_scope_v1.py`编译范围；`gse_structural_supplement_reader_v1.py`按父地图读取并释放位姿、调用已冻结采样规则。读取器会重新核对范围、原文件哈希及float64位姿，拒绝缺失/非有限/重复读取，不访问扫描与模型。无历史的实体保持缺口。

44项相关测试通过（0.49秒），其中包含实际合成Zarr解码和位姿块哈希漂移拒绝；不是用真实数据块测试。没有新标签或优化更新。

下一项：将已编译范围绑定新卡/spec及执行器，preflight后一次生成补充观察清单。再与原3360背景清单去重，计算实际源帧人口，并复用原观测证据生成流程。范围编译不是有效标签判定，不升级研究Gate。所有历史数据、特征和论文图片保留。
