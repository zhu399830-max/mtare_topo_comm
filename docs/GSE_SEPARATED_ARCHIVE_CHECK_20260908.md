# 平行／分层封存扫描与独立几何核对

固定原矩阵24条件：parallel/stacked×3截面×4视点，每观察5帧，合计120帧、1382400射线。使用原seal 5d491bd1c6c89caeb47ea416eb135fee8a1d8a3507f70d95256cb55a91d9a395，48个实际读取NPZ/JSON文件逐项SHA核验，声明与当前固定矩阵相等，传感器pose逐元素核对。未读其他场景、模型或真实世界。

独立convex_main_exit利用64边形有限棱柱半空间求射线首次退出，不调用生产mesh/CSG/teacher。31项软件测试通过，包括非单位方向的射线参数、越界原点拒绝。然后运行`build/gse_supercluster_env_v1/bin/python tools/v3/check_separated_archive.py`。

## 有效结果与错误记录

首次检查错误地对uint8掩码取反及索引，得到超过总射线数的无效计数；该次会话18307的所有数值作废，不能引用其残差。修正为先检查0/1再转bool，同范围只读重算，会话14646 exit0。bool/uint8/int64索引回归通过，非法值2被拒绝。原扫描、标签、run均未修改。

修正后的全射线统计：无效0，最大距离残差0.000006359044874670872米。未根据结果选择容差，故报告数值一致性，不宣称达到一个未预登记的安全误差阈值。射线方向使用原传感器方向并归一化，数值差包含浮点计算差异。

原24目标均无锚点且包含两个10米窗口开口；按原1米匹配接口与独立几何比较没有多报或漏报。空锚点F1仍为null，不能填成1；开口是局部窗口出口，不是门洞或全局边。这是原teacher在限定背景条件的证据，不是神经网络结果。

没有新增完整训练标签，没有重渲染或优化步骤。此前24条件未评分的旧文件保持不变，新核对单独记录。尚须处理多路口中每条支路的可见证据，不能将这24容易背景条件当成完整结构学习数据或替代关系贡献验证。
