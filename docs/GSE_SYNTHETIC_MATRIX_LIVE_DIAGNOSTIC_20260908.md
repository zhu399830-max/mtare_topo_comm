# 固定合成矩阵：运行中局部诊断

这不是完整实验结论，也不是模型训练结果。唯一运行目录：
`results/gate3_semantics/gate3_20260908_gse_synthetic_matrix_v1_seed20260906`。

## 当前已得到的证据

从已保存的实际扫描—标签记录读取，而不是只看测试断言：

- 圆形T路口四个视点的局部锚点依次为 `(4,0,-0.04)`、`(2,0,-0.04)`、`(0,0,-0.04)`、`(-2,0,-0.04)`。结合对应传感器位置，均落在同一个世界原点路口。这说明这些例子没有把机器人位置冒充结构位置。
- 圆形T路口四视点开口数为 `2、3、3、3`；椭圆截面前三视点同样为 `2、3、3`。远处视点的第三支路是否有充分可见证据尚无独立资格结论，不能直接将2判错，也不能将其视为正确。
- 三种截面的T中央视点均通过预先冻结的独立几何评分。圆形Y中央视点也通过。
- 截至同一进程第207.217秒完成39/144个主条件；已完成条件中28个独立几何评分通过、11个未评分。未评分不是通过，隐藏控制尚未执行。

## 对主线的意义和边界

后续同进程读到65个完成条件：33个独立评分通过、32未评分。圆形双路口在view0只输出世界位置(-4,0,0)的锚点，view1/2/3输出(-4,0,0)、(4,0,0)两个锚点；开口数依次3/3/2/3。该变化仍需独立可见性依据，不能仅据参考存在两个路口判定view0漏标。已写入结果约138MB，尚未完成或封存。

本轮检查自动训练答案的局部空间一致性及正确/未知边界，尚不能证明学习模型有效，也不能证明在线图关联成功。当前没有新增训练步骤。尤其不能因为理想构造锚点稳定，就宣称学生能够从LiDAR预测稳定锚点。

保持同一进程运行，完成后按整批原始证据汇总；不改变绑定源码、场景、评分或重新运行失败例。全部144条件中只有45项具有已冻结独立几何预期，另外99项保留未覆盖状态。
