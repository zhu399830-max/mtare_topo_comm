# 45观察候选与缓存已实际接通

加载器 `data/gse_candidate_cache_binding.py` 核验候选run的seal和逐文件SHA、历史紧凑缓存固定SHA，逐条核对观察、源帧、原输入绑定及编码器状态一致性。仅显式处理已知的current_sensor_m→current_sensor命名修正，不做坐标变换。

实际只读执行完成：45观察、32469候选，其中轴对34、面片32435，全部有最近有效回波上下文。没有重新加载原始扫描或checkpoint，没有重新运行编码器和训练。旧缓存文件包含历史目标张量，但加载器没有访问或返回目标字段，模型输入只取compact和来源头。

最大最近回波距离2.8021米。因此supported只表示上下文可查询，不表示候选位置有表面支持或结构已被证实。返回距离与索引，后续不可误用此标志。

几何未知显式保留：轴对只有位置字段已知；面片保留法向有效性、可用法向、点数和五帧支持。候选记录未保存的尺寸、粗糙度及法向不确定性不从其他缓存按索引猜配，不填成测得的0。新CandidateGeometryBatch包含18维数值及18维known mask；几何适配输入36维。此修改发生在任何候选模型训练前，不改变旧封存模型。

13项上下文/读出联合测试通过（0.75秒），包括改变未知字段的数值不会改变B输出。已实际加载的来源绑定和软件mask验证仍不是检测或建图成绩。

下一接八邻居关系和候选目标/评分：关系需要显式保留未知法向与未知射线证据，不能将面片与轴对同邻居视为通道连通。训练目标必须处罚完整直道背景及重复候选；未知区域不罚。先冻结损失和输出去重合同，再小规模训练，不再追加候选生成规则。
