# 修正扫描已转换为统一五帧输入

## 本次完成

没有重新计算射线。直接流式读取已通过的60帧封存记录，转换为12份五帧输入，并将来源和绝对pose独立保存。

精确data_export卡使用独立校验入口，拒绝扩大帧数、读取测试世界或打开teacher/training操作；不是把数据导出改称软件检查来绕过预检。21项联合测试通过，正式preflight无警告，单次create/execute会话48502 exit0。

| 项目 | 实测 |
|---|---:|
| 五帧观察 / 扫描帧 | 12 / 60 |
| 完整扫描位置 | 691200 |
| 本批有效返回 | 691200 |
| 保留多个报告来源的射线 | 77415 |
| 最大float32存储误差 | 1.9010877707614782e-6米 |
| 转换耗时 | 9.982989秒 |
| 峰值RSS | 111196 KiB |
| 输出总大小 | 7093030字节 |

float32存储误差与之前求交误差是不同的量，不能混写成算法精度。

## 输出与信息隔离

每个声明输出一份student NPZ、一份diagnostic NPZ和来源/资格JSON，共36份数据文件；另有源码、配置、日志及摘要。

student仅含五帧距离、有效掩码、相对平移与相对yaw。诊断文件保存绝对pose、reported退出表面来源编码。元数据明确training_eligible=false、structure_labels_present=false、source_completeness_qualified=false。距离证据绑定原始run与文件哈希，不由组装函数自行宣告通过。

独立复核了12份落盘文件：数据类型、60帧形状、有效掩码与零编码一致、多来源统计、相对运动从绝对pose重算逐元素相同。47个封存文件哈希一致。seal SHA-256：`b972519dca54a36466f56b7922b6ed2b120d44da725bbb31ce1c66ba2486f2d9`。

[正式摘要](../results/gate3_semantics/gate3_20260908_gse_covered_sensor_export_v1_seed20260906/metrics/summary.json)

## 尚未证明

本次没有teacher标签生成或训练。保留多来源不代表来源集合的结构监督含义已验证；不能把出口表面身份直接当路口或开口归属。

下一回到现有teacher接口，核对其来源、可见性及局部结构目标的具体使用方式，准备这12观察的明确监督审计范围。优先复用已有监督代码，不重新发明teacher，不扩大真实数据或训练种子。旧实验、原始扫描和论文图片不修改，Phase3仍未通过。
