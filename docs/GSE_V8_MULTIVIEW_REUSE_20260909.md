# 旧V8与多视点人口的复用边界

已验证两个原运行seal及本次实际读取摘要、清单、源码ZIP的SHA，精确按task＋source_sequence_id匹配。没有读取新扫描、重算教师或训练。

- 原V8固定10观察与当前2259观察：交集 **0**。
- 按task＋traversal_id比较：同穿越观察 **0**。
- 因此旧10例只能复用算法与机制证据，不能当作当前人口已有标签。也不重跑那10例。
- 旧V8归档488个src文件中11个与当前不同，包含CSG射线几何、侧向证据、内部证据、端面绑定和尽头处理；不能直接导入当前同名V8称复现。
- 原归档运行入口明确启用qualify_cap_precision=True，并从卡中传递geometry_settings；实际数值绑定仍须从精确原卡和导出链核验，不能仅引用文档。

## 最小有效的下一次真实检验范围（尚未执行）

最新13个路口正例所在的13个任务，保留这些任务全部决策窗口，共141观察、5父地图、193去重变体帧。选择依据是已定位的证据流失机制，不参考模型分数；不是无偏泛化人口，不用于训练成绩或校准。保留全窗口，不能只挑13正例和有利的下一帧。

下一先建立这141观察的精确来源清单，复用原2259读取器和封存输入；同时从旧V8完整源码归档隔离导入，绑定原卡几何参数。只增加确实缺失的多视点机制验证，不扩展到全部2259或改造新教师。原校准180观察无路口候选的问题不会被这批拟合诊断解决。

141只是已保存清单上的候选范围，不是有效标签数；尚未获得新版执行卡或启动许可绑定。进入真实执行前按原预检流程冻结，沿用已有用户授权，不隐式变更数据合同。

机器证据：[身份与源码核对](figures/gse_graph/saved_v8_multiview_reuse_20260909.json)。脚本：tools/v3/saved_v8_multiview_reuse_audit_v1.py。研究总目标未完成。
