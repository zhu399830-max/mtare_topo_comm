# 单点观测支持不能代替结构确认

只读检查94430 exit0。复用原307观察（250拟合、27校准、30开发）、两组最终预测和原射线网格；无模型调用、无teacher载荷读取、无射线重建。预测及卡依据训练seal认证；每个网格校验源绑定和内容哈希，选中查询与保存评分一致。下表中的匹配/误报分组来自已保存独立评分，仅用于事后分析，不进入观测证据接口。

| 开发集路口候选 | 自由体素 | 未知体素 |
|---|---:|---:|
| C0正确匹配 | 27 | 0 |
| C0已知误报 | 63 | 0 |
| C1正确匹配 | 25 | 2 |
| C1已知误报 | 5 | 0 |
| C1无法评分 | 0 | 91 |

校准集C1的27个匹配候选全为自由、81个无法评分候选全为未知。拟合集C1匹配候选220自由/6未知；已知误报35自由/7未知；无法评分候选745未知/2边界歧义。C0拟合匹配234全为自由，已知误报513自由/3未知。没有把边界歧义压成自由或未知的确定判断。

## 能得出的结论

单点是否被射线穿过，不足以确认路口。若仅保留自由体素中的预测，开发C0全部63个已知假路口仍会保留，而C1有2个已匹配路口会被排除。因此不采用单点自由硬过滤，不把这些反事实计数写成新的模型分数。原关系增量未通过的结论不变。

正确匹配允许4米位置误差，候选中心与真实结构支持区不完全等同；部分参考也不能判定所有未知候选为错误。不能将未知区域补成负标签，也不能从本结果选一个新的半径或支持量阈值。

## 后续边界

结束单点过滤支线。下一只读核对现有跨帧结构跟踪、候选/确认状态机及连续观测数据接口，确认是否能够使用真实连续观测累积结构证据。307个独立片段不能冒充连续重访，不按真实边身份拼接；没有连续证据就明确缺口。这个系统层核对不构成原学习贡献通过，更不能以规则过滤替代几何组合消融。

证据源：`results/gate3_semantics/gate3_20260909_gse_block_relation_train_v1_seed0`的固定卡、两组final评分与预测，以及卡内307个`grid_path/grid_sha256/source_binding`。适配代码：`src/mtare_topo/representation/candidate_ray_evidence_v1.py`。未修改任何旧run。
