# 位置绑定修正后的最终分数诊断 V1

Phase 3，2026-09-05。只回答：固定0.5决策全负的成员分数，是否仍有区分正负的排序信号？不重新训练、选模型或调阈值。

## 精确范围

原C01 S01—S10，180观察、900帧、1452可见片段。仅读取封存export的inputs/manifest/target_transport/summary四文件，及唯一geometry-bound训练的gt/predicted/no-relations三份final预测和summary，共八份payload。读取源哈希及相应卡/spec/seal仅用于核对，不增加数据人口。0历史权重、0骨干、0训练、0新扫描、0C02—C10、0图回放。

原有效分母：每支1206中心、886事件（872走廊、14交汇，终点0）、2152正成员/13489负成员；3正/21负原方向UNKNOWN不填。GT/预测数值有效候选2892/11520差异保留。全部是拟合数据，不是泛化。

## 固定算法和证据

复用未修改的独立纯中心几何评价，完整复现原aggregate、逐父结果、unique_center_query和scored_member_mask。只有原评分人口的分数进入诊断，未唯一匹配保留UNKNOWN，不用类别重新挑候选。

成员报告固定0.5混淆矩阵、正负概率分布、final logits BCE、常数先验BCE、AP及AUROC。交汇以原三类softmax中的junction概率做二类排序诊断，同时保持三类argmax混淆复现；不把二类排序当完整事件识别。相同分数必须成组，AP不依赖组内顺序，AUROC平局计0.5；任一类别缺失时两种排序指标为None，不能说该父地图成功。

全局及全部十父地图均报告。先验比例作AP参考，AUROC0.5作无排序参考；这里不新增科学通过线，也不搜索“最优阈值”。若关系分支明显优于无关系且有排序信号，只说明值得单独检验优化/校准问题，不宣布建图可用。若没有排序信号，停止扩大该训练接口，优先检查输入与监督目标可表达性。不用图planner调参掩盖失败。

冻结spec/data_export卡，预检后仅一个不可覆盖CPU运行，上限300秒、4GiB、0.5GB。保存每个原评分成员/事件的分数和身份、逐父指标及十父地图全部三分支概率图、日志、环境/命令/输入哈希、RUN_STATE和seal。任一输入漂移、评分不复现、人口变化或资源超限FAIL，不重试旧run。软件测试先覆盖完美/反序/全平局/混合平局、空/单类、非法数值以及完整合成执行路径。
