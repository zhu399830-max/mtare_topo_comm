# 一对一集合损失的有限修正依据

新增独立诊断 `evaluation/gse_candidate_set_loss_control.py`，未接入训练器。匹配优先保证1米内最大匹配数量，再考虑距离与分数；一目标最多一候选。匹配项用正BCE，背景使用最难的max(目标数,1)项，不把错误轴对冲淡到数万容易背景中。未知未匹配候选不罚，目标没有独立候选时报错而不是删除目标。

11项新旧反例联合通过（0.76秒）：多余高分正候选受到负惩罚；全拒绝/全接受均比正确集合差；近邻双节点可各自匹配，没有空间NMS；增加999容易背景不改变同一最难错误梯度；未知无梯度、缺覆盖失败。

必要限制：两个完全相同输入如果由共享确定性函数打分，仍不可能一高一低。该反例明确保留，不能用loss测试通过声称表示一定可学。只读核验封存C预测中的45观察：精确重复三维坐标行数为0。近重复仍存在，这不证明不存在数值/表示上的混淆。

## 可以执行的下一步

现在可登记一次有限修正，保持45候选人口、观测/几何输入、ABC初始化、种子、500更新及学习率不变，仅版本化损失和主输出合同：固定0.5分数的原始集合为主评分，不使用空间NMS决定成功；旧NMS结果可留作诊断。1/2/4米几何评分不变，0.90仍只是合成接口拟合门槛。

这不是悄悄更改旧成绩：新卡必须标明旧目标为区域软响应，新目标为一对一结构候选；旧run保留。新的反向资源检查和预检通过后只执行一次，不重新搜索阈值/候选/种子。若仍无收益，停止该候选读出扩训，不能依靠继续发明loss保底。

本轮0新训练和模型推理，只有软件反例及封存坐标检查。完整论文目标和开发/测试隔离不变。
