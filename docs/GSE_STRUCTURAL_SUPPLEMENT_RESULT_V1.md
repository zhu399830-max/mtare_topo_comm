# 结构补充采样已完成

独立运行：`results/gate3_semantics/gate3_20260907_gse_structural_supplement_v1_seed20260906`。

按用户同C01–C07补样授权，精确卡及源码/环境已冻结，preflight无错误或警告；create_run一次后执行一次，终态COMPLETED。原背景和所有历史结果不变。

实际结果：70父地图、926结构候选，各选一条事先哈希固定的有向穿越上的共同决策。三种实现都在10米范围内，缺失0；共2778观察、13872去重变体源帧。没有按可见性换方向或删除远样本。耗时5.031秒，进程峰值241,225,728字节，结果33,619,341字节。

全部82项seal重新SHA核验通过。seal SHA-256：`ab4405bf0b934ece6e6528595651aa464f65bc1b5bc444d234901efd7e19f0ec`。各父地图JSON保存结构身份、选定traversal、五帧行号、源序列、三变体距离；还有源码快照、原始进度日志、环境/命令、来源哈希、summary和RUN_STATE。

另对封存原背景清单与新选择进行只读去重核对：6138观察请求中102重复，6036唯一观察、30129去重变体源帧。没有改写旧清单，合并清单尚未正式导出。重叠历史帧不能当独立训练地点；统计仍按父地图。

该结果证明可按原数据取到结构附近的因果观察，不证明能看见所有结构、教师有效、物理可达或模型优于基线。新标签和训练更新均0。没有C08–C10读取，没有新扫描计算。

下一项为合并人口的精确扫描/相对运动/来源证据读取清单，复用已有无损输入与观测证据模块，只导出缺少的输入，保留原公共特征。按观测判断锚点、开口和未知，不能把926构造候选直接当训练答案。原物理可达标签停止备案仍有效。
