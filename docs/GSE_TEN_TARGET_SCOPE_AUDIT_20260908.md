# 真实部分目标能检验什么

实际执行`build/gse_supercluster_env_v1/bin/python tools/v3/audit_ten_target_scope.py`，正常退出。固定封存SHA36160f89c77382ff7ec7456634626a9ab78bd7d50dd7191349d81a263ab1b4a8；summary及10目标文件共11文件逐个验证，压缩/解压内容SHA均符合历史记录。没有扫描、模型、教师重算或训练。

实际目标经过现有observed_targets，而非仅复制旧汇总：19锚点、25开口；20正、20负、8未知，40有效归属项；0完整锚点评分区域、0完整开口评分区域、0物理可达目标。10观察来自3父地图、3独立穿越、44去重变体源帧。未核验的其他封存文件不能称全run重新核验。

所有10例都明确缺少complete_scoring_regions、reference_target_quality、terminal_correspondence_quality、terminal_exclusion_quality。损失接线正确不证明这些目标正确，也不授权完整检测F1或新训练。可验证部分参考关系的掩码，不能据此处罚整个观测中所有额外预测。

## 当前决定

停止重复查看同十例或给其追加教师版本，不重训旧45观察。已有144主条件矩阵只在45条件具备独立预期，99未评分；这才是下一项针对性的验证实现缺口，不是更多GPU或新的分块器能解决。

下一优先从多路口、上下分层条件建立独立几何判据的软件反例：输入为固定声明构造及因果可见证据，区分物理存在、局部可观察与未知；不能复制被检teacher的输出作为答案，不能将局部窗口裁剪端点当真实终止。先做评估软件，不重渲染矩阵、不静默提高旧结果资格，也不保证99条件均能得到确定答案。若只能支持局部关系任务，就明确保留该限制，不宣称完整检测或论文成功。
