# 旧V8执行通道与141读取范围接通

复用原持久worker的有界二进制传输、超时终止、压缩限制与失败不重启机制，仅显式替换旧V8加载器及归档SHA。初始与动态项目模块来源都检查为旧ZIP。客户端原V6默认绑定保持不变；未改封存研究算法。

8项传输测试通过1.04秒：含旧V6回归、V8同进程两次合成echo和3GiB地址空间限制。echo只验证输入原样返回，不调用教师。2项原读取器回归通过0.11秒。

141范围复用原多包reader与原数组访问计划，保留13任务完整窗口并校验与精确清单集合相同。绑定114个源文件、26个输入容器；teacher数组解码padding为9744896字节。193是去重变体源帧，不是193个独立实体。共享NPZ容器可能附带其他行，正式数据卡必须保留附带解码说明，不得声称只解压141行。

只读编译及reader构造exit0，opened为空，尚未读取真实payload。首次编译调用传str而原接口要求Path，已在适配入口规范为Path后通过；未改数据、算法或阈值。

新增工具：v8_historical_teacher_worker_v1、v8_historical_teacher_client_v1、v8_multiview_reader_scope_v1。原reader新增可注入精确scope编译器，默认仍原2259核验，执行时须明确用141版本。

下一冻结141独立数据卡/运行规格与正式执行器，绑定原环境、归档、源码、传输及精确源；核算共享容器附带人口及资源上限后预检一次执行。真实teacher与训练目前0，不把软件通道通过当作标签合格。
