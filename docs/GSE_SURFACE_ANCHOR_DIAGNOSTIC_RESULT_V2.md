# 路口观测检查：兼容修复完成，标签条件仍未满足

2026-09-07，阶段3开发诊断。不是模型训练或方法效果评价。

## 已完成

复用既有P1a加载器，新增严格格式适配层；使用原构造对象的 `as_dict()` 输出制作回归输入，不再自造简化字段。21项相关测试通过。用户明确授权后冻结独立V2卡、源码、环境及命令，预检通过并单次执行；原V1/V1R不修改。

固定C01十父地图、十条物理边、三种几何实现，共30个五帧观察/150帧全部完成，耗时214.204秒，峰值内存667525120字节。43项证据哈希复核通过。

运行：`results/gate3_semantics/gate3_20260907_gse_surface_anchor_diagnostic_v2_seed20260906`。
seal SHA-256：`f8d09457820b47a031e1c388b726b508d36741b8c19ea07f4edfc7cd64a35aad`。

## 结果说明什么

30观察中只有S08的三种实现包含degree≥3的参考路口，均为node_0088：这是一个独立路口，不是三个。全部未通过完整轴向观测支持检查，新增语义标签0、训练0。其他观察不是有效路口正例；不能据此断言完整3360人口缺乏路口。

该路口三条分支的证据相同：第一支46格有自由射线证据；第二支18格自由、10格未知；第三支66格自由但4格存在边界数值歧义。未知不能改成负例或正例。诊断结果不是可通行性证明。

另有确定的实现解释缺陷：两条相反轴线分支共享同一个锚点到轴端的辅助连接段，偏移0.00013084925235543952米。检查程序取参考折线第一段作为分支方向，因此报“初始方向重合”，实际下一段轴线方向不同。辅助连接段只用于观测探针，不应充当通道方向。第一支偏移甚至只有1.59e-14米，进一步说明不能拿该段做方向估计。

这个原因码不支持“真实分支重合”的结论。不过即使修正原因码，第二支仍有10格未知、第三支仍有4格歧义，不能据此自动宣布路口支持通过。

## 下一项及边界

在同范围内自行处理上述实现问题：独立保留源轴线方向与辅助连接段，先用相反分支共享偏移的合成反例验证；不移动锚点、不改网格、不填未知、不追加训练。随后针对已保存证据定位支持缺口，并核实有效标签人口，避免把软件修复当科学收益。机器人物理合同缺口仍按原备案暂停。旧图片、数据、模型与失败结果全部保留。
