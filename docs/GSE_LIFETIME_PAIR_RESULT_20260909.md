# 原内存故障位置完成：不等于标签资格通过

## 做了什么

复用冻结V8算法和原两位置数据，只将每个请求的计算放入独立作用域；完成后释放输入与结果。没有分批、显式GC、提高资源限制、改变标签或重新训练。

数据：S10_3d_complex_C04的c1_mixed实现，edge_0164:d1穿越，source192653/192654，弧长4/5米，6个去重源帧。两个输入容器共22行，仅2行选择为观察，其余20行不计样本。

规格：configs/v3/gate3/gse_v8_lifetime_pair_v1.json。
结果：results/gate3_semantics/gate3_20260909_gse_v8_lifetime_pair_v1_seed20260906。

## 实际结果

- 预检无错误/警告，创建一次；会话22652退出码0。
- 两位置全部完成，34.174296478秒；原父进程1GiB、worker3GiB地址空间上限不变。
- 父/worker峰值RSS分别510861312/1721004032字节，RSS不同于地址空间限制。
- 前例完整raw_interfaces与produced_targets逐元素等于旧封存输出。
- 原先失败的第二位置现在完成。两位置各保留1个锚点、4个开口；锚点传感器坐标分别约(-3.947,0.190,0.102)、(-4.933,0.236,-0.049)米。这里只报告局部坐标，不冒充已完成世界坐标一致性检查。
- 19个封存文件SHA及精确文件集合检查通过。seal SHA-256：80b80a311372ea2a8477953f4431ef7c1bb7e3319ce165ccc2889f6732cb0674。

## 结论边界

该对观察支持生命周期修正有效，不能证明所有复杂场景均满足资源合同。旧全量失败与分批失败保留；本轮不是分批成功，不单凭一次完成证明所有内存问题只有这一个原因。

输出仍明确full_training_gate_eligible=false、human_reviewed=false。完整可评分区域、参考标签质量、尽头对应及排除质量仍缺失。没有模型推理、优化器更新或科学阶段通过。

下一步先核对原141清单中旧121已完成和新完成1项，生成未完成19项的精确来源绑定。复用已有证据，不将失败run改为成功，不重算整批。余项独立规格冻结和预检前不执行；全人口完成后仍需单独评价监督覆盖和质量。
