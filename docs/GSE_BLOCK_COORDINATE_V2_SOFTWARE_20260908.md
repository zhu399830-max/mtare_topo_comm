# 结构坐标V2：已修软件接口，未获得新训练成绩

旧输出在原始坐标半径≥1后将节点强制放到10米球面。V2用 `10u/sqrt(1+||u||²)` 连续映射到球内，原点附近仍是10米/单位尺度，没有新增可调参数；用逐维hypot避免直接平方溢出。

只修改结构位置参数化；同样32个结构查询、64个窗口开口、参数布局及旧初始状态兼容。开口位置/方向、存在分数逐元素不变。原模型公式保留为默认方法，旧封存结果不改写。

24联合测试通过1.01秒。实际V2头从raw=(2,0,0)朝中心目标获得向内梯度，对9.9米目标获得向外梯度。检查原点、单位半径两侧及10倍raw的解析Jacobian与有限差分；旋转一致和10米球域成立。测试还覆盖空输入、未知字段、极大有限值、非法输入、旧缺陷反例与评分合同。

限制：这是坐标映射旋转等变，不代表整个模型旋转等变。梯度仍随raw增大衰减，浮点极端值可能近似球面；精确边界只能逼近。不能把软件通过当成拟合或建图成功。没有训练、没有读取研究样本。

下一合并完成同45原参考边界检查与完整模型损失接线/资源检查，再冻结一次仅坐标修正的配对对照；初始化、样本、预算和评分不变，不复活旧run。若修正后仍无收益，保留失败，不以更多种子掩盖问题。
