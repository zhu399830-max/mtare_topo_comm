# 目标适配、独立评分和三表示精确人口已完成

## 已实际确认

只读加载同45观察，三种分组均使用完全相同的2,435,326个范围内回波及同源冻结特征。R0传感器分组40,500块，R1体素32,432块，R2超点3,190块。人口检查用时28.18秒，未训练、未重新生成扫描、未导出新数据集。

复用既有`expected_geometry`参考定义，27个锚点目标、84个开口目标，84个方向已知。18个观察没有锚点，仍完整保留。每例最多1锚点/4开口，因此本批只能做限定原型的拟合与初步表示对照，不能证明多路口实例分离、未见地图泛化或图质量改善。

这些是观察级目标出现次数，不是27个独立物理地点或84条独立真实通道。三种截面实现与重复视点不作为独立地图计数。

逐例人口和参考哈希组成的清单SHA：`72507ffd4985d2e075f773d8bc3c110458a47136089c2d13e6074f570f39e91f`。已记入不可执行的比较政策；正式数据卡与run spec尚待创建。

## 软件接线

- `data/gse_block_targets.py`：将既有参考转成独立损失目标，未知方向保留掩码，不补宽高/可达/归属。不把不完整参考变成完整空背景。
- `evaluation/gse_block_structure_scoring.py`：原始输出按存在分数≥0.5选择，无NMS；独立位置匹配评分，不复用训练对应。锚点报告1/2/4米，开口1米及方向误差。重复预测计误检、不预测计漏检，不完整参考标记不可评分。
- `tools/v3/inspect_block_learning_population.py`：只读复算完整三表示数量与既有参考目标，不修改旧运行。

24项联合测试通过0.93秒，包括阈值0.5边界、重复预测、空预测、未知参考/方向、容量失败及无穷logit不能经sigmoid藏成有限概率。没有新的模型准确率。

## 下一步已经具体化

编写一次不可覆盖executor：相同初始权重、相同样本顺序、R0/R1/R2各500更新、microbatch1累积4，初末完整评价；保存全部原始预测、独立逐例评分、权重、日志和seal。精确卡、代码、输入、环境冻结后预检执行，旧失败候选头不重跑。

资源与软件检查不再重复。只有明确输入漂移或实现错误时停止受影响运行，不能静默改样本、目标、阈值或扩展训练预算。拟合不足不能宣称整个几何方法原理不可行；拟合通过也不等于论文核心主张成立。
