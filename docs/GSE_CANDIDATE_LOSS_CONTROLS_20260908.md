# 损失反例：训练前淘汰不完整修正

新增独立诊断模块 `evaluation/gse_candidate_loss_controls.py`，没有接入训练器，没有修改旧损失或结果。

5项软件测试通过（0.70秒）：

- 同一个错误高分背景加入999个容易背景后，旧组平均BCE梯度降低1000倍；最难背景项不受这种稀释。
- 正支持分数为整组软目标均值时，旧损失的共同分数梯度为零；要求至少一个中心高分的peak目标仍有上升梯度。
- 未知候选梯度为零；没有任何覆盖候选的目标不能静默跳过。
- 关键反例：peak目标无法区分一个高分正候选与多个高分正候选，重复检测未受惩罚。

结论：有证据说明旧目标允许来源捷径和平坦响应；但简单改成peak+hard-negative也不足以构成完整检测损失。本模块明确返回training_qualified=False，不作为下一轮训练成功保证。不将5个测试通过解释为完整新目标已通过。

下一完成与既定输出重复抑制/一对一评分相容的目标合同：既要有至少一个正确候选，也要对不被抑制的重复及错误高分候选施加明确代价；近邻独立结构不能因规则方便被当重复。优先复用成熟集合检测/难负例处理思路，在软件反例上排除来源常数、高分全选、全拒绝和相邻双节点失败后，才决定有限纠错训练。原45人口、预测阈值、骨干与评分保持，不拿下一轮长训练代替损失设计检查。

0新增扫描/推理/训练。完整图与论文证据仍未实现。
