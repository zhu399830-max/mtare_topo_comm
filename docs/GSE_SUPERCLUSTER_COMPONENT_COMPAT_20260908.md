# 参考聚类器接入与仍待解决的问题

已执行 `env OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 build/gse_supercluster_env_v1/bin/python tools/v3/check_supercluster_partition_compat.py`，会话41178正常结束，状态EXPLICIT_COMPAT_SYNTHETIC_PASS。

## 真正完成了什么

使用官方cut-pursuit求解器，并明确增加本地兼容层：边数组展平、边权置换取逆、单边向量保维、空图返回、连通分量独立求解。上游文件不修改，适配源码可检查，不冒充完全原版复现。

无边三节点输出[0,1,2]；两边强弱关系输出[0,0,1]；两个几何位置完全相同却断开的小图输出[0,0,1,1]。边重排、节点重编号、重复执行均通过。另独立还原CSR的0/1/2/4边列表，确认边与权重索引对应。未增加候选边。分量隔离改变原生求解的执行路径；不能声称所有输出与原版一致或获得全局最优。

本接口仅限thing实例，不支持stuff全局合并；对含stuff输入明确拒绝。输入组件隔离也不等于所有输出组的诱导子图已保证连通。神经网络前向与训练均未执行，研究观察数为0。

## 为什么现在还不能开始地下实例训练

再次核对既有来源人口记录和源码：当前45合成观察有27238次多来源回波。primitive_membership_code是通道构造来源，不是路口归属；同一管道的侧壁和尽头可以拥有相同来源。官方instance_graph监督则需要超点与对象分区的重叠计数。直接换ID不能解决这层差异。

现有primitive_relation_observable_batches保留端点可观察性及unknown_never_negative合同，可以复用为关系目标边界，但它仍不是通用表面点到路口的实例分割标签。此前18个有端盖支持观察不能代表9个交汇已拥有统一支持答案。

下一是明确的任务映射检查，而非另起长训练：用既有字段构造纯软件反例，区分同通道表面、不同通道在同路口连接、多来源未知、相似但不连接。官方分组机制只能解决对象归属，结构关系需要独立可观察监督；若目标不对应就不声称复现地下结构方法。保留已确认的数据与旧关系实现，不追加第三套猜测标签。
