# 20条无效射线：已定位到弯管交点分组，尚未修复

只读工具：`tools/v3/inspect_synthetic_invalid_returns.py`。它先核封存NPZ及JSON的SHA，重建原网格参数，只检查原20条射线，不重新渲染五帧或产生标签。实际退出0；20条均重现`recomputed_exit=null`。

## 具体证据

所有20条射线均从`approach`通道内部出发，并获得该通道网格的多个三角形交点。因此不是Open3D完全找不到交点，也不是远处隐藏支路贡献了返回。

最简单一例为`hidden_branch__ellipse__view3`，帧0、行14、列1。交点距离/外法向与射线点积为：

| 距离（米） | 法向点积 |
|---:|---:|
| 6.4747943878 | -0.18907248 |
| 6.4754333496 | +0.18892343 |
| 6.4812736511 | +0.22511159 |

三点跨度约0.00648米，均被现有0.01米分组规则并入一组。`CSGMeshProvenanceRaycaster.ray_exit_hits`在同一operand同时存在正负法向时保持原inside状态，故这一组没有产生离开事件。后面没有其它可用离开事件，最终返回None，导出为50米且valid=0。其余19条同样出现靠近的正负面交点和未产生出口的结果。

## 可以得出的结论

这是扫描实现中的交点处理/几何语义问题，不是学习模型失败。独立检查没有发现“交点数量为0”；不能将这20条当作真实无障碍远距离射线。现有保护拒绝隐藏删除对照是合理的，旧4项FAIL不追认。

## 尚不能得出的结论及下一步

不能简单改为最近交点、缩小0.01米阈值，或对正负法向计数后直接宣布修复：弯折扫掠网格可能存在内部折叠/重叠，最近面未必是联合自由空间边界。当前证据只定位到分组逻辑不能表达该交点序列，尚未证明哪个面是正确外边界。

下一步限定同20条原射线的交点前后内外关系与原网格定向检查；先形成可复现反例和独立几何预期，再决定一次有依据的修正。原144矩阵不重跑，真实数据不扩、训练0。99未评分条件的缺口仍独立存在，不能让修复20条替代整体监督资格。
