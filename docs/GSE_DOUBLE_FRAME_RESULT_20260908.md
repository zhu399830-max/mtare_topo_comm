# 首帧完整扫描：数值诊断通过

## 做了什么

对既有 `double_junction__circle__view0` 的第0帧运行原16×720激光方向，使用精确事件诊断及既有慢参考回退，与独立凸棱柱并集解析距离逐条比较。未改网格、阈值或旧扫描。作为合成软件诊断使用现有infrastructure预检类型，另存精确范围卡；不是数据集导出或标签生成，未为预检伪造train/validation/test人口。

预检通过，create_run仅创建一次，执行会话89409正常exit0。绑定1666个源码/输入文件，另绑定范围卡，并保存环境、命令和源码快照。

## 实测结果

| 项目 | 结果 |
|---|---:|
| 射线 | 11520 / 11520 |
| 精确候选返回 | 11520 |
| 慢参考触发、未知、超差 | 均为0 |
| 最大解析距离差 | 1.4210854715202004e-14米 |
| 固定比较线 | 1e-5米 |
| 执行器实测耗时 | 9.781563秒 |
| 峰值RSS | 442656 KiB |
| 结果大小 | 19364996字节 |

原始记录索引0—11519完整，所有score.passed为真；封存的11个文件重新计算哈希一致。封条SHA-256为 `be111f5a30aa6ef9012bca21c381437663f3223b6224e8133c98d88699b7a05c`。

[原始摘要](../results/gate3_semantics/gate3_20260908_gse_double_frame_diagnostic_v1_seed20260906/metrics/summary.json)

## 能说明什么，不能说明什么

该圆形截面、该位置、完整扫描方向的距离正确性及成本获得了实测证据。不代表其他截面/位置通过，不验证所有原生求交完备性，也不证明标签、结构模型或建图成功。0新标签、0训练；Phase3仍未通过。

下一步沿用原12双路口声明（3截面×4视点）及各5个历史位置，在独立新规格下检查60完整帧，共691200条方向。先冻结范围和资源保护，保留首失败即停、不覆盖或重试、不把未知填最大距离。首帧耗时仅作成本参考，其余人口可能触发慢参考，不按9.78秒线性保证总时长。暂不扩大到其他场景类型或训练数据导出。
