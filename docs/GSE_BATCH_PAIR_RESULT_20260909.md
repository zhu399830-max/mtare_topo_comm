# 分批修正：第一位置一致，第二位置仍内存失败

独立两位置诊断run：gate3_20260909_gse_v8_batch_pair_v1_seed20260906。11软件测试1.08秒通过，独立卡/spec预检零错警告并只创建执行一次，会话37656退出1。21.6906秒，父RSS峰值510570496字节、worker1765445632字节。

固定source192653与192654属于同一C04拟合地图和同一穿越，共6去重源帧；9源文件，两个输入容器22行，其中20附带行不进入计算。仅侧向证据调用原内核时使用1152射线固定分批，原V8归档、源精度、mesh与field不变。

1. source192653完成。全部raw_interfaces和produced_targets与原失败run的最后完整观察逐字典相等，比较证据已封存。
2. source192654仍在Open3D `list_intersections`发生Embree Out of Memory。此次记录不能确定失败在第几个批次，没有补造该值。

## 由此能排除什么

仅缩小一次输入的射线数量不足以解决这个真实问题。错误在新worker的第二个观察仍出现，所以不需要先经过原121个观察才会发生；但仍可能有一次观察后的原生缓存、对象生命周期或场景加速结构峰值。不能推断是单个场景必然超限，因为同地图前一位置能够完成。

分批修正候选已用完，本轮停止，不再改批量或升上限重试。原141失败run和本pair失败run都保持封存，不能覆盖或把第一位置比较通过称为完整修复。

## 下一步限定为资源定位

只读原场景创建/销毁路径与已有返回大小，检查同任务为何每个观察重复构建全世界场景，以及是否有明确保留引用；用无真实数据的重复场景生命周期检查测量调用前后VmSize/RSS。若需要新的真实诊断，先明确区分假设的观测指标与独立范围，不重复整批，不重新训练。

主研究目标仍未完成；已保存121个观察的机制证据有价值，但剩余20个未完成，不能宣布完整141资格。
