# 真实小批结果：读入与负例计算已到真实数据，资格检查仍未完成

## 做了什么

冻结原10观察的数据卡和执行器，经 `preflight.py`、`create_run.py` 后实际运行。只读取原C01/C03三父地图范围，不生成新扫描、不训练。小批使用原mesh .05m/64和已显式接入部分调用链的field .025m。

## 两次执行的具体结果

| 运行 | 实际结果 | 原因 |
|---|---|---|
| `gse_v8_original_ten_probe_v1` | 15.002秒停止；1个真实候选保存，0个完整比较；峰值约0.90GB | 比较器把锚点evidence说明文字也当作几何身份，误判旧锚点改变 |
| `gse_v8_original_ten_probe_v1r` | 4个观察比较完成后主动中止；第五个开始时停止，进程退出130 | 发现负归属V7及正归属V5仍隐式使用field .01m，未满足全链源参数合同 |

第一处只涉及比较器。首例两个锚点位置逐元素不变，旧True保留，增加一个False；说明文字随新侧面证据更新。修正版只豁免说明文字，记录其差异，其他字段和所有旧已知归属仍严格检查。8项修正版测试包含1e-14位置变化、重复锚点、评分区变化、成员矩阵错维及整数冒充bool拒绝。

第二处是执行方检查调用链不完整，不能归咎于模型或声称方法被证伪。原中止进程没有自动写终态；确认工具句柄退出130后，用只针对该未封存目录的finalizer记录FAILED、四例前缀、终止原因和seal，不重启、不改已有预测。总时间和峰值未取得，保持null，不编造完整运行成本。

两运行原配置、源码快照和候选均保留，15/21文件seal核验通过：

- v1：`f2fb0622ddd73c10832f095216435922b701191c084a1838cf04bbff4ce0a74a`
- v1r：`81e5dd95eb086f4bf6b5c9185dc8081bf8e0cf79013b7823027f2ff370cb0676`

## 保存下来的真实前缀说明什么

| 观察 | 正归属 | 负归属 | 未知 |
|---|---:|---:|---:|
| S04/C01 mixed 36237 | 1 | 1 | 2 |
| S04/C01 ellipse 36237 | 1 | 1 | 2 |
| S04/C01 rounded_rectangle 36237 | 1 | 1 | 2 |
| S07/C03 rounded_rectangle 97000 | 2 | 2 | 0 |

这是**尚未资格化的自动参考输出**，不是准确率、独立负例人口或模型成绩。前三行是同一父地图的几何变体，不能作为三个独立地点。没有完整10例结果，不补填S10结果。

随后只读原四观察，独立比较五帧传感器位置处 .01/.025 两距离场：所有通道的inside符号变化均为0；唯一通道内的帧数分别为4/4/4/0，在两配置下一致。最大有限距离差依次为0.0006683590907288206、0.00008471828024170236、0.00007217361714140225、0.0003226626807777411米。此结果定位参数影响，不将整个中止运行改称有效或推断其余六观察。

## 已完成的代码修正与下一步

V8现在把同一field参数传到anchor的interior/lateral和terminal的positive/exclusion四处分支；旧V5/V7直接调用仍保持原默认，历史实验不改写。新增全部四个构造调用的显式参数防漏检查，联合入口转发测试同时检查terminal两路径。45项回归0.98秒通过，四实际合成场景复验通过。

不立即启动第三次真实教师运行，也不扩2676或开训练。接下来用独立源参数与保存证据检查确定这批结果能支持哪些局部判断，同时推进计划中允许前移的训练/推理共同观测接口软件修正。只有数据字段有独立资格依据才开相应训练；不能用更多小运行替代这个结论。

当前没有运行中的实验，目标仍active。原资产、失败运行和论文图片全部保留。
