# 候选与紧凑观测特征接线

新增 `representation/gse_candidate_context.py`，从候选最近的有效回波读取其900-token缓存中的128维特征，不展开57600×128的全点特征。共同最近点按已绑定传感器布局索引决定，候选顺序不进入选点规则；显式返回最近回波距离和来源索引，不将最近邻上下文当成表面可见性证明。

要求候选与缓存的观察、五帧顺序、坐标系、输入SHA和编码器SHA一致。仅支持独立CPU观察，容量4592；无有效回波时返回未知和-1来源，不填假点。不接受教师身份筛选。

12项联合软件测试通过（0.75秒，6项上下文测试＋6项读出测试），覆盖源不一致、布局漂移、等距返回确定性、无回波未知及ABC隔离。尚未读取真实45缓存或核验其与新面片重建的数值对齐；仅相同stamp不能证明文件真实性，后续加载器仍必须核验封存文件哈希。

下一实现加载器：仅使用已封存候选记录和历史紧凑缓存，核对具体源字节、输入帧，并给轴对候选的未知法向/尺度明确掩码；再接独立目标与评分。未完成这一步不得启动正式训练。最近邻lookup本身不是新创新，主比较仍为候选共享条件下几何属性及关系是否减少最终结构误检/漏检。

本轮0训练、0真实缓存读取和0新增扫描。未改变既有模型和评价结果。
