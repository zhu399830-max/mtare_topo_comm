# 负监督已有实现，不应重新发明

## 核对结论

此前“2676记录没有负归属/完整区域”成立，但不能外推成“仓库没有可用负监督”。本次读完四个exclusion模块、两版query coverage及partial losses，区分三个不同任务：

| 已有机制 | 能支持的判断 | 不支持的判断 |
|---|---|---|
| bound_reference_exclusion | 查询位置有射线观测，且不能匹配完整构造清单中任何degree1/degree≥3锚点 | 全部空间无结构；支路不属于路口 |
| bound_anchor_query_coverage V2 | 已确认结构附近、没有未知参考竞争时，约束未匹配重复预测 | 不确定区域完整标注；新地点身份 |
| bound_opening_reference_exclusion | 不能匹配全部轴线与10米球面的交点 | 真实局部入口不存在或不可通行 |

原清单保留未观测及ROI外参考，避免把隐藏/边界邻近结构删掉后制造负例。正样本匹配优先，条件负例不能压制尚未移动到目标位置的匹配查询。结构中心本来可以不落在回波上，因此V2另用已确认结构证据处理重复点，而不是要求中心必须被激光击中。

## 已运行结果与本轮检查

旧run `gate3_20260907_gse_reference_exclusion_v1_seed20260906`：30观察、10个C01父地图、10独立穿越、150变体源帧。每例固定64格点，1920查询中293条件负、1627未观测、0邻近参考；这是固定格点覆盖，不是模型误报率，也不代表2676补充观察的覆盖。

本轮仅复算旧summary并读代码；17项既有exclusion/binding/partial-loss V2测试通过（1.07秒）。没有重算真实射线、生成教师或训练。精确30条源记录、网格路径及原卡绑定hash保存在`gse_reference_negative_reuse_inventory_20260909.json`；这是复用清单，不是新运行授权，也没有声称本轮重新检查所有网格载荷哈希。

## 执行收敛

不再搜索另一个“负例算法”，不把两种负监督混成一个问题。结构位置已有可复用的条件背景与重复预测机制；局部支路集合的未知/完整性仍是独立缺口，不能用窗口排除补它。

下一直接做2676既有参考的**支路证据元数据清点**：只使用已经保存的teacher provenance与source binding，统计路口/尽头分开的人口、每路口已见支路身份及见证字段、缺失方向绑定字段，按原父地图分区汇总。不运行新teacher，不导出新标签，不把构造全部incident自动标为已观测。清点要回答现有SavedBranchEvidence读取器能否消费这些旧记录、哪些字段需从原始封存引用绑定；不再重复2676总数或背景缺失统计。

在确定支路可用记录前不扩展模型。随后才能把位置条件负监督接入同一冻结任务；既有4米参考排除合同不改为入口阈值或合并半径。完整检测、建图及探索成功标准不变，Phase3仍未通过。
