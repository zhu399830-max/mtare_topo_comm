# 分批相交：合成等价性成立，真实内存修复尚未验证

在原CPU环境和原3GiB地址空间上限内，用16个固定盒子、57600射线比较一次计算与每批1152射线。包含无交点、反向、内部起点；无真实地图和训练。

两种调度均得到1842672个相交。按全局射线与相交键规范排序后，geometry_ids、primitive_ids、primitive_uvs、ray_ids、t_hit五字段SHA全部相同；逐射线交点计数也相同。排序用于比较，不是删除重复交点。

| 量 | 一次计算 | 每批1152 |
|---|---:|---:|
| 单次返回数组字节 | 44454532 | 889348 |
| 进程峰值RSS，KiB | 424476 | 317752 |
| 峰值虚拟空间，KiB | 1809712 | 1702012 |
| 相交处理秒数 | 0.1802 | 0.1777 |

以上只是该合成输入，不是复杂世界成本保证。完整场景加速结构本身的内存不会因射线分批自动消失；原失败还可能涉及它的延迟构建或长期内存保留，不能据此宣布根因已唯一定位。

新增未安装到正式教师的调度适配 `observed_entries_batched_v1`，逐批调用原证据函数，只恢复全局ray_index，原距离/来源/法向/未知判据不变。原生2305射线检查通过：三批（末批1条）得到与一次计算逐字典相等的2302条证据，批边界上的无效、遮挡、多来源保持拒绝。检查exit0约0.82秒。

## 下一步

此证据足以登记一个有限的执行调度修正候选，但还不够宣布真实修复。先把适配绑定到旧归档唯一相交函数，验证其他归档模块不变，并冻结失败位置及已完成邻接位置的精确比较方案；真实执行仍需独立spec/preflight，不覆盖FAILED，不改半径/阈值/射线，不自动重跑整141。

若小范围仍内存失败，停止该修正，不继续缩小射线/几何或偷偷提高上限。原121份证据保留，是否复用须经同源/同输出合同检查；不得把失败剩余部分丢弃后宣布完整通过。

工具：tools/v3/open3d_intersection_batch_probe_v1.py；完整数值与hash：figures/gse_graph/intersection_batch_probe_20260909.json。实际研究输入读取0、教师标签生成0、模型更新0。
