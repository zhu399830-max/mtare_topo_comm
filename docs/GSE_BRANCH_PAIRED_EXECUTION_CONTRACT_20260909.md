# 下一次配对诊断：数据和结论边界

状态：实施合同草案，不是已冻结run spec或训练授权。缺项必须在预检前具体化，不在运行中修改。

## 先检验什么

在相同五帧观测、冻结编码器、位置/方向输出和评价下，比较扫描布局、体素分组、经典几何超点是否改变路口定位和已见支路恢复。单纯分组对照不证明几何关系贡献；关系属性移除对照必须保持分组、邻居与消息计算一致，另行配对。旧扫掠方法保留，只有同源预测而非GT基元能作为公平学生输入；没有同源缓存不能搬历史分数排名。

## 已锁定且不能静默更改的数据

- 既有路口部分307观察：拟合250、校准27、开发30；父地图分区继承旧卡。全部307都保留。
- 951条已见方向作为部分参考；291观察的构造参考全集有见证；16次各缺一条，缺失不作负例。
- 307观察只用于当前路口组件诊断，不是完整节点检测人口。原2676观察、尽头及背景不删除；完整检测/图验收必须包含这些任务，不能以组件结果跳过。
- 输入只来自已有compact缓存：当前坐标点云、valid和冻结900×128上下文；源身份仅用于读取对齐，构造身份只在标签和评分侧。
- 基础源清单为saved_branch_binding_scope_v1，特征清单SHA为9e9d12c056c8ad138a66710da93e58554b430af59f717a55eabfe11b6cd051bc。正式卡还需绑定所消费的特征NPZ及分组实现/环境；本草案不冒充这些步骤已完成。

## 实施方式和公平性

复用BlockPointEncoder、BlockAnchorBranch以及现有条件位置排除/重复预测机制，禁止修改旧45合成加载器来假装新人口。独立development_compact_blocks适配已实现：SHA、来源、编码器、原布局验证后，保留10米内全部有效回波及原射线下标；共享原上下文聚合，不按最近真值选点。

扫描布局仍接受XYZ和既有几何预训练，不称“无几何”。体素/超点用同一输入点集合；分组超过容量明确停止，不删难例。不同表示共用初始参数、样本顺序、预算及最终checkpoint。不得为某一表示单独挑阈值或最好轮次。

沿用首轮seed0、每组2000更新、AdamW学习率1e-3/weight_decay1e-4、有效batch4、micro1累积4、初末评价的预定预算；资源检查前不创建正式run。GPU28GiB、host32GiB、新证据30GiB、三组训练评价12小时为上限，不是完成时间承诺。

## 评分不得混淆

- 位置条件负例只相对完整构造参考集合，不能把score_region.complete改True；报告可评分/未知查询数量。
- 支路归属不使用窗口球面排除作为负证据；支路参考全集覆盖不自动证明所有物理开口已知。
- 几何对应不能利用预测置信度或类别寻找有利配对。保留重复预测、未知范围中的确定预测及匹配延迟。
- 开发结果按父地图汇总，变体不当独立样本；C07不是严格未见测试。
- 当前组件结果不满足完整节点/边F1、建图、探索或论文核心贡献的扩大门槛。感知增益、关系增益和图增益分开验证。

## 开跑前仍需完成的具体项

1. 用新适配读取绑定的真实缓存做一次共享输入/资源检查；确认分组实现能消费同一ROI点集，不重新编码。
2. 固定路口参考条件下的支路匹配容差、槽容量、部分评分合同和正/条件负损失接线；这些不能从开发结果反向挑选。16次缺见证观察保留未知。没有评分规则就不生成正式成绩。
3. 冻结精确数据卡、分组资产、完整初始状态及命令后preflight/create单次运行。不能复用45合成run的授权或2000更新之外临时加训。

本轮5项纯软件适配测试通过0.71秒，没有实际缓存解码、训练或新标签；没有把草案列为已运行实验。
