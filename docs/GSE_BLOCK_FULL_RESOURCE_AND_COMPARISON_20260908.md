# 完整结构链资源检查与首轮表示对照

## 完整链已通过资源检查

RTX5090D上使用57,600个合成点、4,096块、900个随机冻结特征槽，执行块内点编码、冻结上下文汇总后的结构读出、一个锚点和一个开口的损失与反向。

总参数464,011，前反向0.350秒，GPU峰值258,227,712字节（约246MiB），进程峰值1,880,092,672字节。全部参数梯度有限，冻结上下文无梯度，前后参数状态哈希一致。优化器更新0，研究观察0。时间不含CPU分组输入构造，不能直接换算端到端训练时间。

复现入口`tools/v3/check_block_structure_resources.py`。环境只发出旧TF32配置API的弃用警告，执行成功；没有因此改动冻结训练环境。

## 对照只改变分组方式

- R0：按现有传感器特征布局分组（帧与方位列）。不做几何分割，但仍有坐标与历史几何预训练，不能称作完全无几何基线。
- R1：此前封存的固定体素面片，保留全部块内点。
- R2：此前封存的超点，保留全部块内点。

三组使用同一份原始点、冻结特征、点编码器、读出和损失；初始参数逐元素复制，共享相同样本顺序、seed0、每组500更新、有效batch4。只做初始和最终完整评价，不选择最好轮数。主比较R2对R1，R0用于判断几何分组相对传感器分组的增量价值。

该比较不证明关系消息传递的贡献；以后检验关系必须固定相同邻居，只移除/破坏关系特征。旧扫掠模型当前无等价结构输出，仅作几何参考，不拼入结构F1排名。

参数登记在`configs/v3/gate3/gse_block_representation_policy_v1.json`。这是**不可执行的软件/比较政策，不是已预检的运行规格**。正式训练仍须完成精确目标人口、目标适配与独立评分测试、不可覆盖executor及输入/代码/环境冻结，再预检创建单次运行。

继承已有合成接口目标：锚点和开口1米F1各至少0.90，完整列出多余与遗漏预测；分数阈值0.5，无NMS，附锚点2/4米敏感性。该45例不是独立真实地图，拟合通过仍不能宣布论文方法成功或进入探索。

当前已实现加载器的显式R0/R1/R2选项；默认仍只读取旧R1/R2，避免改变已有检查行为。R0实际计数将在精确清单核验时固定，没有伪称本轮已训练三组。

下一步一次完成目标适配和独立评分合同，再冻结executor，避免重复检查同一软件部件。旧失败候选模型保持封存，不继续扩训。
