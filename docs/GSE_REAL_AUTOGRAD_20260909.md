# 真实缓存的5090前后向检查通过

固定原样本S01/C01 mixed sequence598、帧806–810，使用既有32657点和冻结上下文。仅R0/R1计算链检查，无teacher读取、无优化器、无checkpoint保存。

| 检查项 | R0扫描组 | R1体素组 |
|---|---:|---:|
| 可训练参数 | 488584 | 488584 |
| 峰值allocated字节 | 148029952 | 149485568 |
| 峰值reserved字节 | 167772160 | 167772160 |
| 所有参数梯度有限 | 是 | 是 |
| 权重未变 | 是 | 是 |

host峰值1540521984字节。共同初始状态SHA为6f0b4db721dbddef25c8c5ece54e68ddc0fef55982f6b597bb50d52011d03b29。使用最大64支路槽、32节点查询；测试标量只检查可微计算图，不是研究损失、拟合或真实预测质量。R0首次CUDA调用0.238秒、R1后续0.011秒，含预热差异，禁止作为表示速度排名或2000更新工期承诺。R2前后向尚未在此检查执行，不据CPU分组推导GPU通过。

沙箱内/dev/nvidia*不可见且torch报告CUDA false；宿主只读查询确认RTX5090D可见。使用获允许的宿主访问执行工具check_development_autograd_probe.py，会话82306 exit0。没有安装或修改驱动。

## 匹配合同前置固定

branch_paired_matching_policy_v1.json登记prospective组件规则：沿用位置主合同4米、敏感性1/2米；置信度0.5不做NMS；方向主容差10度，完整报告5/15度敏感性和连续角误差。10度是当前局部方向任务的预先设计值，不是测试通过证明合理或由开发分数调出的最优值，不是物理通行阈值。所有方法共享规则，不能挑最好容差。固定容量32节点、每节点64方向槽，不截断真值；旧64全局开口接口不被冒称与之相同。

旧完整结构/图验收不改，组件参考集合完整不等于全场景标注。此政策不是训练run许可。

下一将实际部分目标/条件负监督/独立评分与同源缓存接入正式executor，冻结精确输入与环境后preflight/create一次配对运行；不再做同类资源试探。R2跨Python环境的分组缓存交接需统一纳入该冻结输入，不能暗中用R1代替。完整目标仍未达成。
