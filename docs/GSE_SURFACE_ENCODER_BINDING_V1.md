# 公共编码器权重接线

2026-09-07。当前仍在 Phase 3：检验面片关系是否改善结构图。本次完成训练准备，不改变标签、人口、A/B/C 或科学验收线。

新增 `gse_surface_encoder_checkpoint_v1.load_surface_encoder`，接收调用方已授权读取的不可变文件字节，以及预先冻结的 SHA-256 和训练轮次。先验证文件哈希，再使用受限反序列化；检查旧 observable seed0 格式、监督合同及全部张量的名称、尺寸、类型和有限性。拒绝部分加载、自动类型转换和不安全反序列化回退。加载后 CPU/eval/无梯度，同时返回与现有训练器一致的模型状态哈希。

加载模型不会消耗调用方 CPU 随机数状态，避免改变三组新头初始化。文件哈希证明来源文件一致，状态哈希绑定提取特征和训练器；两者不能混用。调用方不能从任意权重计算一个哈希就称为“预先冻结”。接口不读取地图、不选择权重、不认证标签或特征缓存行来源。

验证：13项新增合成测试与31项既有编码器测试，共44项通过（2.20秒）。包括实际旧网络合成权重序列化、加载、五帧特征提取、重复输出一致和训练模式切换后编码器保持冻结。不是历史模型在真实数据上的成绩。

已有历史规格 `configs/v3/gate3/primitive_composition_anchor_model_readiness_v2.json` 记录了 seed0 来源：

`results/gate3_semantics/gate3_20260902_primitive_relation_observable_three_seed_training_v1_seed0/artifacts/models/seed0/selected.pt`

其记录的 SHA-256 为 `8d5d2e0ec9779c28b068d0c7db80aeed38ebdb22dff5b781c26234d9001287fb`。本次只核对规格中的记录，没有读取或验证该历史权重文件。真实提取仍需新操作范围绑定、轮次来源核对和预检，不能复用旧实验许可冒充新提取已经执行。

下一步把此入口接入精确输入清单的公共特征导出准备；标签生产仍是主线缺口。合格新标签、真实新模型训练和公共真实特征提取本次均为0。旧实验及论文图片不修改。
