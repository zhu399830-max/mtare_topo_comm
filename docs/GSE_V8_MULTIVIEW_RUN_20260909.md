# 141多视点真实机制验证已启动

独立run：gate3_20260909_gse_v8_multiview_mechanism_v1_seed20260906。

冻结卡与规格后preflight零错误零警告，create_run仅一次。真实执行会话43871已确认存活，首个观察S02/C05 mixed source16700进入计算。尚无完整结果，不能宣称补标有效。

执行复用原有封存、资源、逐窗口完整证据导出；新V8验证器强制原版本及mesh .05/64、field .025、精度保护，并拒绝重新渲染首次返回。17项合成/传输/读取回归1.18秒通过，包括参数漂移拒绝、零目标和未知保留。

数据：141选中观察、13任务、5拟合父地图、193去重变体帧；26输入容器336行，其中195附带行不成为本次样本。原teacher数组解码padding9744896字节，114源文件。范围为已发现机制问题的定向诊断，不是无偏泛化评估。

上限：CPU，GPU0；父1GiB/worker3GiB虚拟地址空间、总墙钟10800秒、完整结果30GiB。任何错误封存停止，不重试、不删窗口、不变更教师。运行中不修改绑定源码。

下一持续监控同一43871至终态，核验seal与141精确来源、逐窗口目标和未知，以及原13正例后续证据是否保留。成功导出也不等于完整标签资格；校准无路口覆盖仍未解决。训练与模型调用0。
