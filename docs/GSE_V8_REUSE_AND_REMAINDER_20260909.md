# 不重算已完成结果：141项证据复用索引

原失败run的137个文件与新生命周期run的19个文件全部核验SHA及精确文件集合。旧121项加新2项有1项重合，重合的完整raw/targets一致，因此可复用122个不同观察。每项核对完整来源记录、同一V8归档及同一组几何参数，没有按标签成败筛选。

索引：configs/v3/gate3/v8_completed_union_v1.json，SHA-256为622b98c37920ae57fc06816eb62400ec599bf1fd7f2320bd3030956dd6357c46。每项保留原run文件路径与哈希，旧run仍保持原来的失败状态，不覆盖、拼改或重封存。

剩余19项：

- S10_3d_complex_C04，c1_mixed实现：弧长6至10米，5项。
- 同父地图ellipse实现：弧长4至10米，7项。
- 同父地图rounded_rectangle实现：弧长4至10米，7项。

均为同一物理穿越的几何实现，不能算三个独立父地图。31个去重变体源帧；读取计划26个源文件，5个NPZ容器共50行，仅19行作为样本，其余31行是共享容器附带行。教师数组解码计划2236416字节，不是整个运行内存估计。

精确scope重复编译一致，原读取器使用显式scope_compiler构造成功，opened=0，未读取真实扫描或生成标签。首检查命令的通配导入遮蔽了函数名，误检查旧2259元数据；改为显式别名后重新核验19项。错误检查不用于支持结论。

下一步：独立数据卡/spec绑定剩余19项及生命周期执行器，预检后再执行一次。不能从122项复用成功推出标签已合格；完成后还需完整141多视点、未知与位置统计，校准覆盖及标签质量缺口仍存在。没有训练、测试世界访问或阶段推进。
