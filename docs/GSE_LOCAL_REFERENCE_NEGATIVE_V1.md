# 局部参考排除负例：待绑定的自动判据

## 来源绑定软件已实现

`gse_reference_exclusion_binding_v1.py`核对冻结的task/sequence/原帧及构造内容SHA；复用原学生投影，逐字节计算射线网格的source_geometry_sha256；解析冻结构造全部degree1/degree>=3参考，不按可见正例或10米ROI筛掉未知/邻界参考。原始运动与来源codebook仍由现有verify_alignment校验。调用者不再提供“完整=True”代替清单。

12相关测试通过0.79秒，包括有效绑定、错帧、错网格与删除参考拒绝。绑定测试使用合成输入，检查数值/身份协议，不证明射线物理上由给定网格生成；先前实际后端场景测试也不能代替真实人口质量。完整性仅指冻结构造的参考集合，非全部可能几何事件。训练资格仍False。下一项冻结真实只读检查范围，报告条件负例及未知比例，不直接全人口开训。

用户要求自主推进后，直接实现并检验一条可计算判据，不等待人工作为唯一入口。

给定有明确观测支持的位置、完整构造锚点清单（包括所有未获可见支持的锚点）及既定匹配距离：如果该位置不能匹配清单中的任何锚点，才输出“相对该完整参考集合的负例”。靠近任何参考、观测未知或清单未确认完整，均忽略，不当负例。此规则针对参考锚点检测，不用于开口可达性或无条件几何语义背景。

`gse_reference_exclusion_v1.py`实现此逻辑，6合成反例通过：出现实际非空负例、未知不负、隐藏参考不能删去、空正例不等于完整清单、三维距离及匹配边界、非法状态拒绝。4米仅来自现有主评分匹配合同，模块要求调用方显式提供，不在本次调参。

当前限制：调用方提供的完整性及观测状态仍未绑定原数据，函数明确返回supplied_not_verified及training_eligible=False。它也不能证明TNG覆盖所有真实几何事件。下一项是把完整构造清单和已有射线网格接入，检查判据在真实来源上的适用范围，再决定是否可以作为局部负监督；不直接将score_region.complete改True，不把条件负例当全场景F1。没有新增真实读取/标签导出/训练。
