# 当前训练入口与证据缺口

当前问题：在相同输入和图后端下，面片几何关系是否改善结构与连接恢复。尚未开始新A/B/C真实训练，不能宣称方法有效。

## 最新完成：全人口V6更新

`gate3_20260908_gse_supplement_joint_v3_seed20260906`已完成2676观察/210任务，2899文件seal核验通过。耗时5233.912738780025秒；871处None→True，正归属总数748→1619；1609锚点和5568开口保持不变，没有开口增删。旧V4及原raw不改，新增证据501304789字节。此结果仅统一目标版本，不认证完整标签或真实模型收益。

seal SHA-256：`c5c5f5fd8cfa48a10ee2c9726c1c0b1ac9785fd0b696db8a07403ac9bb71c8af`。

## 已经可复用

补充特征与封存目标的2,676个(task,sequence,frames)全部对应；索引按各自SHA封存验证，不靠目录排序。拟合60父/2,310观察，校准5父/189观察，开发5父/177观察。旧3,360特征不变。

| 分区 | 锚点出现次数 | 开口出现次数 | 已确认归属对 |
|---|---:|---:|---:|
| 拟合 | 1,389 | 4,783 | 627 |
| 校准 | 111 | 404 | 55 |
| 开发 | 109 | 381 | 66 |

表中不是独立结构数量，也不是准确率。均来自supplement_joint_v2运行内的V4目标索引，不包含单例V6补丁。

已有真实单例证明：GPU缓存按GPU原路径精确验证，CPU教师网格按CPU原路径生成，可接成训练样本。该例有56,752个有效点和532个面片。单例通过不证明全人口目标正确。

## 尚未完成，不能用软件测试替代

### 整批归属监督缺口（只读原封存结果核实）

真实连续段核对：使用原run封存source_reads绑定7份已有构造（3父地图、10观察），逐字节核SHA后调用完整适配器。S04/C01每变体77源、69个degree2点→9连续段；S07/C03 rounded_rectangle为118源/100个degree2→19段；S10/C01每变体161源/138个degree2→25段。各份仍分别有1/1/2个未解degree2连接，保留不snap。28个ID非incident候选中，3个S04/C01序列36237的opening0→anchor0实际处于通向node_0013的相同参考连续段；这是真实切分导致的候选误排除，不能给False。其余25对与目标锚点不同参考段，且所查询opening段无未解degree2连接，但这不证明当前扫描支持否定归属。全部28对仍unknown，下一项原射线的观测区分证据；不重导出2676或以构造连续性冒充物理/观测标签。

来源排除审查：只读已封存10个多锚点观察及其原raw引用链，逐项核SHA/源身份；28个未知关系的opening来源都不在相应节点的参考接口来源集合中。这只是构造候选，不是已观察负例，未重标。解析反例将相同直通道区间[-2,10]在五个不同位置切成两段，外部几何并集完全相同，而ID非incident规则由False变True；一个真实间隙反例仍保持断开。6项新反例与24项损失测试共30项1.17秒通过（非真实射线模拟）。因此不采用ID-only负标签，也不把不同source或另一个True当作否定证据。下一判据必须先保留度2切分下的几何连续性，再核对实际观测是否支持隔断；缺证据仍unknown。

逐个核对2676目标压缩SHA并解码membership：1619个True、0个False、724个None，总2343对。读取分析15.16秒，没有新标签、模型或优化更新。10个多锚点观察只来自3张父地图：S04/C01的36237（三变体）、S07/C03的97000（仅rounded_rectangle）、S10/C01的178898和178901（三变体）。它们不能当10个独立地图。

`gse_surface_losses_v1.py`仅对membership_valid项计算独立二元交叉熵，V1/V2部分负例补丁仅补presence，不补membership负例。因此当前归属项单独看，所有logit趋向正无穷即可令其损失趋零；这不是结构判别成功的证据。此结论不表示锚点/开口回归任务也不可学，更不表示几何关系方法已被证伪。

受影响的是归属拟合及组合学习贡献的解释，类别为监督/损失可辨别性缺口，而非GPU或数据损坏。下一项先验证观测支持的局部不归属判据（至少包含相邻双路口、分层及未知遮挡反例）；不得把已有None批量改False，不因另一对True就强制单归属，不用构造ID进入模型前向。已有固定小样本/ABC科学门不变；不直接扩大训练或重新导出2676。

实际损失回归：新增两项合成测试，完整24项在1.15秒通过。只有一个正归属、另一归属未知时，恒定logit=20让归属loss低于3e-9，未知梯度严格为0；独立提供一正一负时，同样恒定输出loss大于9.99，正负梯度方向相反。几何匹配没有被置信度改写，既有多正归属测试仍通过。由此定位为监督缺口，不需重写BCE或改为单选softmax。合成负标签只用于检验损失响应，不是实际教师负例，更没有给现存724个unknown赋值。

1. 全人口已产V6部分监督；参考目标质量、尽头归属质量与完整评分区域仍未认证，不能把版本更新完成等同于标签资格通过。
2. 条件负例与重复预测损失已实现，但不是完整区域标注证明。已有评分明确不给完整F1，不能直接用于宣称达到原开训/扩训门槛。
3. 全批2,676观察均不是连续路线记录；只能用于局部感知或独立穿越片段，不能拼接成连续回环。
4. 此批每观察至少有一项部分正目标。它不替代原未筛选背景人口；这也不代表每观察没有可用负查询，二者不能混淆。
5. 原缓存已齐，反复提取或增加训练种子不会解决以上证据缺口。

## 下一步边界

优先统一实际可用目标版本与资格证据，复用原始接口，保留原版本对照；不覆盖旧run、不删除unknown、不把完整标注标志直接翻真。准备小规模接口拟合必须明确有效正/负监督与可评分范围，不能用仅正例召回冒充完整检测F1。

此次索引核对只读210个目标任务索引和特征manifest，没有重新读扫描、提特征、生成目标或训练。目标继续，不进入C08–C10或闭环。
