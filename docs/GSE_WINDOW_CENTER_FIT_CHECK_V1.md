# 真实中心约束检查：椭圆形状可拟合，不能据此覆盖全部截面

2026-09-07，已封存30观察的只读原因诊断；273个打开来源文件重新核对哈希，不写新标签或正式run。脚本`tools/v3/check_window_center_fit_v1.py`可复现逐候选数值。

60个有支持候选中，56个在参考中心的横向和竖向两侧都有表面返回；4个仅横向单侧。该符号统计依赖参考平面/中心，不是模型输入，也不是位置资格充分条件。

进一步仅用支持点在参考截面上的投影，线性最小二乘拟合椭圆并估计中心。参考截面方向和同源支持点已经提供，因此是乐观的条件式诊断，不能作为完整非学习基线或部署成绩。

| 几何实现 | 可形成椭圆／输入候选 | 中心与参考偏差中位数 | 最大偏差 |
|---|---:|---:|---:|
| ellipse | 20／20 | 0.00105米 | 0.00605米 |
| mixed | 16／20 | 0.13466米 | 4.41782米 |
| rounded rectangle | 19／20 | 0.10805米 | 13.29782米 |

统计没有删除失败拟合：5个未形成有效椭圆单列，不计入偏差分位数。其余55个偏差中位0.03242米，90%分位1.07064米。三变体不是独立地图，也未据此做显著性主张。已用已知椭圆中心及共线退化合成输入独立检查拟合计算。

## 解释与决策

这些结果支持“真实观测并非都无法约束中心”，尤其正确形状族的椭圆实例在此条件下可恢复参考中心。另一方面，圆角矩形不能硬用椭圆替代，部分视野也会导致外推不稳定。最大13.30米例仍在横向、竖向两侧有回波，说明简单两侧符号检查并不足够；其代数残差不大也不能证明中心可靠。

不能由错配椭圆的失败断言原始教师中心错误，也不能只拿椭圆变体开训或重选样本。本次不会引入“椭圆拟合通过即标签有效”的新门，也不把拟合中心替换原参考。

下一步按原有超椭圆几何表示检查局部参数约束与不确定性，而非再增加新的网络或长训练；保留本诊断作为非学习拟合的形状错配分析。开口候选、位置资格和最终训练标签仍分开，qualified_labels及真实训练更新仍0。
