# 同一真实观察的三种分组已接通

固定使用原307路口清单首个拟合观察：S01_flat_tree_small_C01__c1_mixed，sequence598，帧806–810。不是按模型效果挑样。读取原特征缓存，SHA256为ee7a80456b1e60d71ba822058fd4e1e2e2ba1f7bb2d6ca39a8b679c24e0d8956；无新扫描、标签、特征提取或模型前向。

| 表示 | 10米内有效回波 | 分组数 | 聚合冻结上下文形状 |
|---|---:|---:|---|
| 扫描布局R0 | 32657 | 525 | 525×128 |
| 固定体素R1 | 32657 | 839 | 839×128 |
| 官方SPG组件＋既有连通适配R2 | 32657 | 39 | 39×128 |

三组点坐标SHA相同：e02459706583fc950b5467693c4d2693edd18247db7ee4fb450bc21205868b05。原射线索引SHA相同：4358f6815dd26996c61cc1e0918d7a0a691938ac003524494fb3196644cc3552。没有以浮点最近点重新匹配来源。

R0/R1读取/分组/上下文聚合合计0.878637秒，进程RSS569651200字节。R2含聚合1.151415秒，RSS177934336字节。环境不同，不能据此排名算法速度；这里只验证资源量级，不能外推复杂场景最坏成本。39块不是39个路口，也不是超点识别正确。

执行工具`tools/v3/check_development_grouping_probe.py`：默认R0/R1在冻结Torch环境；`--native-spg`使用独立系统Python3.12官方libply_c/libcp环境。R2先做分组调用，再补齐纯NumPy上下文聚合重查同样本，最终会话58841 exit0。未改变采样、分组参数或阈值，不是研究训练重试。

## 明确完成与下一步

真实compact读取、三分组相同输入及上下文接口已通过这个固定样本。停止追加同类单例资源检查。尚未保存307例分组缓存，也未执行网络前向/反向，不称训练资源预检完整。

下一完成版本化部分路口—方向集合的独立评分及条件位置负监督接线，再用精确数据卡统一冻结一次表示提取/训练规格。方向匹配阈值、集合完整性与未知区域必须先定义，不能看开发结果挑阈值。保留完整结构/尽头/背景与图收益主标准；组件部分评分不能替代它们。
