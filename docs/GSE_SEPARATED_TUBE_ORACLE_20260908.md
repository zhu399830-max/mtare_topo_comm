# 平行与分层通道：独立几何预期

新增evaluation/gse_separated_tube_oracle.py，只处理既有parallel和stacked直管声明。未读取扫描、生成训练标签、调用生产teacher或训练。

两管是相同有限凸截面沿X轴的横向平移，外包围盒在至少一个横向轴严格分离，五个历史传感器位置均严格在主管内。则从主管内部发出的射线必先退出主管边界，不能在首次返回前看见另一条管。已有独立直管解析模块检查实际64边形截面和有限轴向区间，不拿reference_inventory当可见答案。

24固定条件=2类型×3截面×4视点。软件预期全部为主管的两个10米窗口开口、零局部路口/尽头。这里窗口开口不等于物理门洞，更不等于拓扑节点；未检验机器人可通行性。

29测试在1.15秒通过：24声明条件、3接触/重叠拒绝、历史pose越界拒绝、伪造inventory不改变预期。会话27212 exit0。包围盒分离只是充分条件，不满足时拒绝判定，不把接近却实际分离的几何误称接通。

## 尚未证明

未证明封存mesh和LiDAR实现准确实现这些声明；未读取24例实际range/valid。不能把这24例旧未评分自动改为PASS，也不能扩大到双路口、弯曲或交叠条件。输出明确archived_scan_verified=false、teacher_qualified=false、training_eligible=false。

下一绑定原144矩阵中这24例的精确封存记录与SHA，先核对扫描有效返回和独立主管首次退出，再比较原目标。保持旧run与分数不变，以独立诊断输出报告，不重渲染、不增教师版本、不训练。多路口还需单独可见支路证据，不能从直管结论推广。
