# 尽头部分目标与联合接线

补充端到端验证：`tests/v3/unit/check_gse_terminal_producer_backend.py`在锁定Open3D sidecar实际生成一条双端封闭通道，原serializer→网格→五个不同位姿/朝向的57600射线→原float32首次返回→来源/运动校验→尽头生产器，全链不模拟诊断。得到两个部分尽头目标；将返回全部标为无观测后，两个参考均保持未知。耗时约1.02秒，0真实数据读取/0输出文件/0训练。这证明合成场景接线，不证明复杂地图标签质量，也未产生完整评分区域。

`gse_reference_terminal_targets_v1.py`复用零容差、原始首次返回端面匹配入口。上游仅考察原degree-one构造端点，degree-two切割点与10米窗口边界不产生尽头。有唯一来源端面返回见证时，生成构造参考的当前传感器坐标位置监督；无见证保持未知，不相信单独的支持布尔值。

联合生产器默认同时生成路口、开口及尽头部分目标。尽头追加在路口锚点之后，保留证据索引；新增归属列为None，不因尽头只有一个通道就猜其开口对应。总锚点超过32或位置重合报错，不截断或自动合并。没有完整背景证书，完整区域仍False。

本次38合成/组件测试通过（1.03秒）。其中生产器接线模拟上游结果；端面证据本身由独立组件测试覆盖。真实新读取、标签导出、训练均为0，不表示方法或真实教师已合格。它也不证明当前观察排除了所有隐藏分支。

下一主要缺口是实际参考质量、完整评分区域及尽头—开口对应。已有部分目标不能只按匹配正例评分后报告完整检测F1；正式导出仍需精确范围冻结。旧数据、运行和论文图未修改。
