# GPU实际接线与资源结果

`check_synthetic_fit_resources.py`在宿主5090D上退出0，固定 `four_way__circle__view2`，1观察/5历史帧。原SHA绑定、原seed0epoch2冻结编码器，A/B/C相同seed初始化；前向、损失、反向，未调用优化器。不是拟合实验或方法收益。

组装耗时0.660秒，1290面片。A/B/C前向反向分别0.164/0.033/0.034秒，含首次启动差异，不用于推断A比另外两组慢。最大已分配显存650148352字节，最大预留884998144字节，主机峰值1941164032字节。只覆盖该单样本、不含AdamW状态和全45缓存，不是完整训练峰值承诺。

锚点存在/位置分母32/1，开口存在/位置/方向64/4/4；未知尺寸、可达、归属均0。梯度与loss有限；编码器及三头模型状态在反向前后哈希不变。训练0更新，说明接线能运行，不说明能学会。

TF32旧API产生弃用警告但未导致失败；未升级环境或改变冻结数值设置。

执行准备：新增精确合成卡校验并接入preflight分派；训练核心新增可选逐更新进度回调供日志/资源检查，不改采样或损失。原训练核心13软件测试通过（24.00秒），测试中的微型优化不是正式科研训练。下一完成runner、冻结卡/spec并预检，再唯一执行；没有正式运行目录。
