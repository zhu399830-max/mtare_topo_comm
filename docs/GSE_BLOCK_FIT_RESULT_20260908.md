# 三种分组表示的结构拟合结果

## 做了什么

同45合成观察、225帧，27个结构位置目标、84个窗口开口。原编码器冻结，保留块内全部点形状与观测上下文。三组共用464011参数网络、初始状态、样本顺序和评分，仅改变点的分组。seed0，每组500更新，微批1累积4；固定最终结果，无最佳轮次选择。

## 实际结果

下表使用预先固定的1米匹配和0.5置信度，无预测去重。TP/FP/FN分别是找对、多报、漏掉。

| 分组 | 结构位置 TP/FP/FN | 位置F1 | 开口 TP/FP/FN | 开口F1 |
|---|---|---|---|---|
| 扫描位置 R0 | 6/39/21 | 0.1667 | 63/81/21 | 0.5526 |
| 固定体素 R1 | 0/50/27 | 0 | 72/23/12 | 0.8045 |
| 几何超点 R2 | 0/52/27 | 0 | 74/34/10 | 0.7708 |

三组均未达到位置与开口F1同时≥0.90的拟合标准。2米位置F1分别0.5833/0/0；4米分别0.7500/0.3117/0.1519。不能以放宽匹配距离宣布通过。

## 能说明什么

体素和超点开口预测优于本轮扫描分组，但三组结构定位均不合格，超点未优于体素开口F1。共同读出和训练机制仍需定位；尚不能把失败归因于具体模块，也不能宣称几何学习原理不可行。这是训练样本上的拟合，不是泛化或建图成绩。

执行正常结束，用时145.227秒，主机峰值2433748992字节；三组GPU分配峰值均低于295MB。29个封存文件SHA核验全部通过。没有资源失败或自动重试。

## 下一步与停止边界

不增加轮次、种子或调整阈值。只读已保存逐例预测，区分定位偏差、低置信度、重复和背景误报，制作真实同场景对比图并检查共同读出。未经证据定位不启动新的训练，不进入图/探索比较。旧图片、权重和结果全部保留。

原始结果：[summary.json](../results/gate3_semantics/gate3_20260908_gse_block_representation_fit_v1_seed0/metrics/summary.json)。训练日志、最终权重、全部初始/最终预测、输入清单和源码快照在同一run。Seal SHA-256：`9993ec4cbbc08521a099ceba8b81fdfafeef7b91477b2f5fd9a0d3bb383bdfdf`。
