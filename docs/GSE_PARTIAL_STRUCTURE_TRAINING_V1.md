# 原180观察的结构小头短训练合同

2026-09-05，Phase 3。问题：在固定几何输入上，显式相对关系能否改善中心和部分成员的拟合？不是论文完整方法验收，不替代未见地图、跨几何一致性、完整检测和图实验。

## 数据与预算

使用已完成的`gse_partial_structure_export_v1`：C01十个父地图、180观察、900既有帧、1452个可见基元。只读取导出的inputs/manifest/target_transport/summary四文件，并核封存来源。0新扫描、0骨干推理、0旧checkpoint加载。父地图为独立单位，重复标签不扩大独立样本数。

原始中心1206（304独立实例），成员2155正/13510负，事件872走廊/14交汇（6独立）/0终点。两输入分支方向对应后均为2152正/13489负，3正/21负因方向歧义未知。GT有效query总数2892，预测有效query11520；该差异必须报告，不能把GT/预测对照解释为仅几何噪声不同。

固定seed0、hidden64、每支26826参数、Adam(lr=0.001)、batch18、每支300更新，共900更新。三支相同初始权重、相同样本顺序、相同更新预算：

1. 原GT轴线＋显式相对关系（理想几何输入参考）。
2. 已冻结raw预测轴线＋显式相对关系。
3. 同一raw预测轴线，关闭显式相对关系路径及位移投票（直接消融，不叫完全无组合）。

只更新三个小头，GT/预测轴线均不反传；全部32预测轴槽不筛选，GT中心/身份仅loss和评分。损失沿用既有五项等权、中心尺度50m的集合训练候选，源冻结后不改；未知标签继续mask，不为未匹配query增加背景负例。训练仅保留固定第300步作为结果，不搜索最佳checkpoint或阈值。

## 独立评价与解释

每支初始化和第300步各评价全部180观察，共1080次小头评价窗口。训练匹配包含目标类别/成员，不能用其matches评分。评价器另按中心欧氏距离做一对一匹配；对应不唯一则事件/成员拒绝，显式报告覆盖和缺失。它衡量最佳候选集合容量，不是自主检出节点。

报告中心距离分布、未匹配目标和未分配query；两类已知事件的分类F1及包含terminal/reject列的混淆矩阵，不能声称三类macro-F1；成员固定sigmoid>=0.5读出，未校准、不搜索，给原始分母、条件F1、覆盖及最好/最坏界。恒答走廊约98.42%准确率、恒答负成员约86.24%准确率只是退化参考，不是成功。

逐父地图保存原始和最终结果；原180仅mixed且全部为拟合样本，不称泛化或配对一致性消融。直接对照第二/三支的同预算中心/成员变化；若没有独立增益，不扩大三seed/闭环来掩盖。此次不会以局部达到0.95/0.90宣布原完整事件/端口门通过。

## 执行与停止

CPU wall上限1800秒，host RAM/GPU各4GiB，结果<=0.5GB。纯合成18行/13目标/64query的GPU三更新检查1.464秒含启动、峰值256419840字节，零真实数据；这只是预算依据，不是训练结果。

预检后只创建一次不可覆盖run。源/身份/计数/初始权重/顺序/预算漂移，非有限loss/梯度/参数，或资源超限即FAIL并保留日志，不挑checkpoint、改阈值或隐式重跑。保存源卡/规格/environment/command、初始状态、共享schedule、逐步日志、三最终权重、六组全量预测/独立评价/逐父结果、完整预览与seal。全部历史论文图片保持不变。
