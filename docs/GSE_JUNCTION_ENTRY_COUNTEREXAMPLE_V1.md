# 合成T路口暴露进入端面代理的漏标

## 源内部分归属接通

V2补独立源内归属：同一射线既有唯一局部源内方向证据，又是同源窗口开口的排他穿越见证，且途中无其他参考节点接口竞争，才能补部分参考对应。没有见证保持未知，多个支持节点或竞争接口拒绝。它与原端面进入路径分别留存，不生成物理可达边。

实际single与layered场景均1锚点/3开口/3部分归属；overlapping仍0锚点/2部分开口/0归属，三个实际后端进程均退出0。17组件测试通过，包含无共同射线和未标中间节点拒绝。脚本已增加三归属断言；上述进程启动时尚未加入该断言，但其打印结果确证三True对应。训练资格仍False，缺完整评分区域、可靠负例及实际参考质量，不扩大训练。

## 实际分层与重叠反例

同脚本`--revised --scene layered`：增加z=6米的同形T路口，实际CSG五帧求交与全部生产组件运行。当前层1锚点/3开口，上层三接口均无见证，保持未知，不因XY相同产生另一层锚点；3归属未知。退出码0。

`--revised --scene overlapping`：第二T参考平移x=0.5米，两个参考的局部源交集重叠。帧1/2/3触发同时包含歧义，源内见证全部拒绝，0锚点/2部分开口，退出码0。通过的是“不要凭来源身份强行分辨重叠参考”的拒绝断言，不是恢复了两个可辨真实路口或完整开口召回。这个极近重叠案例不能替代后续可分辨近邻双路口的召回检查。

两场景各5帧/57600射线，无真实数据读取、标签文件或训练。新增检查没有模拟距离矩阵，使用实际网格、源场和CSG返回；仍只是合成教师局部行为。归属完整性与可评分区域未获证明。

## 独立V2接线结果

新增anchor_targets_v2/joint_targets_v2，V1默认语义保留；合成脚本加`--revised`选择V2，原无参数仍断言失败。相同原场景/轨迹V2输出1锚点、3开口、0确定归属、3未知归属，实际后端通过目标数量断言。进入/源内射线分别保存在provenance，源内支持不会绕过归属的原进入见证条件。17相关组件回归通过；实际双路口与分层完整后端测试尚未完成，不能将矩阵级反例冒充场景级验证。真实导出与训练0，完整评分区域和归属仍待解决。

## 源内证据实现进展

新增`gse_interior_branch_evidence_v1.py`：每帧传感器严格处于至少三个不同轴向incident源内，且没有第二路口同时满足时，按实际射线方向和唯一返回来源保存分支证据。复用现有扫掠场的近似距离（不是精确物理证明），原字段的+inf表示界外；首次接线误拒该合法值，已按原接口含义修复，NaN/-inf仍拒绝。

4组件测试通过（近邻重叠未知、分层不竞争、额外隐藏方向不抹可见三向）；这些测试直接提供距离矩阵，不能充当真实双路口/分层网格验证。原T场景实际后端产生源内证据9985/9985/9396，原端面进入仍0/0/1864。未更改场景或轨迹。当前目标生产器尚未接入新证据，旧零锚点断言仍失败，保留失败而不宣布修复完成。下一步修正版目标接线与实际近邻/分层反例，真实读取/训练0。

## 实际结果

可复现命令：在锁定cano_e1_zarr2187_v1环境，以PYTHONPATH=src执行`tests/v3/unit/check_gse_joint_producer_backend.py`。测试目前按预期目标断言失败，未将断言改为接受零锚点。

原构造serializer、三条重叠扫掠通道、CSG首次退出射线、五帧运动/来源绑定和联合生产器实际运行，未模拟上游返回。结果：3个窗口开口，0路口锚点；3个原端面进入见证分别为0、0、1864。锚点拒绝原因为FEWER_THAN_THREE_DISTINCT_OBSERVED_INWARD_DIRECTIONS。

左右支路起始端面分别位于x=-1和x=1，五帧传感器均位于x=0；两支的向内方向分别为+x和-x。射线可以沿左右通道看到表面并穿过窗口截面，却不可能从当前位置先沿向内方向穿过身后的构造端面。该端面是重叠构造的边界，不是必须经过的可见开口。

## 影响与处理

这是教师证据代理的漏标，不是模型训练失败。此前13/61等组件通过仍真实有效，但只验证了已提供进入见证后的接线，不能证明教师覆盖正常路口；“下一步只缺完整背景”的表述不再充分。

暂停以当前联合生产器生成正式人口标签或训练。保留失败脚本、原几何、轨迹和断言，不调轨迹或减小三方向数量来凑通过。

可选处理：继续要求原端面进入会系统性丢失这类样本；移动采样位姿只是绕过反例；推荐在原授权局部监督开发范围内，补充传感器已位于源通道内时的局部表面/分支证据，并与已有进入证据区分。必须同时测试近邻双路口与上下分层，不能用同源回波总数直接替代局部对应。这需要有限教师实现修正，不需要新数据范围、阈值或用户再次运行批准；修正前保留当前失败，不扩训。尚不构成整体方法科学GATE_FAIL。
