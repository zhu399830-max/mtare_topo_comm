# 训练真正缺什么

本次是 Phase 3 / Gate 3 软件核对，不是方法成功。原99合成条件不变，本次不重渲染、不生成标签、不训练。

实际 `gse_surface_losses_v1.py` 的8项损失允许分别屏蔽未知字段。只有可达性损失需要物理可达参考；结构学习不必等待完整安全证明。但锚点和开口检测要求有观测支持的实例及位置。部分监督V1/V2验证来源和参考覆盖，不能把错误构造截面变成正确物理开口。

75直管单元界、24曲面数值代理和射线支持，目前是几何诊断，不是完整实例位置标签。未知背景不能当负例；完整检测F1不能只算匹配上的真值。尺寸、可达性和归属缺失可以屏蔽，但必须撤回对应能力声明。

新增 `teacher/aperture_field_eligibility.py`，对应8损失字段，接入只读工具，4软件测试通过（0.11秒）。没有重跑99行新版输出，不能称为新的封存资格账本。

下一只读复用原45个独立评分条件的冻结记录，与损失适配器核对实例位置、重复目标及完整评分覆盖；不继续给物理数值代理加补丁。45例只能作合成接口验收，不能替代真实32例拟合。真实目标未满足前不启动训练，不改门槛、数据范围或旧结果；论文图片保留。
