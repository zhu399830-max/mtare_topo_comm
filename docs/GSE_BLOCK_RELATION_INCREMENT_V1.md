# 同分组的显式几何关系增量：软件接口

## 训练前资源与决策合同（2026-09-09）

一次合成CUDA前反向检查已完成（会话95221 exit0），57600点、54块、零优化器更新。两组各789256参数，共同初始化SHA-256为118f9f420b7675fea79ae2b43f58c0d7a98b4b404ec4516b205f94ffa7329ab9，峰值reserved343932928字节。此为合成资源检查，不是实际训练吞吐或模型效果；首组含预热，不能比较两组速度。

冻结原307观察（250拟合、27校准、30开发），C0/C1各2000更新，seed0、AdamW学习率0.001、weight decay0.0001、微批1累积4；原损失和评分不变。主决策在4米/10度合同下：C1相对C0开发可观察参考支路召回增加至少0.05，已知支路误报与未知预测不增加，节点TP不降低且已知节点FP不增加；全部父地图和1/2米、5/15度敏感性完整报告，不挑阈值。该线仅决定是否值得扩大关系实验，不替代完整检测F1、图质量或论文贡献验证。无收益不追加训练、种子或参数搜索。

正式执行绑定独立数据卡和规格gse_block_relation_train_v1，先preflight再只创建一次run；保留旧资产。以下“尚未冻结”等描述为此前软件阶段的历史状态。

## 真实既有缓存可行性核对与原训练接线已完成

只读检查20649 exit0，耗时92.87秒。使用原scope SHA360ad935554ff25fb53cfd77b890366afcff3de922102b1860fa4decdb89c10e，307观察250/27/30、8025627个原ROI回波；按原reader认证缓存/分组/上下文，未读取teacher载荷。R2每例3–54块，共50170条有向消息邻居；110个退化块保留，没有中心重合。全部307个观察反向排列后邻接对应及关系属性逐项一致。没有删除样本、归并块或改变分区。

新增block_relation_training_v1复用原build_model、forward_observation、固定调度，以及单次修正的objective/train_update。C0/C1同seed0初始化state逐项一致；点编码、原查询头及新增消息层均可训练，冻结上游编码器上下文的策略不变。这里复用的是原初始化方式，不是挑选旧训练最佳checkpoint迁移。

训练构建与关系模块合计3项测试0.92秒通过（57925 exit0）。本次无模型在真实人口上前向、无优化器更新。下一执行一次有界资源检查并固定两组运行规格；保持两组同输入/同顺序/同损失/同评分，不再导出一套新缓存。

本轮不再比较不同分块。固定同一超点分组及原点坐标、点编码、冻结上下文和路口—方向输出，检验显式块间关系的额外作用。旧R0/R1/R2结果保留为分组诊断，不改写。

## 复用已有代码

新BlockRelationIncrementV1复用gse_surface_relation_model_v1中的三层_MessageLayer，并复制现有BlockAnchorBranch作为输出头；不重新实现解码器或增加开口/可达性头。只接观察字段，不接教师节点、地点编号或真值候选。

每个块选择8个最近邻（不足8个用有效掩码），同距离按坐标排序，不按输入编号排序。邻接只用于信息交换，不表示可通行。重合中心导致固定k邻接语义不唯一时明确报错，不根据样本/模型效果删除块。是否在现有307人口出现尚未核实，正式训练前必须检查；不能隐瞒该限制。

关系向量9维：中心相对XYZ/10、中心距离/10、范围差XYZ/10、两端退化标记。这里没有新法向、自由空间或射线证据，不能沿用旧面片接口的9维含义。范围是当前坐标轴下的包围盒，不是通道宽高。

## 配对含义

- C1：相同邻居与消息层，读取9维关系。
- C0：相同邻居与消息层，9维输入置零；保留全部原坐标、范围和点编码。

C0不是无几何模型，因为原坐标和邻接已含几何。实验仅检验**显式相对属性**的增量，不声称去除了全部空间关系。两组参数布局/数量相同，但C0中乘零的关系列不获有效输入梯度，须报告此限制，不能仅凭总参数数相等宣称有效容量完全一致。

两组都必须训练并报告初始及最终评价，不能只给冻结C1破坏关系后分数下降就称训练消融。预算、初始化、可训练范围和原损失/评分需在正式运行前一次固定；本轮还没有创建训练card/run。

## 软件结果与边界

2项组合测试0.73秒通过：不同高度进入关系特征，块重排后邻居/关系一致，重复中心拒绝；两组参数数量一致，关系改变会影响预测，梯度可到达复用消息层，完整输出对块排列在数值容差内一致。

这些只是随机合成输入的软件证据，不是模型准确率或几何贡献。模型不是旋转等变网络，坐标破平局也不保证旋转等变，不能据此宣称整体旋转不改变预测。没有真实数据或checkpoint读取，没有优化器更新。

下一步：在原已授权缓存和分组上核实邻接唯一性、绑定坐标与容量，然后将新模块接到原读取/损失/评分路径并做单次资源测量。只有这些成立才冻结C0/C1训练；失败按具体数据/数值证据处理，不再次扩展其他模型或骨架参数路线。
