# 原超椭圆表示的局部中心约束检查

2026-09-07。新增解析局部灵敏度计算，不进行拟合或学习：中心、宽高、形状指数和朝向共同变化，先扣除形状/朝向能够解释的变化，再报告剩余中心约束。中心单位沿用10米，尺度和指数用对数参数；按点数平方根归一化，重复同一批回波不会人为增加指标。

4项合成测试通过：解析导数与数值扰动一致、完整轮廓有中心约束、短墙段约束弱、圆形朝向不确定不能误判为中心不确定、重复人口不增加指标。

固定原30观察中60支持候选的只读检查完成，273来源文件重新核对哈希。使用原源参数在对应弧长上的插值、参考截面及同源点，因此不是部署估计器。局部单截面近似不等于原变化扫掠三角网格，残差单独报告。

中心最弱方向的归一化灵敏度范围约0.0048—2.0447，中位0.3756；**不是米、置信概率或通过阈值**。最低的两例是S08 edge_0086的圆角和mixed实现，都是横向只看到一侧的候选。部分双侧观察也有弱方向，因此不以“两侧有点”作资格认证。

当前60例参考形状残差RMS最大约0.0249（隐式无量纲残差）。不能将其当米级噪声，也不能由局部非零导数证明全局唯一或中心精确。

代码：`src/mtare_topo/teacher/gse_superellipse_constraints_v1.py`；只读复现：`tools/v3/check_window_superellipse_constraints_v1.py`。不写真实标签、不改旧run，不挑候选或增加训练种子。

下一步需要实际尺度的位置歧义检查，而不是凭灵敏度临时挑一个阈值；保持原来源、观测与误差含义分离。qualified_labels及正式训练更新仍0。
