# 真实S08案例：确认两次假阴性来自数值对应方式

在原全人口诊断已绑定的来源内，只读S08 C07三种几何实现的缓存。每任务容器含16观察，共解码48观察；重放只针对同一sequence133465的三种实现，不据模型分数选例。沿用原方向生成路径，normalize64→cast32，Open3D0.19.0求交，与保存float32参数精确相等，并再要求唯一对应基元来源。未增加容差。

| 实现 | 旧支持射线 | 精确重放支持射线 | 五帧分别支持 |
|---|---:|---:|---|
| c1_mixed | 0 | 21141 | 3140 / 3597 / 4143 / 4780 / 5481 |
| ellipse | 112 | 21837 | 3345 / 3790 / 4306 / 4882 / 5514 |
| rounded_rectangle | 0 | 21141 | 3140 / 3597 / 4143 / 4780 / 5481 |

结论限于这三例：两次旧“无支持”确为数值对应假阴性，已有回波充分支持参考端面；不是模型失败或真实不可见。椭圆原已支持，精确重放恢复更多见证。射线数量不是独立样本数，这里仍只是一个物理末端。

使用已有cano_e1_zarr2187_v1环境（Zarr2.18.7/Open3D0.19.0），未安装依赖；另一个topology环境缺Zarr不能直接读数据，已避开而非改动其环境。重放后84个实际打开的来源文件哈希再核对通过。此次为只读原因诊断，输出在工具日志，没有新增正式sealed run；不能冒充已冻结修正版总体结果。

旧全人口run和labels保持不变。下一步将原数值对应纳入独立修正版，并验证米级“穿过端面再返回”反例仍未知。单基元重放未完整重建CSG分组；不匹配不能自动当负例。完整路口/开口监督和背景有效范围仍未解决，不开训。
