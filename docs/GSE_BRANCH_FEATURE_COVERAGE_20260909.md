# 特征已对齐，支路评分范围已明确

本轮按task、sequence、完整五帧行号对齐原feature_manifest与307个路口观察，全部唯一匹配；原特征文件均存在。清单SHA与封存值一致。本轮不读取/解码特征数组，不把文件存在当载荷校验通过，不重复编码。

| 分区 | 特征对应观察 | 全部参考支路数 | 有见证支路数 | 参考支路全部有见证的观察 |
|---|---:|---:|---:|---:|
| 拟合 | 250 | 787 | 774 | 237 |
| 校准 | 27 | 87 | 87 | 27 |
| 开发评价 | 30 | 93 | 90 | 27 |

合计967条参考incidence、951条保存见证、16条缺失。291次观察覆盖各自路口的全部构造参考支路；16次每次缺一条，涉及5父地图。这里的全部仅相对冻结构造incidence集合，不是完整物理开口、视野或地面通行的认证。

缺失观察的准确身份和来源帧已保存于`gse_branch_feature_coverage_20260909.json`。16次全部保留，不把相邻帧或三变体当独立样本；不给缺见证支路生成负标签。此前307/951方向绑定已检查接口是原构造成员，本轮比较完整原构造的paths数与已见接口数，不再重算方向或射线。

## 直接用于下一份配对合同的边界

1. 正目标使用已见方向集合；16次不完整观察仅对已知部分评分，未知不当负。291次的参考全集覆盖可作为闭集参考任务的候选评分范围，但需要在新数据卡中明确限定，不能把旧record的完整区域标志直接改True。
2. 位置误报复用条件锚点排除和确认结构重复预测机制。它不自动解决支路方向负例，4米参考合同也不是地点合并半径。
3. 两条通路用同一输入缓存、初始化和评分。现有缓存为点坐标、有效掩码与900×128冻结上下文，不是已经为这307例完成四种表示的分块缓存。仍需复用现有BlockPoints/分组适配，不能把45合成样本的超点缓存张冠李戴。
4. 307只覆盖路口部分。原1302次尽头参考与背景人口须纳入最终数据卡的保留/使用说明，不能为了较易拟合而从最终方法评价删除。

下一项固定最小配对数据与执行规格：明确参考条件支路集合、条件位置负例、未知评分及输入表示提取预算；复用已有特征、分组与读出，不发明新模型或重复前端训练。任何正式训练仍须数据卡/冻结输入/预检，当前没有训练run或方法收益。

复现只读检查：

```sh
PYTHONPATH=src /home/zeng-workstation/.local/share/mtare_topo_comm/envs/phase3_torch290_cu129_zarr2187_v1/bin/python tools/v3/check_branch_feature_coverage.py
```

会话72119正常退出；另一次只读调用保存16缺失记录，未改变数据。原manifest有2676条缓存身份，源哈希`9e9d12c056c8ad138a66710da93e58554b430af59f717a55eabfe11b6cd051bc`。原数组载荷完整性待正式消费逐文件核验。
