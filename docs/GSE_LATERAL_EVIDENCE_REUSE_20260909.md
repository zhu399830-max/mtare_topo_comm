# 侧面进入证据已有实现，不另造教师

本次核对并实际运行 `gse_observed_operand_entries_v1.observed_operand_entries`：它对原射线查询完整通道表面相交，只保留原首返回之前、来源唯一、法向为进入的相交。没有修改此实现或真实标签。

新增4项原生Open3D网格检查全部通过，直接调用测试函数耗时约0.78秒。旧CPU环境没有pytest，首次pytest命令未运行测试；随后用该环境runpy调用四个无fixture测试函数，exit0，没有安装或更改环境。

- 原解析T形反例的5条北向射线：全部识别到x=1的侧向进入，不再要求穿越y=0的构造封口。
- 前方有遮挡、后方通道长度6米与9米：相同原首返回给出相同空证据，未使用隐藏长度改变可见答案。
- 即便错误提供北侧来源ID，首返回距离仍拒绝遮挡后方交点。
- 多来源回波保留不确定，不强行选一个来源。

这些是有限射线几何函数测试，不是完整五帧标签资格。改变隐藏长度也不是完整的不同隐藏拓扑反例，不能声称已解决结构可辨认性。

完整接线也已存在：`gse_lateral_branch_evidence_v1`要求传感器唯一源内部、侧向进入点在原源内部、唯一同节点incidence配对及相反进出方向；`gse_reference_anchor_targets_v3`组合这些证据。构造身份只可用于教师，不可进入学生。

下一直接复用已有 `tests/v3/unit/check_gse_lateral_anchor_v3.py` 的真实合成CSG四场景（原失败T、分层T、普通T、重叠歧义）检查完整接线。该文件的old是当前v2，不是封存历史版本，必须明确版本边界。通过也不直接改2259标签；还需核验真实渲染参数绑定与有限修正范围。无候选校准人口缺口仍独立存在。

本次测试：`tests/v3/unit/test_gse_observed_operand_entries_v1.py`。训练、真实数据读取、完整教师调用均为0；旧实验及图片保留。
