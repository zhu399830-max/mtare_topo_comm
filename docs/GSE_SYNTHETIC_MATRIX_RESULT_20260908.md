# 合成监督矩阵：完成运行，但未获得完整标签资格

唯一运行：`results/gate3_semantics/gate3_20260908_gse_synthetic_matrix_v1_seed20260906`。

## 结果

144个主观察（720帧）、12个固定观测控制全部处理结束，退出码0。45个预先规定、有独立几何答案的条件全部通过；另外99个没有独立完整评分依据，不能称为通过。隐藏支路控制8通过、4被拒绝。没有新增训练步骤，没有完整标签或科研阶段PASS。

已完成主条件包括12类几何、3种截面、4个视点。独立评分只覆盖直道、尽头、可见堵墙的全部视点，以及T/Y/四岔中央视点。它检查锚点、开口的位置及多余/遗漏预测；没有因此证明所有归属关系、尺寸、可通行性或真实人口标签正确。

## 4个控制失败的直接证据

| 固定用例 | 无效返回射线数 |
|---|---:|
| hidden_branch__ellipse__view3 | 1 |
| hidden_branch__rounded_rectangle__view1 | 1 |
| hidden_branch__rounded_rectangle__view2 | 10 |
| hidden_branch__rounded_rectangle__view3 | 8 |

从保存的NPZ `valid_mask`直接计数；每观察57600射线。其余8个隐藏控制的无效返回数为0。失败保护要求全部记录段具有有效返回，故上述4项不能认证删除隐藏支路不改变观测。尚未证明无效返回由何种mesh/射线问题导致，也未证明有效射线均通过几何排除；不能把无效返回忽略后追认旧结果。

问题属于扫描/对照前提，不是训练失败。下一只读检查原保存射线、原mesh/来源绑定，定位这20条无效返回；不重渲染整个矩阵，不放宽有效性，不换场景，不启动真实人口扩训。若是实现错误，另版本修正并保留本次证据；若观察合同本身不足，则保留不可资格状态。99未评分仍是独立待解决范围。

## 成本与复现

- 实际运行1027.429秒，峰值RSS807215104字节，结果228343576字节。
- 36次场景构建，四视点复用场景，不重复渲染控制。
- 312个封存文件逐一SHA-256核验通过。
- seal SHA-256：`5d491bd1c6c89caeb47ea416eb135fee8a1d8a3507f70d95256cb55a91d9a395`。
- 输入卡与spec：`configs/v3/gate3/data_cards/gse_synthetic_matrix_v1.json`、`configs/v3/gate3/gse_synthetic_matrix_v1.json`。

本次结束的是一个诊断运行，不是整个建图方法或论文目标。
