# 可选扫描纠错：原20条无效射线均恢复预期外边界

## 实现与验收

`CSGMeshProvenanceRaycaster`新增默认关闭的`rescue_missing_with_interval_winding`。只处理原结果None，不改变原有效返回。对原交点间区间计算每个operand的整数绕数；source64与packed32必须一致，起点必须与原inside一致，边界来源必须对应原相交operand。找到联合内部首次变为外部的边界才返回。

数值非整数（积分残差超过1e-6）、表面查询、负绕数、精度不一致、每射线超过128个不同交点或证据不完整均拒绝。1e-6为数值整数检查，不是新的空间净空门；128为保守计算容量，不截断交点。禁与signed-field/unique-candidate过滤选项共用，防止绕过旧资格过滤。原1cm分组参数未改。

12项测试通过（0.79秒）：内部重叠最后边界、普通单层、重复闭合壳/重复事件、无状态变化事件、分离通道、重合operand完整来源、不完整面/起点不一致拒绝、真实Open3D旧失败新恢复、普通有效结果不变，以及已有四项CSG对照。离散伪相切事件已覆盖；这不等于任意实际退化相切网格都已证明正确。

原封存20射线`inspect_synthetic_invalid_returns.py --rescue`退出0：旧模式20条均None，新模式20条均对应上一轮独立诊断的最后外边界。没有重渲染完整帧，没有保存新扫描或生成标签。原4控制FAIL不追认，原312文件封存不改。

## 下一项集成范围

只准备4个原失败观察的新版本局部纠错卡/spec：ellipse view3、rounded_rectangle view1/2/3；4个五帧观察，共20帧出现、230400射线位置，其中20个旧invalid允许用新选项确认返回，其他230380位置必须逐元素保留。派生观测明确新版本，不能称为原观测完全未改。

重新绑定来源编码、检查4个新观测的隐藏删除对照与部分标签一致性；原结果保留，不重跑144矩阵。新文件/卡/spec未生成前不开始数据导出；99未评分范围与真实人口/训练资格仍未通过。若派生观测的源绑定或反事实前提失败，停止该项，不修饰为PASS。
