# 141观察来源和历史V8已绑定

`v8_multiview_manifest_v1.py`按已定位的13个任务保留全部窗口，精确绑定原2259清单的行索引、task、序列身份、原逐观察证据路径与SHA。实测141观察、13任务、5父地图、193去重变体帧；全为C01—C06拟合部分。生成文件与再次编译逐元素相同，重复编译确定性通过。

原十例封存数据卡的geometry_settings已实际读取并验证seal/hash：mesh轴向0.05米、角采样64、field0.025米。不是从合成默认值推测。原入口启用的源精度保护保持true。

`v8_teacher_snapshot_probe_v1.py`在原CPU环境成功隔离导入历史V8及原raw诊断，exit0（约0.16秒）；所有初始加载项目模块均来自旧ZIP，并记录归档成员SHA。归档SHA为b963b3207c65b2ed69504f4d6040e9b94a05d18f9305dc35ca0ca2a3ff04efea。当前11处源码差异没有覆盖或混入。

这只是来源清单和导入验证。真实扫描读取、完整教师调用、模型调用与训练均为0；尚未创建正式运行。141选择针对已发现的错误机制，不能当无偏训练/泛化人口。

下一复用既有bundle传输与原2259读取器，接独立V8 worker并检查返回来源/动态依赖；数据访问必须缩到这141观察，不能因复用reader读取其余2118观察。随后冻结独立数据卡/规格和资源上限，预检一次执行，不重新跑原十例或全2259。

清单：configs/v3/gate3/v8_multiview_mechanism_manifest_v1.json。原结果与论文图片不变，项目目标仍未完成。
