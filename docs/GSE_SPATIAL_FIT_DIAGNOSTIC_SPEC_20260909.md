# 同16样本SPATIAL补充诊断

当前问题不是发表结论，而是区分“共享检测管线不能稳定拟合”与“基元组织路径特有问题”。按最新用户明确授权，只新增一次SPATIAL中心拟合；PRIMITIVE复用已封存的`gse_grouping_center_supervision_v2_seed0`，不重新训练或重复损失/梯度审计。

## 数据、初始化及监督

精确16观察、12个中心参考、5个父地图和5个独立路口，五帧历史共80次引用、68个唯一源帧。父图为S02_3d_tree_small_C05、S03_flat_unicyclic_small_C04、S06_3d_branch_medium_C02、S08_3d_loop_rich_C01、S10_3d_complex_C04。无正例4观察不等于完整背景。

保留前次精确源帧清单、目标及输入哈希、相邻五帧顺序、seed0选择和顺序，不读C07评价载荷/C08—C10。不补标签、不扩大样本。

模型793992参数、位置残差前三行零初始化、其余参数同seed0。复用已确认监督：4米背景排除不变，新增的观测明确且唯一已确认实例的重复候选监督不变；未知不作背景。中心位置/10、平衡存在性、训练匹配、主1米及辅助0.5/2/4米评分都不变。

唯一表示差异：PRIMITIVE使用已有几何分块；SPATIAL使用相同点上的确定性最远点采样及最近种子分组，逐观察严格匹配块数M。原始点、源帧、观测查询和预训练上下文来源相同；上下文按各自分组重新汇集。由分组产生的块中心、范围、邻接和池化差异属于同一组织路线差异，不能从此实验单独归因某个几何属性。

训练仍为1000次更新、AdamW学习率0.001/weight decay0、微批1累计4；无分支损失/匹配、无增强及新种子。只看完整过程与最后一步，不选好轮。

## 已有预测先做的分解

使用每个检查点已保存的全部预测，不重跑网络：

1. 忽略置信度和覆盖筛选，在1米内作最大匹配数量优先、距离次优的一对一匹配，得到定位召回上限。
2. 仅保留原sigmoid≥0.5的预测，重新计算一对一定位上限。
3. 与原始已知域实际匹配比较；未知不改背景。

全体目标数减第1项为定位缺口；第1减第2为置信筛选缺口；第2减第3为覆盖/匹配缺口。单预测不能重复恢复多个参考。逐目标另列无近邻、近邻低分、被覆盖排除与一对一竞争。仅“每目标都有最近邻”不算一对一召回。

PRIMITIVE末轮已得上限8/12、置信筛选后8/12、实际8/12；四个最新漏检均无1米内预测。第100步有1个低分近邻，第900步有2个；第800步上限为0。前置结果保存在`docs/figures/gse_graph/primitive_localization_ceiling_20260909.json`。

## 可比性门禁与证据

运行前比较公共模型、初始化、监督、数据读入及独立评分源码哈希；训练循环AST除PRIMITIVE→SPATIAL选择外完全相同，SelectedReader AST相同。初态权重逐元素与旧第0步比较；16项点、帧、目标、两种分组数组及查询坐标/索引与旧证据比较；环境与1000批顺序相同才训练。

保存精确卡/spec/环境、对齐检查、同16初末图、11组检查点及全部预测、原始训练日志、逐检查点定位漏斗及新旧对照。新增8项测试只覆盖新一对一分析和SPATIAL接线，不重做旧教师/梯度审计。

GPU28GiB、主机32GiB、新增30GiB及原12小时上限保留；实际仅1000步。输入漂移、配置不一致或数值错误停止，不重训PRIMITIVE凑公平性。

## 结论边界

- 两条路径都无稳定定位上限：优先定位共享检测/优化管线，不能归因基元路线无效。
- 定位上限高而实际召回低：定位存在性或覆盖接口，不换几何表示掩盖。
- 仅一条路径明显退化：记录组织方式相关性及接线检查，不用单种子小样本证明方法胜负或唯一根因。

不开展正式扩训、分支、真实建图；即使小拟合通过也不自动推进。原初始化与监督修复继续保留。
