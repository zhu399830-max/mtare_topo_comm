# 位置字段资格检查：候选支持不等于中心已确定

2026-09-07，合成反例；没有读取新真实数据或修改此前run。

构造两个封闭矩形通道，具有相同可观察顶面片段和远端返回，侧壁未被两条有效射线看见。两组射线的起点、方向、距离、有效性和来源帧完全相同；独立解析射线—盒体计算确认两通道都产生所给首次返回。

它们在10米局部球边界上的参考轴线中心相距12米。当前“同源穿越＋一个局部边界返回”规则在两者均给出proposal_supported=true，且支持索引相同。

因此这个规则不能单独认证参考中心位置。反例只有稀疏合成观测，**不能据此说真实60候选位置都错或全部无法判断**；它证明的是规则作为充分条件不成立。也不证明几何关系学习原理不可行。

新增测试`test_gse_window_opening_identifiability_v1.py`包含真实几何、相同射线、独立首次返回核对及12米中心差；与候选和竞争测试合计9项通过。测试通过表示成功复现局限，不是语义教师通过。

当前处理：保留候选层与原60/63统计，不将reference_position_m直接转换为opening_position_m训练目标，不调阈值追通过、不扩大真实运行。宽高与中心字段必须分别检查观测约束；有完整信息的真实例不能因为这个反例被一概删除。

下一步只检查现有真实候选的表面几何约束与中心可确定性，不新增第三套标签、网络或长训练。qualified_labels仍0，所有旧证据和图片保留。
