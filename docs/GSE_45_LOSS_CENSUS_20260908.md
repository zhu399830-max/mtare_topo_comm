# 45个已评分合成观察的训练接口核对

## 后续软件实现：限定合成oracle完整监督

`evaluation/gse_fixture_loss_targets.py`只接受固定声明内容完全一致且独立oracle覆盖的原型；不读取生产标签来决定完整性，不接受未覆盖视点/修改地图。位置与方向来自独立公式，尺寸、可达和归属保持未知。它仅是软件检查接口，不授权真实教师导出。

新3项回归加原损失24项，共27通过（1.20秒）。实际反向验证完整空锚点区域的多余节点获得压低存在概率的梯度，区域外梯度为零；改回部分背景后不产生锚点负梯度。首次2项因NumPy scalar不满足标准数值接口失败，已在新适配器显式转换；未放宽原校验。没有优化器步骤、数据导出或模型效果结论。

下一准备复用封存合成扫描的一次有界拟合规格，先核公共冻结编码器与输入能否无重导接线、精确记录有效人口和成本；不把该软件通过当真实32例门通过。若资源/数据合同未满足，不启动训练。

只读工具 `tools/v3/inspect_scored_fixture_loss_targets.py` 在冻结Torch环境退出0；核验原summary和45个压缩记录SHA，再逐例通过真实 `observed_targets` 适配器。没有修改标签、重渲染或优化步骤。

人口：直道12、尽头12、T/Y/四叉中央各3、可见阻挡12，共45观察。不是45个独立真实地点。

| 实际进入损失的字段 | 有效数量 |
|---|---:|
| 锚点位置 | 27 |
| 开口位置与方向 | 各84 |
| 已知归属 | 48，全为true |
| 尺寸、可达性、物理参考 | 均0 |
| 完整锚点背景、完整开口背景 | 均0观察 |

18观察没有锚点正例，但这不代表它们会教模型“不应产生锚点”：生产标签背景明确不完整，基础损失屏蔽未匹配预测。部分损失可能另外提供条件负监督，本统计不声称它们全部无负梯度。

独立合成评分器确实对限定直管/中央星形场景提供完整预期并检查数量；生产标签有意不宣称一般世界完整性。这是两个不同合同，不是把评分PASS直接送入训练就自动获得完整监督。48条正归属也不能独立验证归属判别能力。

下一步：实现只接受这些已声明原型、明确分离于真实数据的合成完整监督适配与过预测负梯度回归。完整性只能来自固定原型独立oracle，不从生产标签数量或模型输出推断。之后才能决定是否做一个注册的合成拟合；不把它代替真实32例或几何组合收益实验。不再重复同45例盘点，不更换网络或追加种子。
