# 观测面片交会候选：未改善覆盖

单次运行 `results/gate3_semantics/gate3_20260908_gse_axis_patch_diagnostic_v1_seed0`，预检通过；45观察全部完成，耗时5.522秒，峰值RSS435441664字节。0训练和学习推理。56文件seal核验通过，SHA为`d1a593d2604ce1a63d66de776bbe2a317184ede8f16bc2a008fcd07b0885c848`。

| 候选来源 | 总数 | 1米匹配 | 漏失 | 多余 |
|---|---:|---:|---:|---:|
| 仅轴对（封存前次及本次独立字段） | 34 | 9 | 18 | 25 |
| 轴对＋面片交会 | 37 | 9 | 18 | 28 |

2米和4米统计相同。18可观察终止锚点没有任何恢复。新增3候选来自圆角矩形的T形1个和Y形2个，不是尽头。原始日志、各例拟合轴线、面片交会支持、评分和环境已保留。

预登记的27锚点全覆盖条件未满足，停止用这套候选集合启动完整节点学习筛选。它是候选规则失败，不是一次新的训练失败。

可直接定位到的证据：圆形尽头view2存在600面片、575个数值上唯一的轴线—面片平面交点，但0个通过面片外接框包含条件。可见阻挡对应例同样如此。因此不是整个流程没有生成面片，也不是所有线面都平行；过滤后没有有效候选。尚不能仅凭这三个数确定是轴线位置偏差、稀疏面片范围、法向还是缺少端面支持。

下一只读定位固定尽头例的回波、法向与交点到局部支持范围的距离，沿用旧扫描和参数，不重训、不扩大范围、不把失败结果改成通过。尤其不能未经证据就放宽面片范围，或用真实尽头中心补洞。若最终需要通道截面而非无厚度轴线的支持判定，应另立版本与反例，不静默修改本次成绩。

当前结论：中央路口位置候选仍有覆盖；完整结构候选尚不合格；几何关系学习是否改善图仍未验证。
