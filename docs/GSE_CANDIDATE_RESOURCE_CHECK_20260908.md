# 候选读出：真实缓存组装与GPU资源检查

`data/gse_candidate_training_examples.py` 将已核验候选、上下文、几何掩码、八邻居及独立目标组装为共享样本。真实执行得到45观察、32469候选，其中468个正支持候选、32001个背景候选。27锚点覆盖不变，目标不进入模型前向。

`tools/v3/check_candidate_readout_resources.py` 在主机GPU对预定四叉圆形view2执行ABC前向与反向，无优化器。该例1296候选，也是本人口最大候选数；5个正支持和1291背景。每组355457参数。A/B/C峰值分配显存94716416/95129600/174537728字节，主机峰值RSS1566785536字节，均低于原28GiB显存/32GiB主存上限。有限梯度检查通过。

A/B/C测时约0.220/0.0058/0.0089秒，包含首组CUDA初始化影响，不能据此声称C比A快；这也不是500更新训练的端到端吞吐预测。

命令环境为固定Torch sidecar，OMP/OPENBLAS线程1，CUBLAS_WORKSPACE_CONFIG=:4096:8。执行终端session73162退出码0。新增优化器更新0，编码器推理0；仅源绑定缓存组装与单例资源检查，不是正式训练结果。

下一冻结一个新的不可覆盖候选拟合run：ABC各500更新、seed0、相同初始化与样本顺序、冻结编码器、初末完整评价、同时保存去重前后全部成绩，不选最好轮数。沿用微批1/累积4、AdamW学习率1e-3及weight decay1e-4预算；在精确新卡与预检后执行。若有拟合收益仍需开发人口泛化和图质量验证，不直接进入闭环。
