# 共用合成扫描适配与隐藏对照差异

新增SyntheticSensorScene：同一构造/截面缓存一个mesh与距离场，四视点共用；切换构造或控制条件需新scene，哈希不同时拒绝缓存复用。沿用原16×720扫描、.05m/64网格与.025m距离场、因果相对运动、来源代码及verify_alignment。代码本身不生成标签。

新增独立直管尽头正控制：解析射线与端平面求交，要求线段位于实际64边形截面内部且端点在10m ROI。不是只检查理想超椭圆（它可能略大于离散壁面）；不调用标签生产器。此解析器仅适用于声明的凸直管，不冒充通用矿井可见性或机器人通行证明。

6项矩阵/构造/解析控制单元测试通过，1.91秒。当前正控制覆盖直道、尽头、前端封闭腔体三类，其他交汇/遮挡的独立可见性定义仍未完成。

## 四观察实际软件检查

固定circle的straight view2、terminal view2，以及hidden_branch view0的原/控制两观察。仅合成软件检查，无真实数据或结果目录导出。

- 直道五帧57600有效返回，局部端面支持0。
- 尽头五帧57600有效返回，凸64边形解析条件支持4840条端面射线，均有效。初版理想圆条件数4844，已在运行范围未冻结前纠正为实际多边形，不能混报。
- 隐藏对照有效掩码、平移与相对航向逐元素相同，但ranges_m不完全相同。
- 15条不同，最大差2.384185791015625e-7m，所涉返回约1.988–2.008m。
- 原严格相等断言保留，脚本退出1；未修改射线、未加容差宣布PASS。两次执行分别用于初检和差异定位，不是两次正式实验。

## 下一步及边界

不能因为地图名叫hidden_branch就认定新增支路对观测无影响，也不能用小差异直接当作数值误差证明。下一独立检查删除支路的实际mesh包围范围是否与原记录的射线段完全隔离，明确它能证明的是记录观测段的几何排除，而不是任意隐藏地图或全局安全。

若几何排除成立，才能讨论固定共同观测、改变不可观测构造的反事实测试；这与分别渲染两次要求bitwise一致不同，必须明确变更测试定义并保存原不一致结果。不在本轮静默转换。若排除不成立，当前配对不合格，不再随意选位置凑通过。

156观察正式合成批次尚未冻结或执行。没有新训练/模型结果；旧证据与论文图片不动。
