# 几何关系增量作用：对照接口已实现，尚无训练成绩

原B与C不仅相差关系属性，还相差邻居消息。因此原B/C差异不能单独归因于几何关系属性。

新SurfaceRelationControlV1复用原C全部结构，完整分支数值与原C逐元素一致。C0保留相同邻居、三层消息、参数布局、原始观测通路和32结构/64开口输出，仅把9维显式关系属性置零。输入参数不包含构造身份或teacher。未知值不通过置零静默隐藏：非有限关系输入仍报错。

## 实际检查

联合运行test_gse_surface_relation_control_v1、test_gse_surface_relation_model_v1、test_gse_surface_losses_v1、test_gse_surface_prediction_scoring_v1：46 passed in 1.93s，会话94685 exit0。

证明完整分支与旧C一致；C0等价于C输入零关系；C0仍响应邻居变更但忽略关系变化；两分支消息层和原始观测路径都有梯度，冻结context无梯度；C0关系列梯度为零。新模型接现有surface_relation_losses时，将membership标未知，该任务有效计数为0且两个membership头不获得梯度，其他受监督路径继续工作。没有优化器步骤、地图读取或训练导出。

## 不能得出的结论

这不是几何关系有效的实验成绩，也不是拟合成功。C0保留一元坐标、法向和几何邻居，能够隐式计算关系，不能称为完全无几何。关系列置零意味着这部分权重没有有效输入，参数布局相同不等于全部有效容量相同。现有模型不是旋转等变模型。新类不能被用来偷偷重启已经耗尽修正预算的旧位置读出实验。

下一须核对既有结果与共同标签适用范围，确定还缺哪一个可有效评分的配对比较；同一来源、未知掩码、样本顺序和最终评分必须绑定。若目标缺失则不能对未监督的归属/不确定性头宣称学会，不扩大种子，不制造路口实例标签。
