# 分支数值资格模块

本次完成可复用的非标签模块 `evaluation/gse_cap_branch_precision.py`，并接入只读路口检查工具。输入是原三角形、原射线和原方向，不允许float32代替原双精度几何，不伪造射线编号。

模块用上一轮源坐标区间计算判断进入方向。相同射线若有多个三角面记录，只要存在不稳定记录，就不能通过挑选有利记录获得资格。方向计数沿用原精确方向区分，不新增角度或距离阈值。未知与稳定射线分别返回；所有结果显式声明不是标签、物理连通或完整观测资格。

新增5项测试：三个稳定方向；一个稳定加两个边界方向；顺序/重复方向；重复射线冲突；无效源与编号拒绝。与原20项检查共25项通过，0.29秒。这里只是合成几何模块测试，不是完整CSG标签程序的路口测试。

只读工具已接入模块并通过语法编译。本轮没有再次读取四个真实观察、没有新增标签或训练。上一轮工具结果仍引用上一轮源码和文档，不冒充本轮新执行成绩。

下一项为源绑定适配：复用原扫描身份、返回前/ROI和构造核验，把数值资格结果显式交给调用者；先在真实代码路径的合成观察中检查稳定和边界分支。不得静默改变旧生产器默认输出、覆盖FAILED运行或直接启动第三次教师批次。
