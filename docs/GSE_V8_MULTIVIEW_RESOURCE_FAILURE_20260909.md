# 141观察验证因资源错误结束，121观察证据已保留

会话43871真实退出1，RUN_STATE=FAILED。完整运行约1288.59秒（21.48分钟）；不是仍在后台运行，也不是完整141通过。

已完成121观察，第122个失败：S10_3d_complex_C04__c1_mixed，source192654，decision_index1，arc5米。原始日志显示Open3D/Embree在`scene.list_intersections`申请内存失败，调用来自原封存V8的侧面进入证据函数。

原上限为父1GiB、worker3GiB虚拟地址空间。测得父峰值RSS382386176字节、worker1767632896字节。RSS低于上限不代表分配应该成功，因为地址空间限制不是RSS限制。当前证据不能区分单次全部相交规模、原生缓存/分配器占用和跨观察对象保留；不得立即认定物理RAM不足或算法无效。

137个封存文件的SHA和精确文件集合核验通过。seal SHA为2e5ddeef503cbe00fd851e2342d2480a4cd4230b831233202a809f04580cc9f6。121条完成日志与121份逐观察证据存在；summary.completed_observations为null是执行器仅在全批返回后赋result造成，不能理解为零完成，也不改写封存summary。

原首任务6位置保持路口的结果现在已随失败run封存，仍只能作为局部机制证据。最后20个观察未完成，不能删掉它们后宣布全批成功。模型推理/训练均为0。

## 接下来的处理

1. 保留这次FAILED，不覆盖、不续跑，不重新计算整个141。
2. 只读已有源码、已保存证据大小与日志，定位全射线相交的内存峰值来源；用无真实数据的合成网格检查固定分块相交与一次相交的等价性和内存行为。分块是计算调度，不得改变射线、阈值、几何或丢弃相交。
3. 仅在原因与等价性有证据后，登记一次独立执行修正；是否可复用121份结果必须以精确源/算法和比较合同为依据，不能直接拼接掩盖失败。当前不执行修正或重试。

若分块不足以解决，继续资源诊断而不是加训练。校准人口覆盖缺口和学习有效性仍是独立待解决问题。
