# 官方超点组件已独立构建，分块连通性反例已确认

## 后续完成：有界原生接口与最大输入检查

`gse_spg_extractor.py`完成体素→cKDTree近邻→官方几何特征/分块→连通适配→原点映射。近邻只返回每点46项，不建立N×N距离矩阵；保留45几何邻居/10邻接邻居、reg=.1、verticality倍权2。近邻等距截断不宣称任意点序下同一结果。

输入上限57600点，不截断；空与少于46体素返回明确未分块状态，保留体素/原点映射。原生结果必须有限、分块成员与assignment一致且每点恰好出现一次；不会用零填充修复NaN或丢点。已验证23项联合测试（0.23秒）。

持久独立前缀额外解包SciPy1.11.4-6build1，未安装系统包；其deb SHA256为`ab2abe2431b49d54f203cc2cc394885a3a8bf3a3eebf0b1748f916158a1c9f31`。原组件清单V1不改写，本项作为新增依赖待正式运行统一冻结。

真实原生调用软件检查：4096合成平面点0.103秒、进程峰值75,403,264字节；57600点1.108秒、峰值290,508,800字节。两例原点映射和五帧计数完整。该简单平面耗时不是复杂矿井最坏成本保证；未生成新扫描、标签或训练。

复现：使用上述独立PYTHONPATH/LD_LIBRARY_PATH，以系统Python运行`tools/v3/check_spg_native_resources.py`。此前`smoke_spg_partition.py`的全对全距离仅用于128点软件例，正式适配不使用该写法。

下一准备同一封存45观察的R1/R2表示提取合同与统一属性接口，先绑定完整运行依赖、来源及资源上限，再预检执行一次提取。只比较分块数量、几何属性、来源映射与成本；共享检测读出未合格前不训练、不报节点或图质量增益。

## 后续完成：持久构建资产和五帧体素映射

独立目录已复制到`build/gse_spg_cpu_v1`（不覆盖旧目录、不删临时资产，约256MiB）；9个发行版包、2个官方源码包、2个库的13项SHA及系统Python二进制SHA保存在`configs/v3/environments/gse_spg_cpu_build_v1.json`。它只是组件构建清单，动态运行依赖闭包尚未冻结，不冒充完整正式sidecar。

`gse_spg_voxel_mapping.py`复现官方prune的float32坐标、以观测最小坐标为原点、首次出现顺序编号及均值累加，额外保留原点→体素、点数及五帧计数。没有改成零原点网格，也没有删点或猜来源。空、非法帧、非有限数据、uint32网格溢出明确处理。

16项体素/连通联合测试通过0.12秒。固定种子20260906的512个合成点，缩为263体素；与持久目录内官方libply_c.prune中心逐比特相等（最大误差0），五帧计数总和512。单个样例一致不证明所有边界输入或点序都一致。

下一完成有界近邻/原生调用适配，禁止全人口两两距离；对少于46点、退化几何、空点及容量超限在调用C++前明确处理，再冻结完整提取器资源与运行依赖。不读取研究人口或启动训练。

## 后续完成：连通适配验证

`representation/gse_connected_partition.py`已实现每个raw标签诱导子图的无向连通分量拆分，不新增边/距离阈值、不混合不同raw标签。返回raw标签、原点到新分块、分块到raw及全部成员索引；输出与输入复制隔离。特别测试了相同标签不能借其他标签的路径连起来。

9项测试通过（0.08秒），覆盖断开同标签、跨标签路径、重边/反向边/自环、空与孤立点、映射、负标签/越界和浮点索引。官方128点烟测连续3次均保留raw=[128]，适配后=[64,64]。这是本项目的适配结果，不改写上游输出；对固定点序确定，不宣称数值标签对点序不变。

下一补齐持久独立环境的可复现构建记录及体素—原点—五帧来源映射，再做有界原生接口资源/退化输入检查。没有正式表示人口提取、训练或图成绩；原监督及检测瓶颈仍在。

本次是软件构建与合成调用，不是研究数据实验。没有读取地图、生成标签或训练，没有安装到系统或冻结Torch环境。

## 实际构建

独立目录：`/tmp/gse-spg-build-ArlXbU`，当前约256MiB。下载Ubuntu依赖包后使用`dpkg-deb -x`解包到该目录的`prefix`，没有运行包安装脚本。

- Python：系统3.12；NumPy：解包1.26.4；Boost：1.83.0-2.1ubuntu3.2；Eigen：3.4.0-4build0.1。
- SPG commit：`0209777339327c9b327b6947af6c89b20bb45981`；源码压缩包SHA256：`58869706c9ba7b7cd678071ae8c810d9c6b30ce63a4263c94b6ba80a664ad7b4`。
- cut-pursuit gitlink：`e9501d158c94c286b3cd84563f0636268886fa50`；源码包SHA256：`42e11ed08fe4a82721006ef13bb8dbfbf911d82141132365480119ce23684d34`。
- `libply_c.so` SHA256：`17c4babe96206c5db4f2f699ba7565b06ac10cb12d80d86a416a77a6e12d757e`。
- `libcp.so` SHA256：`8790b359819d7f3002cb92959546d2cb469b1807d865d279bb7b73fba073b607`。

使用g++直接编译官方翻译单元，未编辑上游算法。共同选项`-shared -fPIC -O3 -std=c++11`，包含Python/NumPy/Boost头文件，链接boost_numpy312、boost_python312、python3.12。ply另加Eigen头文件与`-fopenmp`；cp加其include目录。首次cp命令遗漏这个目录，报API.h找不到；补齐官方include路径后成功。保留这一执行错误，不称首次编译全部通过。

源码/依赖及二进制目前是临时软件资产，尚未成为冻结可部署环境。后续若纳入实验，需在持久独立前缀冻结全部包/命令/动态库依赖，不能直接以此文宣称环境复现完备。

## 烟测及限制

固定种子20260906，生成两片相隔3米的平行平面，每片64点，共128点。只用官方几何特征45邻居和分块10邻居，reg=.1，verticality倍权2；不运行Delaunay，不做训练。该烟测没有体素预处理，故不是完整R2人口提取器。

结果：全部点有合法分块对应，连续3次分块等价；每次返回1块、128点。随后独立遍历输入邻接图，确认跨层边为0、连通分量大小为[64,64]。所以“分块ID相同”不保证几何邻接连通，不能直接用作连续表面或可通行节点。该反例不证明SPG论文失败，只约束本项目的接线方式。

这次3次相同不证明所有输入、点序和环境都确定。当前固定C++包装器内有`srand(0)`，不应与作者对整个流水线随机性的提醒混淆。

## 下一步只允许的局部处理

保持上游分块结果作为原始证据，不调正则/邻居数。实现表示适配层时，对每个分块与**已有输入邻接图连通分量**取交并拆分为连通部分；保留raw block→connected patch及原点映射。该拆分没有新增距离阈值，但必须明确是我们的适配，不冒充原始输出。

先用软件反例检查：已断开的同属性表面拆开；原本连通的同标签区域不变；不同上游标签不混合；空/重复/非法索引明确处理。只有此类接口验证后才能冻结正式表示提取，不训练、不把分块邻接当机器人通路。

复现烟测：

```sh
env OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 \
PYTHONPATH=/tmp/gse-spg-build-ArlXbU:/tmp/gse-spg-build-ArlXbU/prefix/usr/lib/python3/dist-packages \
LD_LIBRARY_PATH=/tmp/gse-spg-build-ArlXbU/prefix/usr/lib/x86_64-linux-gnu \
/usr/bin/python3 tools/v3/smoke_spg_partition.py
```

项目总体目标仍未完成，候选检测失败和投票监督缺口没有因此解除。
