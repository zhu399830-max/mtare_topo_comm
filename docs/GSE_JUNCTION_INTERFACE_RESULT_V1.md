# 真实界面诊断：有穿越记录，但细微先后顺序不能当连接标签

## 本次实际做了什么

沿用原固定C01的10父地图、10边、三几何实现，30观察／150帧。预检通过后只创建一次独立run，5.0697秒完成，峰值581111808字节，结果107073315字节，43项seal核验通过。原数据、旧run和图片未改动。

运行：`results/gate3_semantics/gate3_20260907_gse_surface_junction_interfaces_v1_seed20260906`。

这30个观察中，只有S08的同一个真实路口（三个几何实现）在当前10米范围内。不能把它们当三个独立路口，也不能把其余27观察算漏检。

| 几何实现 | 原始界面交点 | 有窗口内穿越的射线 | 有有序界面对的射线 |
|---|---:|---:|---:|
| mixed | 70207 | 33869 | 20381 |
| ellipse | 72037 | 34990 | 20223 |
| rounded rectangle | 70207 | 34090 | 20547 |

以上是条件式参考诊断，不是检测准确率，也不是可通行证据。

## 发现的问题及依据

路口node_0088的三个参考端点为edge_0084末端、edge_0086末端和edge_0092首端（接口0、1、2）。接口1与2之间的顺序既出现1→2，又出现2→1。

mixed中，2→1有13682条记录，间隔中位数约0.000000477米；1→2有6696条，中位数约0.000000954米。三实现该接口对最大间隔都小于0.00064米。相比之下，0→1的中位间隔约1.96—2.03米。

这说明相近界面的浮点求交次序不能直接解释为有意义的空间结构次序。原因可能涉及几何近重合与求交精度，尚不能仅凭这些记录断言原网格完全共面。原组件只对参数完全相等保留无序，因此软件满足其合同仍不足以验证语义合同。

## 决策

不改变本次run，不放宽距离容差、不将两万条射线包装成成功标签，不重新运行整批来追求更好数字。停止把细微界面先后关系用于语义标签；保留原交点作诊断，下一步核对源界面的空间关系与实际可见表面边界。也不要求所有真实分支必须能被同一条直线贯穿。

新语义标签0、训练更新0。此结果揭示了自动教师证据的局限，不是新模型失败或几何关系学习原理被否定；A/B/C方法贡献仍未验证。
