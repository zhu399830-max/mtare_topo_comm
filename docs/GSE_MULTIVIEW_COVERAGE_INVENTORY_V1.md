# 多位置修正：先冻结候选覆盖，不直接启动训练

针对旧307样本集中在穿越首窗口的问题，本次复用原20260906哈希排序每父地图第1条物理边及原方向，扩展为该穿越的全部既有五帧窗口。没有使用目标类别、可见性或模型分数来挑样，也不补造跨穿越历史。

| 分区 | 父地图／有向穿越 | 五帧观察 |
|---|---:|---:|
| 拟合 C01—C06 | 60／60 | 2079 |
| 校准 C07 | 5／5 | 180 |
| 开发评价 C07 | 5／5 | 135 |
| 合计 | 70／70 | 2394 |

三种几何实现共享798个逻辑窗口，去重源帧3234个，包含原背景取样角色的210个观察。这些不是2394个独立地点，也不是已经核验的2394份结构标签。C07仍是历史开发分区，不称严格未见测试；不读取C08—C10。

清单程序重新计算并逐项核对原哈希选样身份、三变体对应和连续窗口，2项软件测试通过。每个观察保留task、source_sequence_id、sequence_row、frame_rows、弧长、原split及读取来源哈希。输出不可覆盖。

## 现在还不知道什么

正例、完整可评分背景、未知比例全部保持未定。按完整穿越取样可避免只保留最近位置，但不能保证源扫描能支持路口结构，也不能保证起始路口之前有连续历史。“原背景取样”只是取样角色，不是没有路口的负标签。

## 执行边界

这是候选元数据清单，不是训练数据卡或标签导出批准。下一项核算旧输入包／缓存复用与缺失源chunk，分别绑定拟合和校准的精确读取、监督核验范围；开发评价保持分开，不用其结果调标签或阈值。所有候选的失败与未知都保留，不能再次只挑成功的单窗口进入模型比较。

在新的源读取或标签生成前，提交精确数据卡、源码和运行规格，沿用已授予的常规执行授权。先确认同一结构的多位置监督一致性及完整背景覆盖，再决定是否启动原有限修正实验。旧307与旧预测不改写，现有网络、地图与编码器继续复用。

清单：[multiview_coverage_inventory_v1.json](../configs/v3/gate3/multiview_coverage_inventory_v1.json)，SHA-256：`5f4b0277e8e4c53d01e1fb729f1d5566720c9d05aa7f515130aa40b3ef2dfeca`。
