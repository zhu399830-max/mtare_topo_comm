# 坐标修正V2完整接线检查

## 原有监督能否由新输出表示

`check_block_reference_domain.py`核对旧run seal、原参考源码hash及45例参考摘要，未改标签。27个锚点距原点0.04—8.0000999994米，均严格小于10米。新映射逆坐标最大模长1.33338，重构误差最多8.8821e-16米。无需挪动目标、调整窗口或放宽评分；84个窗口开口继续使用原球面接口。

## 完整模型资源与梯度

执行原资源工具新增的显式`--smooth`选项，选择V2，默认旧模型不变。合成57600点、4096块，全链使用原结构损失；RTX5090D前反向0.351003秒，464011参数。GPU分配峰值258227712字节、主机峰值1890922496字节；所有参数梯度有限，冻结观测特征无梯度，参数前后hash一致。0优化器更新，不是训练成绩。

## 下一步

唯一坐标修正实验：固定旧45例和R0/R1/R2各500次更新，原AdamW、损失、1米主评分、0.5阈值及无去重不变。显式核对初始权重逐元素等于旧run共享初始状态，样本序列完全一致；结果必须是新的不可覆盖run。完成新卡/规格/源码环境冻结及preflight后执行，不再重复上述准备。旧run保持原样，失败也不追加种子。
