# 公共特征提取的精确来源范围

2026-09-07。读取6个已有元数据文件完成范围编译；没有读取扫描NPZ、权重或C08—C10内容。当前是运行准备，不是特征提取已完成。

`gse_surface_feature_scope_v1.compile_feature_scope` 绑定封存的输入、选择清单及各自seal，逐项确认两份观察清单一致、每task16观察、各父地图不跨分区。为每个观察记录原NPZ路径与摘要、原行号、五帧和原序列身份；不按模型分数重排/筛选。

实际范围：70父地图、210文件、1120物理边、3360五帧观察、16800源帧；fit/calibration/development为2880/240/240。C07只作历史开发人口，不是严格未见。扫描文件的摘要来自封存manifest，实际字节将在执行时验证。

权重沿用历史observable seed0：选定epoch=2。旧规格的 `frozen_inputs` 记录文件SHA `8d5d2e0ec9779c28b068d0c7db80aeed38ebdb22dff5b781c26234d9001287fb`；训练summary按旧规格摘要核验后确认轮次，不依据新结果选模型。最初错误读取 `source_sha256` 导致元数据阶段KeyError，按实存 `frozen_inputs` 修正后通过，没有模型运行或历史修改。

编译范围SHA（规范化JSON）：`ae11721a299c091f74fff5a211e8b299ecdcbe58e3e3bfe208f36cbd14956389`。

单纯900×128 float32上下文的总大小为1,548,288,000字节（约1.44GiB）；这是数值尺寸计算，不含点坐标、mask、索引、元数据或封存副本，不是实际资源实测。保持逐观察microbatch1，禁止保存展开后的57600×128高维点特征。预计资源和时间仍需正式runner检查，不能套用该数声称全部缓存只有1.44GiB。

下一项为该精确范围的运行器、数据卡和预检；目前未创建正式run，训练和新标签均0。公共特征是可复用准备，不解除标签、A/B/C比较和建图收益的研究缺口。
