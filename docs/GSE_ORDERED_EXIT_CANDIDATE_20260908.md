# 快速交点候选：已实现，尚不能直接导出扫描

复用原生求交，只对排序后的完整事件列表检查每个通道的进入/退出。没有1厘米分组，正间隙不会被直接吞并。重复同源、相切、同距离同时进入/退出、非交替、流未闭合均请求慢参考；同距离多通道退出保留多来源。最大量程不改变检查整个事件流的要求。

代码：src/mtare_topo/teacher/ordered_exit_candidate.py。联合测试24 passed、5 skipped in 0.24s；跳过是测试解释器缺Open3D，不是原生验证成功。

随后在已有Open3D环境运行tools/v3/check_double_fixed_ray_native.py，会话65657 exit0。原异常射线三个原生交点：快速候选与慢V2均为1.9981379508972168米；独立解析1.998137966459527米；旧处理30.04574203491211米。快速候选归约0.000124192秒，不含原生求交和场景构建；不能拿这个数宣称总体加速倍数。

重要限制：有序交点自洽不能证明原生求交完整，也不能证明网格内部只有一个壳层。因此输出明确为candidate，而非qualified；尚无生产快速渲染器接线，不得绕过数据资格检查。下一用原生反例核验快慢对照及参考触发，包括多壳、相切和重复交点。未导出标签、未训练、未覆盖旧run。
