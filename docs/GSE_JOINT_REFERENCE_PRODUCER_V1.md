# 锚点、开口与部分参考归属接线

本次实现`gse_joint_reference_targets_v1.py`，联合已有锚点与窗口开口生产分支，不新增真实读取、批量诊断或训练。

归属正监督只表达构造参考条件下的对应：同一射线是某锚点分支的已绑定进入见证，也是同源开口的排他穿越见证；接口交点位于开口平面内侧；全部开口见证不能存在其他路口竞争或无法对应的射线。未作为正例锚点的原接口也参与竞争，不能先筛掉困难候选再宣称唯一。

没有见证、不同来源、先到开口再到接口、中间多个路口、不同射线支持不同锚点，均输出None，不输出负标签。重复三角形交点不增加权重。输出仅部分参考对应，不证明地面可达性、唯一物理归属或可部署连接，不能直接生成图边。完整区域继续False，身份与射线证据保留在teacher_provenance而非损失记录。

验证：8项新增合成接线测试，与目标适配、两类生产器、方向绑定及损失回归合计61项通过，1.39秒。测试中模拟上游生产结果；真实证据模块另有组件验证，不能将这次合成成绩当作真实标签质量。实际新标签导出0，优化更新0，完整训练资格仍否。

下一项仍为完整局部监督：尽头目标、实际参考质量及有证据的完整评分区域。不能从None批量补False，不能靠只评分正例通过检测F1门。全项目目标未完成，旧资产及论文图片均未修改。
