# 快速候选原生反例：不能直接充当合格返回

命令：env PYTHONPATH=src OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 /home/zeng-workstation/.local/share/mtare_topo_comm/envs/cano_e1_topology_v1/bin/python tools/v3/check_ordered_exit_adversarial_native.py

Open3D0.19.0，8个固定盒体，exit1；未读取研究数据、未训练。失败脚本保留，未改成通过。

| 条件 | 快速处理 | 慢参考 |
|---|---|---|
| 正间隙、分离、重合多来源 | 约2米候选 | 相同 |
| 正长度重叠 | 约30米候选 | 相同 |
| 端面接触 | 请求参考 | 约30米 |
| 内部附加壳层 | 请求参考 | 约2米 |
| 完全重复壳层 | 约2米候选，原生只报告1交点 | 相同 |
| 表面起点 | 约2米候选 | 未知 |

测试失败在“重复壳层必触发回退”断言：该预期不符合原生接口的实际去重行为，不能把它包装成已检测重复壳。更重要的证据是表面起点：事件序列自洽不证明起点满足内部假设。两项均不能通过换容差修补。

停止的是候选直接用于扫描返回，不是全部软件工作。下一实现逐起点原网格/float32占据一致性检查，保留慢参考的数值与边界拒绝规则；按几何与位置绑定复用。此举不能解决原生交点是否完整，后者仍需独立证据。尚无生产快速导出器。
