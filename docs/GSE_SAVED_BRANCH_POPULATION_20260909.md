# 已有开发支路证据：人口已清点，读取适配已完成

没有重新生成地图、扫描或标签。本轮按冻结卡身份读取2676条旧记录，检查源帧对应、路口/尽头排列、接口唯一性、射线索引与分类型见证包含关系；记录总指纹与上一轮一致。工具为`tools/v3/inspect_saved_branch_population.py`，默认只输出摘要，`--details`输出逐父地图和逐记录引用，不写文件。

| 分区 | 观察 | 独立路口 | 路口出现 | 带见证支路出现 | 独立尽头 | 尽头出现 |
|---|---:|---:|---:|---:|---:|---:|
| 拟合，60父地图 | 2310 | 96 | 250 | 774 | 372 | 1139 |
| 校准，5父地图 | 189 | 11 | 27 | 87 | 28 | 84 |
| 开发评价，5父地图 | 177 | 12 | 30 | 90 | 27 | 79 |

独立实体按父地图＋构造节点去重，变体不重复计数。总307次路口、1302次尽头合计1609锚点，与旧汇总一致。951次支路均有非空已保存见证；这是参考证据出现，不是951条独立通道，也不证明全部可见支路已经标全。拟合的10个观察同时含路口和尽头。

支路的进入见证非空次数分别720/81/81，内部见证分别69/18/9，两种可重叠，不能相加当独立支路。当前记录只存这些旧证据，不用后来合成诊断的surface-entry能力冒充旧人口已有能力。

## 接线缺口已定位并处理

原`read_saved_branches`要求位置列表与路口provenance一一对应；真实旧记录将尽头追加在列表后面，因此不能整条直接传入。新增`read_saved_junction_branches`依照已有`terminal_anchor_start`严格检查并读取路口前缀，原记录不变，尽头不变成空支路目标。13项读取器测试通过，0.15秒；尚未声称951方向均已实际绑定。

全人口原记录的interface_ids与witness数组已检查。方向不在这些目标内，仍需原轴线＋原朝向。已逐hash核对一个样本S01/C01 mixed sequence598的V3→V2→V1引用：V1保存接口的endpoint_key/node_id/interface_id和anchor_world_m；这支持复用原`bind_axes`路径，不需要猜测入口或新造方向标签。这里只核查一条引用链，不能称全人口引用链已核验。

## 下一步直接实施

为现有307个含路口的观察生成精确只读方向绑定清单：使用本次固定卡中全部对应记录，不依据模型分数筛选；绑定旧引用链、构造文件及原朝向来源的路径/hash，再调用现有轴线绑定与新混合读取适配。按原fit/calibration/development隔离核对951支路，不增加观察或重新调用教师。结果只证明既有方向证据可读取，不自动改为完整训练资格。

尽头1302次参考独立保留，不因当前支路读取器只处理路口就从最终任务删除。负例和完整性仍须与前一轮明确的条件位置机制分开。后续训练仍须统一卡/规格/评分；不把本轮人口和软件通过写成模型或建图增益。

本轮初次字段打印未过滤嵌套witnesses导致控制台截断，随后改为计数与摘要；所有计数来自完成的全人口工具，不从截断射线输出估计。
