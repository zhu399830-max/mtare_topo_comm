# 源绑定端面精度检查接线

新增 `evaluation/gse_bound_cap_precision.py`。先复用原source/frame/位姿/返回来源和交点检查，再按显式源mesh参数重建端面，核对三角面属于正确端点，独立计算三角形残差并输出逐分支数值资格。只消费原进入候选，不补射线、不生成标签，不改旧生产器。

实际合成代码路径 `tests/v3/unit/check_gse_bound_cap_precision.py` 已执行退出0。每场景五帧，用真实CSG射线渲染、原接口诊断、原来源验证与新适配器，无真实数据读取。

| 合成输入 | 原进入数 | 稳定进入数 | 稳定方向 |
|---|---|---|---:|
| 在源内部，未穿过端面 | 0 / 0 / 0 | 0 / 0 / 0 | 0 |
| 三方向明确端面穿越视点 | 3317 / 3317 / 1864 | 3317 / 3317 / 1864 | 3 |
| 恰在端面 | 0 / 0 / 0 | 0 / 0 / 0 | 0 |

每个场景同时验证输入raw未修改、错误source_sequence_id被拒绝。恰在端面的场景在旧诊断阶段就没有候选，因此只验证空证据不膨胀，**不证明源绑定适配器已在真实渲染中重现S07量化假进入**；量化翻转由已有精确几何单元测试覆盖，S07由此前封存数据只读检查覆盖。

本次测试自身有两次失败，保留原因：第一版错误要求内部视点必须有三条端面进入，实际零条，改为明确的零端面反例；第二版补充视点y=3在有限通道外，被原渲染器拒绝，修为通道内y=1.5。未改实际数据、阈值或生产器；不能把测试预期错误叫作模型失败。

19项分支/区间/原来源/原方向单元测试0.27秒通过。没有第三次真实教师运行、标签导出或训练。

下一步给端面证据增加显式可选的资格路径并用合成生产器检查输出影响；旧默认路径保持可复现，内部/侧面证据不能被端面精度模块一票否定。正式新标签版本仍需独立范围与规格，不能凭此软件通过直接扩训。
