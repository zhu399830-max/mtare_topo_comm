# 补充采样有现成来源，不必重造地图

2026-09-07。用户在收到同C01–C07补样建议后要求“那你想办法呀”，按该方向恢复有界实施。原3360观察、特征、图片和全部旧结果不动。

## 已经实际核对的库存

仅读取70份封存身份记录、210份封存构造、1份原分区和2份seal，共283文件；3.238秒完成。全部来源摘要在读取前及结束时核验。三种几何实现的节点和端点归属完全一致；构造degree2分段不计结构节点。

| 分区 | 独立构造路口 | 独立构造尽头 | 父地图 |
|---|---:|---:|---:|
| 拟合 C01–C06 | 414 | 385 | 60 |
| 校准 C07 | 35 | 31 | 5 |
| 开发 C07 | 32 | 29 | 5 |

以上各实体至少有一条incident物理边对应的穿越保存了合法五帧序列。没有把双向穿越或三种几何实现当独立实体。**这不是有926个合格标签，也不证明每个结构在这些观察中可见。** 源身份仅用于采样分层/标签与独立评分，不允许进入模型候选筛选。

原固定开发批只覆盖5个附近路口和5个附近尽头；现在完整既有库存有32/29个候选结构。这证明有补充选择的现成来源，不保证新观察全部有有效标签。构造/并集不一致仍可能发生，必须保留未知，不靠TNG直接认定端墙。

实现：`src/mtare_topo/data/gse_structural_inventory_v1.py`。复用旧完整因果身份校验，新增三变体拓扑一致、物理端点唯一归属、结构到可用穿越对应；背景候选边保持未标注，不默认负例。24项相关单元测试通过（0.24秒），包括顺序不变性、缺变体/重复归属拒绝、无历史实体保留以及C08拒绝。

本次0激光读取、0模型推理、0新选定观察、0标签、0训练更新。不是一次训练run。

完整按父输出的规范化JSON摘要为 `9d9a02cb35715a78ab0add5cd018f12e9c78916e552c5b0dd694e84b4dd610d7`；283来源映射摘要 `9ebfe7bf32581171f4752e65890f53cf216e2dc61fef1326051756fb8d088609`。身份库存seal为 `d5cb060439bfeef3cd85f5a58ae244e8abf9b5237451a6a59b9311f9225e52e6`，构造P1a seal为 `79fd988ac8c205d74c93e4858b7b579571a06502e778f31634046b48791a0668`。

## 接下来采用的处理

1. 在这份结构库存上预登记按父地图、路口/尽头分层的确定性选择，另保留背景观察；不看模型分数，不反复替换未知样本。
2. 用原位姿记录确认所选因果观察的位置，再冻结精确补充清单。可用穿越不等于观察靠近结构，不凭d0/d1命名猜方向。
3. 对固定补充清单做观测标签核对，构造末端重叠等冲突保留未知。只有达到原人口/评分要求才进入32观察拟合及A/B/C。

不降低10实体门、不换C07分区、不读取C08–C10、不重训旧编码器。新样本若与原3360重合，按task/源序列去重并复用已有特征。原批次与补充批次分别留档；未知不是负例。
