# 开发系统完成核验（不等于论文方法通过）

## 最终结论：本次端到端开发交付完成

v9冻结源码在隔离目录实际复现成功（64359 exit0，70.06秒）：68.214米、12XY到达、2支路目标到达、2返回及2当前方向确认。33.012秒返回→33.412确认→38.412支路目标到达；52.612返回并确认→56.012待办目标到达。17里程节点16记录边。完整隔离工作区保存在 `results/gate6_single_robot/gate6_20260910_gse_direct_isolated_reproduction_v1_seed11`，其中原运行21项seal均核对一致。

逐源核对：294个被几何使用的源帧均存在bag，无缺失；290个决策请求对应289个bag目标时刻，末尾59.612秒请求在shutdown边界未录入目标话题，仍保存在完整决策日志，不计执行完成。最后一个返回仍在进行，未完成状态保留。不能声称零ROS消息丢失或全地图完成。

逐项目标复核：短路线回放有3×10观察原结果；激光、定位、几何、地点任务、记录图、目标选择、原局部规划与执行事件由v9及独立复现证明；原始bag/逐帧决策/图快照/执行事件/配置/日志/源码哈希均存在且已检查；图片已渲染查看；隔离恢复→创建→执行命令已实际执行。由此完成本次有界开发系统交付，不要求几何学习胜出，也不撤销历史失败。

保留限制：非学习几何底座；开发模拟器位姿；局部XY执行；当前方向仅历史水平sector工程代理；无学习优越性、语义真值图精度、全地图覆盖率、物理安全认证、多机器人或论文录用结论。后文是过程审计历史，出现“未完成”指当时状态，本段为最新结案。

最新v9实际通过开发执行合同：68.785米/12XY/2支路/2返回/2重新确认，21项封存核对通过，源码和图片保存。独立实际复现尚待完成；以下旧失败段保留历史意义，不作为v9终态。目标尚未标完成。

最新软件进度：直接支持return_observation与历史窗口几何已分开，调度、deferred和返回重确认接入current_direction_supported；27项针对性测试通过，含历史残留不刷新返回点和历史匹配不构成重确认。下一实际验证尚未执行，目标保持未完成。

## v8原始激光追溯：时间窗口不等于当前可见

只读sealed bag，用原扫描适配器、RangeExitBaseline和原配置逐帧检查：22.14秒检测181°/357°/88°三个方向，22.34秒182°/88°/346°；22.54秒仅188.5°/88°；22.74和22.94秒只剩90.5°。但22.94的五帧几何仍有3个primitive，因为窗口包含22.14和22.34的历史证据。返回39.54—41.54秒逐帧只有约91.5°方向，五帧几何也仅1个primitive；缺失发生在前端方向候选，不是评分或图匹配删掉了正确候选。

原22.94秒位置XY=(32.855,1.554)，返回首次为(32.921,2.032)，停稳为(32.895,1.741)，最终差约0.191米。不能据此证明更精确停车就一定恢复方向：原位置的当前单帧本来就没有那两个历史方向。

目前修复保证的是“几何窗口输出和位置同一时刻”，还没有保证“这个位置的当前观测直接支持该方向”。下一定点实现应保留逐候选当前帧支持，并将返回用的几何及位姿快照绑定到实际直接支持该方向的观测；历史任务不删除，不把窗口继承当新观测刷新，不改方向/距离阈值。先以合成五帧‘移出路口但历史仍包含路口’测试这一接口，再考虑实测。该修复尚未实施。

最新：绑定修复v8的实际运行仍GATE_FAIL，53.525米、11XY到达、1支路目标到达、1返回、0重新确认。绑定时间已一致，40.34秒返回后96次观察都无合格匹配；首次只有一个候选，距保存目标9.682米／方向差112.782°。需要继续定位候选缺失，不能宣称完成。图见[本次真实轨迹](figures/gse_branch_geometry_return_binding_v1/trajectory.png)。

本表核验当前目标的完整交付，而不是扩大为全地图探索或研究优势认证。

| 目标要求 | 已检查的实际证据 | 结论 |
|---|---|---|
| 先有短路线顺序回放 | `gate4_20260910_gse_saved_geometry_replay_v1_seed0/metrics/summary.json`：3种几何实现各10观察，各产生2节点、1记录边和10几何目标 | 已完成给定位姿回放；不是控制实验 |
| 激光→局部几何→任务 | 返回闭环原始bag有294激光、290目标；逐帧记录保留来源和几何提议 | 已运行非学习几何接口，不是训练模型成绩 |
| 地点拓扑及未探索方向 | 最终快照有3个多方向观测地点、16里程导航节点、15记录边，方向保留未尝试／已到达状态 | 已有状态化任务及记录图；地点不冒充精确路口中心 |
| 实际局部规划执行 | 同一bag保存289条局部路径、2884条速度指令；11次XY到达、66.25米累计行驶 | 开发仿真闭环成立；不是物理安全证明 |
| 返回并继续任务 | 原运行25.938秒开始返回，36.738秒返回地点，36.938秒当前几何重新确认并发出目标，40.538秒到达 | 一个完整返回／重确认／执行循环成立 |
| 无跨段伪造边、因果记录 | 21项针对性测试全部通过，包括中间节点、前缀一致、不跨段连接、返回弯折、拒绝无记录捷径及任务身份保持 | 软件合同通过，不替代大样本图精度 |
| 保留原始证据 | 原运行21项SHA-256全部一致；bag、配置、环境、日志、图、任务和执行事件均保留 | 已核验 |
| 图示与解释 | `GSE_GEOMETRY_CLOSED_LOOP_HANDOFF.md`及真实轨迹图片、图片来源哈希 | 已交付 |
| 可复现启动 | 隔离恢复722个冻结源码、补齐工具后实际运行；44.36米、10次XY到达、1次返回，但0次方向重新确认和0次支路目标到达 | 启动成立，完整行为复现失败；目标未完成 |

隔离运行已终止，未重试。原始隔离工作区完整保存到 `results/gate6_single_robot/gate6_20260910_gse_return_isolated_reproduction_v1_seed11/`，其内嵌运行封存哈希逐项通过。23.941秒启动返回，31.941秒到达原地点，之后没有方向重新确认事件。当前证据将问题定位到返回后的重新观测／匹配链，尚未确定是几何变化、目标匹配还是状态机问题，不据此修改阈值。

原闭环完整路径：`results/gate6_single_robot/gate6_20260910_gse_branch_geometry_return_v1_seed11`。短回放完整路径：`results/gate4_topology/gate4_20260910_gse_saved_geometry_replay_v1_seed0`。

## 必须持续保留的限制

## 隔离失败的只读定位

返回目标记录混用了两个时刻：`_remote_candidates`从地点取首次`orders[0]`、observer和navigation_node，而frontier方向、目标与source_refs在`observe`中不断更新到`last_order`。本次返回20.541秒的观察地点，却比较22.741秒的几何方向，数据绑定并不一致。

| 时刻 | 观察位置XY（米） | 最近候选与保存方向的夹角 |
|---|---|---|
| 首次观察20.541 | 31.699，−0.466 | 35.741° |
| 几何最后更新22.741 | 35.469，−0.971 | 0° |
| 实际返回31.941 | 32.165，−0.394 | 23.499° |

返回后140次保存观察全部为零个合格匹配，并非多候选歧义。首次返回的最近目标距离1.384米，满足2米条件，但23.499°不满足15°方向条件；最后仍约1.251米／26.589°。因此状态机持续HOLD与当前合同一致。结束时距返回仅27.8秒，未达到30秒重新观察超时，不能声称超时处理失效。

这是返回目标的时空绑定问题，未证明模型或整个几何路线失败。已实施定点修复：为每个frontier保存与其几何同一时刻的observer、navigation node及order，返回时使用这一组一致记录；地点首次观察位置仍保留其原意。“几何更新后返回来源不得停留在旧时刻”及缺失绑定拒绝等23项相关测试通过，不改变15°／2米条件、不重训。修复尚未通过实际闭环，不能宣称已排除其他原因。

失败中心评分器不参与控制，历史GATE_FAIL不改。当前非学习几何底座不证明几何网络优于基线，不证明严格未见泛化、回环身份精度或探索效率优势。固定时限结束仍有待办，本次没有完整地图覆盖率；地面通行最终由原局部规划与执行链处理，几何方向不是物理可达证明。

开发目标与论文目标分别报告：即使本表全部通过，也只完成端到端开发系统交付，不能自动进入训练、保护测试或多机器人实验。
