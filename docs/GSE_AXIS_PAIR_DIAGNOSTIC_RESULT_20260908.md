# 三维轴对候选：正式诊断结果

运行：`results/gate3_semantics/gate3_20260908_gse_axis_pair_diagnostic_v1_seed0`。预检无错误/警告，单次执行完成，45观察全部保留。CPU耗时4.248秒，峰值RSS431284224字节，0训练、0学习模型推理、0checkpoint读取。56个封存文件哈希核验通过，seal SHA-256为`6b2e4ae2ff89617f5ec2edf81a55e19c40f2b1bbfc5b6bc040dc5e49f75c06b2`。

输入：封存合成45个五帧观察，225帧记录，6类程序形状、3种截面。不是45独立地图，不是真实泛化数据。卡片与命令见`configs/v3/gate3/gse_axis_pair_diagnostic_v1.json`；scope SHA为`c1b7d2be4e0fa871dc515bd422dc9c955b6d35f4e594b7f5125c2a9abf1ac967`。

## 实际结果

| 项目 | 结果 |
|---|---|
| 中央T/Y/四叉路口 | 9/9存在1米内候选，匹配位置误差约0.014–0.035米 |
| 直道 | 12/12无候选 |
| 全人口候选 | 34个，一对一匹配9，多余25 |
| 已可观察的尽头/阻挡锚点 | 18个全部漏检 |
| 1/2/4米总体统计 | 均为匹配9、漏失18、多余25 |

这是候选覆盖初步证据，不是完整检测成功。总体一对一precision为9/34，recall为9/27；没有用事后聚类或角度阈值提升成绩。

## 能解释的问题

中央路口的大多数候选紧邻同一点，来自不同支路配对，故产生重复。圆形/椭圆的T及四叉还有约x=3.94米或-2.24米的候选：近乎共线的轴线仍有数值上唯一的交点。只检查两线最近距离是不够的，它们的分离距离也可以极小；必须保留方向条件和观测支持。

单轴没有配对，因此不产生尽头，这是接口能力缺失，不是把数据漏掉。零直道候选也不能证明任意弯道/遮挡不误检；本人口的中央无障碍路口非常简单。

## 对后续的影响

预登记的中央9例覆盖条件满足，可将观察几何用于结构候选的路线继续评估。但不能直接训练一个声称覆盖所有结构的候选筛选器：当前尽头候选召回为零，筛选无法恢复不存在的候选。

下一先保存同场景候选可视化，并明确完整节点读出的两部分：交汇候选重复/伪交点处理，以及有实际表面支持的终止候选。只有各任务具备相同观测输入、完整背景和明确未知边界，才登记学习对照。不得复制轴线裁剪端点、使用真实尽头坐标播种或只删除尽头类。旧自由查询和偏移投票不重训；旧学习结果作为失败分析保留，不与不同候选合同硬拼排名。

本次不是Gate通过，不证明几何关系学习收益，也没有运行图回放。
