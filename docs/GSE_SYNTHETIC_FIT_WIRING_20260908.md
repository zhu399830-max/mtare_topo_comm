# 合成拟合接线检查：无需重新生成扫描

Phase 3 / Gate 3，下一研究问题仍是几何关系是否改善结构和图。本检查只决定已有合成接口能否接上模型，不是方法比较或真实训练。

## 已实测

`inspect_scored_fixture_loss_targets.py --check-inputs` 核验封存SHA，并用现有投影函数逐例处理45观察、225帧出现、2,592,000射线位置。有效返回同为2,592,000；投影前后有效掩码一致、有效XYZ有限，每个五帧历史都有支持。只读取该固定合成人口，没有C08–C10或真实新标签。

输入只由距离图/50、有效掩码、相对平移和相对yaw组成。构造、来源编号及oracle只在独立监督/绑定中使用，不进入编码器。CPU投影核对不证明CUDA逐元素相同；正式特征设备需明确绑定，不悄悄混合。

宿主 `nvidia-smi` 确认RTX 5090 D可见：总显存32607MiB，检查时占用810MiB。沙箱内命令失败不是驱动实际不可用，不采用CPU训练兜底。

复用历史seed0 epoch2编码器，原 `selected.pt` SHA实际核验为 `8d5d2e0ec9779c28b068d0c7db80aeed38ebdb22dff5b781c26234d9001287fb`，与已冻结特征合同一致。本次没有加载推理或选新checkpoint。

## 接线与成本边界

现有 `FrozenDualPathEncoderAdapterV1.extract_compact_features` 可接三输入；`train_paired_surface_models` 默认调用基础掩码损失，可接专用完整合成目标，无需改真实部分损失上下文。

45例的XYZ＋900×128 float32上下文＋valid净数组共54,432,000字节，不含索引、面片、序列化及训练临时内存；不是训练峰值测量。逐点高维特征只在单微批展开，不导出全人口展开特征。

下一冻结精确45例输入/声明/权重清单及独立合成数据卡，复用现有runner框架准备特征提取和有界拟合，先资源前向/反向检查，再预检与唯一run。拟合基线与方法沿用A/B/C共享初始化和后端；不因合成例容易就更改真实比较人口/验收。当前0新特征文件、0优化步骤，不能估称已训练完成或承诺吞吐。
