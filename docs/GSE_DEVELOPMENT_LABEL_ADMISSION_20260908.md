# 已有开发标签的复用资格：先复用，不重新生成

本次只读旧数据卡、报告和已保存参考记录，无扫描计算、教师生成、训练或测试集载荷读取。当前Phase3的主问题仍是几何关系能否改善结构感知和图。

## 实际核对，而非计划数量

来源：`results/gate3_semantics/gate3_20260908_gse_supplement_joint_v3_seed20260906`。

逐条读取全部2676个`produced_targets`记录，另有210个任务容器（不是额外观察）；2676个task/sequence组合唯一。与原summary核对观察、锚点、开口和正归属总量全部相等。

| 字段 | 实际数量 |
|---|---:|
| 父地图 / 任务 / 观察 | 70 / 210 / 2676 |
| 结构位置参考出现 | 1609 |
| 窗口开口参考出现 | 5568 |
| 正 / 负 / 未知归属 | 1619 / 0 / 724 |
| 完整锚点 / 开口评分区域 | 0 / 0 |
| 完整训练资格 / 人工确认记录 | 0 / 0 |

所有记录列出complete_scoring_regions、reference_target_quality和terminal_correspondence_quality缺口。出现次数不是独立结构数，三变体不作为独立父地图。

summary SHA-256：`3dd30b10487e22b52d999b38f28831a86708201b55d7a666a869a0fe063b55cd`。
按文件名排序、每行“文件名 空格 压缩字节SHA256 换行”的2676记录清单摘要：`585b7ce46dcf60427919b4f2ecf1563aaccc477f6334043636f933ed7303cf98`。这是当前只读复算指纹，不冒充本轮重新验证了整个历史seal。

第一次遍历把任务容器当单观察，KeyError立即停止；第二次显式区分两种schema并报告210容器，未漏删观察，四项汇总断言通过。无历史文件修改。

## 旧资产如何保留

- 原3360固定人口及公共特征：继续保留，不重复编码；它不等于合格结构标签数。
- 新增2676观察：优先复用已有扫描、来源、部分位置及关系证据，不重新生成地图。
- 旧180观察：有成员负例但没有完整候选背景，且只有6个独立交汇、没有尽头；不能把旧成员负例当新局部支路背景。
- 45合成观察：保留已完成三表示拟合及失败图，不当开发泛化数据。
- 新12观察：仅两个构造路口，用于多路口接口回归，不替代70父地图人口。

## 具体缺口与决定

不是“没有数据”，而是已有局部参考还不能处罚所有多余预测。记录中的开口明确为construction-reference window section，不是唯一推断的入口几何。未知不能改成负例；已有正例可复用但不自动获得完整检测资格。

停止本批直接训练准入，不停止整个项目，不宣称几何方法失败。没有新方法、阈值或采样决定需要用户选择。

下一项只核对既有完整区域/排除证据实现及其已运行结果：它能排除的是窗口截面、端面还是局部结构？先确定能否在现有2676记录上复用已有证据表达背景和局部支路未知范围，并列出精确输入清单。不得先运行新teacher，也不得继续加网络模块。若必须新增监督规则，另行明确规则及反例、绑定数据卡后才执行；不把部分任务包装成完整检测或建图收益。
