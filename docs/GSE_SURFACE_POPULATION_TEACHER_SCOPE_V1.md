# 固定人口观测证据：完整读取范围

保留70父地图、1120边级单位、3360观察及16800原五帧源帧。拟合/校准/开发分区不变，普通背景观察不删除，不用空间覆盖结果挑正例。

新scope编译器逐行核对封存选择与输入导出，结果一致。原观测缓存210个NPZ继续复用；教师新增读取210构造、210来源码表、630数组头和4590压缩块，共5640条原始来源路径。字段仅sensor_xyz_m、yaw_deg、primitive_membership_code；学生不得读取这些真实身份字段。

位姿/来源块解码填充总计1523985984字节，属于全任务累计量，不是峰值RAM。实现必须每任务只取80个选中帧、16观察，逐任务释放。共享块按并集计算，不重复虚报为独立帧。没有新增LiDAR渲染或模型读取。

本轮实际仅635项元数据（含既有选择/导出清单及630头）读取，构造/来源/位姿数据块尚未执行读取。三项新测试通过，验证共享位姿块去重、来源块并集及相邻重复帧拒绝。

下一步实现全任务顺序读取器，复用原verify_alignment逐观察绑定学生相对位姿、原始绝对位姿、来源码表和回波；完成合成集成测试后冻结具体观测诊断卡与run。范围清单不授予训练资格，也没有证明实际可见标签人口。C08–C10和正式测试世界不读取。

## 分批读取器已实现

`gse_surface_population_teacher_reader_v1.py` 每任务读取共享的80帧来源与原16观察缓存，逐观察执行原verify_alignment，在16个观察全部核对后才返回整批。只保留当前任务数组，不构造全人口点云特征张量。构造与来源码表保存在teacher-only字段；学生保留原六数组合同。

实际Zarr压缩格式合成测试及既有来源回归18项通过。包含最后一个观察序号错误、位姿偏移、缺少压缩块、重复消费任务和范围外任务拒绝；校验读取文件集与精确范围一致。测试没有读取真实payload或权重，也没有验证任何语义精度。

下一步冻结全人口观测诊断卡和运行规格，使用该reader接现有端面证据并报告背景/未知人口。不因reader通过跳过标签质量及科学门。
