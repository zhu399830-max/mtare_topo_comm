# 首次合成拟合：特征完成，缓存头接线错误，0更新

预检在绑定Torch环境PASS，唯一run `gate3_20260908_gse_synthetic_fit_v1_seed0` 已创建并实际运行。默认系统python预检缺NumPy，换用绑定环境后通过，未改实验。scope仍45观察/225帧/A-B-C各500更新预算。

实际11.050秒停止，主存峰值2,032,963,584字节；45例共享特征全部组装并保存于 `artifacts/shared_examples.pt`，正式优化0。训练入口要求缓存头 `coordinate_frame=current_sensor`，新组装器误写损失标签的 `current_sensor_m`。同一物理坐标、不同schema字面值；不是几何坐标计算错误、数值失败或学习失败。

14个证据文件SHA核验，seal `988e00d2525ce6b31b1bc3f5c265c0bd342bd77d6a41dfe443ab9365c1eadd33`。RUN_STATE=FAILED，不重启、不覆盖、不补成成功。源码快照保留原错误实现。

修复：缓存头专用构造函数使用训练schema并在返回样本前调用真实训练头校验；2回归通过0.72秒，旧错误名明确拒绝，源帧重复明确拒绝。没有放宽训练验证器。

下一独立纠错版本精确绑定封存共享特征，复用数字数组而非重新编码45例，只修头部schema；先全人口重新类型化/入口验证，再新卡/spec一次执行。旧run不重试；训练预算/模型/标签/阈值均不改变。此处只有实现纠错，不代表已经重新运行或获得研究收益。
