# 有界非线性位置检查结论

2026-09-07，固定60候选只读原因检查，不创建训练run或改标签。

中心固定在原位置、弱方向−1米和+1米，分别允许宽高、形状指数及朝向重新拟合。每次最多100次函数评价，计算域半轴0.01—100米、指数2—16、角度±π；这是明确的数值诊断边界，不是地图先验或标签通过门。

必须给原中心同样的形状拟合机会。初次仅对比未经重拟合的源参数得到8个域内偏移残差不更大的结果，这个比较混入了局部截面近似误差，不用于结论。补齐原中心同预算拟合后，实际对照如下：

- 60个原中心拟合与120个偏移拟合，总计1562次函数评价；每个均未超过100次。
- 原中心11个触及计算边界；偏移66个触及边界，全部保留记录，不能据此当作可靠内点解。
- 双方均为内点收敛解的配对比较51个，其中4个偏移1米后的残差不高于原中心重拟合。
- 这4个来自S08 mixed/rounded的edge_0086、S08 rounded的edge_0084以及S09 mixed的edge_0116。

例如S08 mixed edge_0086：原中心重拟合残差0.00416，+1米时0.00357。两者均为无量纲局部隐式残差，不是位置误差或扫描误差。

合成回归验证固定中心位移、预算、独立非线性残差及原中心控制项。源码`gse_center_profile_v1.py`，只读复现命令为`check_window_superellipse_constraints_v1.py --profile-fixed-centers`，273来源文件再次核对。

## 到此收尾的结论

不能凭局部截面拟合残差认证全部中心；也不能说这4个参考中心错误或已找到合法替代世界。没有验证完整射线一致性、扫掠连接、10米球边界条件或全局最优。前次单步线性补偿没有找到替代解，不是唯一性证据。

停止继续追加位置扰动、拟合模型或事后阈值。保留候选及字段未知状态，下一步回到独立观测/标签对应，不把本诊断变成新的科研主线。合格标签和正式训练仍0；原run、数据和图片未改。
