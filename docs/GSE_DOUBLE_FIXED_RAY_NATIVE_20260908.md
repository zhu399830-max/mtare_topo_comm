# 原双路口固定射线原生对照

## 性能分解补充

tools/v3/profile_double_fixed_ray_native.py在同环境实际运行，会话8350 exit0；距离结果保持。V2场景与查询0.347087秒，interval_winding_exit0.296292秒，其内部10次oriented_winding累计0.284663秒，约占V2场景与查询82%。网格生成函数累计0.120917秒。单例profiling不是全人口吞吐；缓存场景不能解决主要逐三角面开销。

决策：慢V2保留为稀疏数值参考，不直接全量。下一验证版本化快速交点区间处理的软件合同，不跨正间隙、不改变多来源，含糊事件走参考或未知。通过独立解析和V2对照前不接入导出。这是加速路径待实现，不宣称加速已经完成。

问题：此前封存扫描的近墙被报告成远墙，是否确实由交点处理导致？

复用声明double_junction__ellipse__view3，历史位置3、射线6306。原点[1.9000000000000001,0,0.04]，方向[0.05232798574151599,-0.9984774386302507,0.017452405410415962]。五个有限通道原网格参数轴向0.05米、截面64边，共323200三角面。初始field占据与独立解析区间逐元素一致。未读取扫描载荷或生成标签。

| 对照 | 返回距离（米） |
|---|---:|
| 独立半空间区间解析 | 1.998137966459527 |
| 旧Open3D交点处理 | 30.04574203491211 |
| 新全射线区间验证 | 1.9981379508972168 |

实际命令：env PYTHONPATH=src OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 /home/zeng-workstation/.local/share/mtare_topo_comm/envs/cano_e1_topology_v1/bin/python tools/v3/check_double_fixed_ray_native.py

会话87745 exit0，Open3D0.19.0。网格生成0.11543秒，旧场景+查询0.05755秒，新场景+查询0.34420秒，进程峰值395696KiB。单次测量，不是稳态吞吐。源码保留完整断言和原始数值输出。

已确认该固定射线修复；未确认其余射线、标签、学习和建图。下一先分离耗时、评估场景与三角面缓存复用，不能把慢速参考直接应用完整矩阵。旧run和论文图片未改。
